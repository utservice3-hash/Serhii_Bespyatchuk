# UTS Dashboard — довідка проєкту

Аналітичний дашборд для відділу продажів логістичних послуг (CRM = Kommo).
Стек: Node/Express + Postgres (backend), React + Vite + recharts (frontend).

> 🧭 **Нова сесія / продовження роботи → почни з `docs/HANDOFF_QUICK.md`** (головна точка входу за 30 сек: стан «Де ми зараз», правила, черга; ОНОВЛЮВАТИ розділ «Де ми зараз» у кінці кожної сесії). Детальний стан + інфраструктура — `docs/HANDOFF.md`. Повний беклог — `docs/BACKLOG_FULL.md`. Макет звітів — `docs/KVP_REPORT_SPEC.md`. Відновлення даних — `docs/BACKUP_RECOVERY.md`.

> 🚧 **ТРИВАЄ RESET ЯДРА (з 13.07.2026).** Порядок робіт — `docs/MASTER_PLAN.md`. Він головний. Словник метрик — `docs/METRICS_GLOSSARY.md` (нормативний). Опис CRM — `docs/CRM_SCHEMA.md`.
> **Зроблено:** КРОК 1 (досинк + бекфіл подій + **бекфіл відсутніх угод** 1.4), КРОК 2 (грошові метрики: анкер по даті входу + дедуп, знімки прибрано — `core/money.ts`). Далі — КРОК 5+ (куб фактів, звірка).

> 💡 **Робочий стиль (за проханням власника):** коли надходить задача під реалізацію — виконати її, а потім ДОДАТКОВО запропонувати, як покращити / що додати / що змінити (погляд керівника КВП, а не лише мовчазне виконання).

> 🎯 **ФІЛОСОФІЯ ДАНИХ (незмінна):** **CRM (Kommo) — єдине істинне джерело; дашборд — його дзеркало.** Будь-яка цифра має відтворюватись із CRM і збігатися з нею; розбіжність — це баг дашборду. Не вигадувати метрики, не згладжувати аномалії.

## 🚨 «УСПІХ ЗА 0 МС» — ПІДПИС АВАРІЇ, ЯКА ТРАПИЛАСЬ ДВІЧІ (10.08.2026)

**Симптом:** банер «дані застаріли», `sync_state` стоїть годинами, а `job_runs`
щопівгодини бадьоро пише `last_success_at = now()`, `last_duration_ms = 0`.
Перший раз 5 год 38 хв, другий — **14 год 52 хв**.

**Механізм (доведено на другому випадку):** прохід `syncKommo` підняв in-process
прапорець `syncRunning = true` і **завис** на запиті до БД (у лозі тієї хвилини —
шквал `REQUEST TIMEOUT (20000ms)`). Далі кожен тік виходив ПЕРШИМ рядком функції,
а `runJob` рахував це успіхом.

🔴 **`finally` рятує від ПОМИЛКИ, але НЕ від ЗАВИСАННЯ.** Обіцянка, що не
завершилась, не виконує ні `try`, ні `catch`, ні `finally`. Прапорець стояв `true`
до рестарту. Це не «рідкісний випадок», це властивість мови — тож будь-який
in-process прапорець зобов'язаний мати **стелю часу**, а не лише `finally`.

🔴 **ПРОПУСК — НЕ УСПІХ.** Джерело правди про здоров'я джоби 30 разів поспіль
казало «все добре» САМЕ ТОМУ, що вона не працювала. Ранній вихід тепер повертає
`jobSkip(причина)` і пише `last_skip_at`/`consecutive_skips`, не рухаючи успіх.

🔴 **УСПІХ НЕ СТИРАЄ `last_error`.** Було `last_error = NULL` при кожному успіху,
тож помилка 18:00:28, що почала аварію, зникла ще до того, як ми про аварію
дізнались. Свіжість визначається порівнянням `last_error_at` із `last_success_at`.

🔴 **ДВА НАГЛЯДАЧІ, ЩО ДИВЛЯТЬСЯ КОЖЕН У СВОЄ, НЕ БАЧАТЬ РОЗБІЖНОСТІ.**
`job_runs` казав «успіх», `sync_state` стояв 15 годин — обидва «в нормі» поодинці.
Ловить лише пряме порівняння двох джерел (`watchSyncDivergence`, поріг 90 хв).

🔴 **СИГНАЛІЗАЦІЯ ПРАЦЮВАЛА. ГУДОК БУВ ВІД'ЄДНАНИЙ.** Нічна звірка о 00:35
зафіксувала `stale, ageMin 425`, `freshnessWatch` відпрацював — і обидва кликали
`sendAdminAlert`, який мовчки виходив: у `backend/.env` **немає
`TELEGRAM_BOT_TOKEN` / `TELEGRAM_ADMIN_IDS`**. Про обидві аварії власник дізнався,
подивившись на екран. **Сигналізація, про несправність якої не сигналізують, гірша
за її відсутність — на неї розраховують.** Тепер стан каналу видно в
`/api/health` → `alertChannel`.
⚠️ Найнадійніший алерт — **зовнішній** монітор на `GET /api/health` (`sync.stale`):
коли лежить сам застосунок, він і повідомити не зможе.

⚠️ **ЛОГ БЕЗ МІТОК ЧАСУ — НЕ ДОКАЗ.** Я знав із БД, що все почалось о 18:00:28,
мав збережений лог із помилками — і не міг сказати, які з них ті самі. Мітки
додано. **Ротацію ми не контролюємо** — файл пише `npm start | tee -a` під панеллю
adm.tools; при аварії ПЕРША дія — скопіювати лог поза цей шлях (саме тут ми
втратили причину першого випадку).

Гейти: `#68` (стеля прапорця — чиста функція, у прод-режимі **не** дозволена до
пропуску) · `#68b` (пропуск≠успіх) · `#68c` (успіх не тре помилку) · `#68d`
(розбіжність джерел) · `#68e` (замок називає тримача).

## 🛑 ПРАВИЛА РОБОТИ (додано 13.07.2026)

0. **ПЕРЕД БУДЬ-ЯКОЮ ЗМІНОЮ КОДУ — ПЛАН НА ЗАТВЕРДЖЕННЯ.** Заміри → план → **СТОП** →
   код. Не «накидаю план і одразу почну», а саме зупинка й очікування «роби».
   Єдиний виняток: правка, яку сам власник назвав **терміновою** й прямо попросив
   зробити без плану.

   **План — не переказ задачі своїми словами.** Обовʼязково: **(а)** файли, рядок
   на файл, нові позначені окремо; **(б)** дані — міграція/бекфіл, скільки рядків,
   чи оборотний, і прямо: **revert коду не відкочує дані**; **(в)** що НЕ робимо —
   це важливіше за «що робимо», саме тут прохід розповзається; **(г)** що може
   зламатись — які ще екрани й запити читають те, що чіпаємо, **поіменно**;
   **(д)** гейти, і на кожен одне речення «якщо зробити X — червоніє» (гейт, який
   не може провалитись, у план не пишеться); **(е)** приймання — який блок, яке
   число, який підпис, і скріншот САМЕ того місця, що змінилось; **(ж)** обсяг —
   рядки й кількість проходів.

   🔴 **ГОТОВОГО КОДУ В ПЛАНІ БУТИ НЕ МАЄ.** Код, показаний «для ілюстрації», —
   це вже порушення: обговорювати написане важче, ніж намір. Дозволено назви
   функцій, сигнатури, форму SQL одним рядком.

   ⚠️ **Якщо план не сходиться** — заміри суперечать задачі або задача нездійсненна
   на наявних даних — **не підганяй план під задачу**. Скажи «так не вийде, ось
   чому» і запропонуй, що можливо. Це очікувана відповідь, а не провал.

0b. **«НЕ ПРАЦЮЄ» І «НЕ ДОЧЕКАВСЯ» — РІЗНІ ВИСНОВКИ.** Перш ніж назвати щось
   зламаним, зʼясуй, **коли воно мало спрацювати за розкладом**, і дочекайся цього
   моменту. Стосується і планових джоб, і **відкладених стартів після рестарту**.

   📐 Спіймано двічі за один прохід 07.08.2026: (1) «рестарт не зрушив синк» —
   насправді відкладений стартовий виклик мав дійти до `syncKommo` аж через ~13 хв
   (сходи +2.5хв, далі +45с, і він у списку ОСТАННІЙ); (2) «плановий тік
   відпрацював» — насправді то був той самий відкладений старт о 05:28, а не крон.
   Обидва рази поспішний висновок вів у неправильний бік діагнозу.

1. **Немає даних у CRM = БЛОКЕР, а не привід підмінити метрику.** Не «спрощуй» визначення, щоб зійшлось.
2. **Заборонені циклічні перевірки** «A = B ✓», де B — це те, чим ти щойно означив A. Приймається тільки зовнішня звірка: наше / ручний лист / Kommo API напряму.
3. **НЕ ВИГАДУЙ бізнес-правил.** Немає в `METRICS_GLOSSARY.md` → це питання до власника. Пиши «ВІДКРИТЕ ПИТАННЯ» і зупиняйся.
4. **Лист каже ЩО рахувати, CRM каже СКІЛЬКИ.** У ручному листі є похибки. Не підганяй CRM під лист.

## 🔬 ПРОМТ — ЦЕ ЗАВДАННЯ, А НЕ ДЖЕРЕЛО ФАКТІВ ПРО КОД (02.08.2026)

Постановник працює **без доступу до репозиторію** і знає код лише з наших описів.
Тому частина тверджень у промті — **припущення, подані тоном факту**. Це не привід
не довіряти; це причина **перевіряти перед тим, як робити**. Той самий принцип, що
ми весь спринт застосовували до системи: **доказ важливіший за правдоподібність**.

1. **Твердження про поведінку коду — ПЕРЕВІРИТИ перш ніж робити.** Не збіглося —
   зупинись і покажи розбіжність. **Не підганяй реалізацію під формулювання промту.**
2. **Рішення про ДОСТУП і ПОЛІТИКУ (хто що бачить; що баг, а що функція) — тільки
   власника.** Виконуй лише те, що прямо позначене як його рішення; решту неси йому
   ПИТАННЯМ, а не здогадом.
3. **Названий механізм / скрипт / команду — перевір, що вони роблять саме те, для
   чого названі.** Не роблять — скажи, а не виконуй буквально.
4. **Числа й посилання на рядки з промту — припущення, поки не підтверджені своїм
   заміром.** Свої попередні числа теж: помилився — виправ вголос (див. «93 → 35»).

**Чому це записано.** За одну сесію промт чотири рази розійшовся з кодом, і кожного
разу буквальне виконання зробило б гірше:
- `pre("/api/dashboard/kvp")` назвали **помилкою патерна** — насправді це правильна
  семантика (сусід через дефіс є ІНШИМ роутом); «виправлення» зламало б межі;
- **9 із 16** запропонованих мапінгів роут→вкладка були неправильні — вкладку
  визначає ФРОНТ (який екран кличе роут), а не співзвучність назв;
- `--build-links` назвали механізмом, що догонить покриття `client_key` — він
  **за побудовою виключає `funnel_stage='paid'`**, тобто саме ті угоди, заради
  яких його кликали; прогін дав нуль зрушення;
- команду приймання кілька разів писали як `TEST_SCOPE=prod npm test` — вона
  **обходить read-only preload** і пустила б набір у прод із правом на запис
  (правильна — `npm run test:prod`; обхід тепер закритий гейтом у `db/pool.ts`).

⚠️ **Порядок дій, коли промт розійшовся з кодом:** спершу зроби ту частину, що не
залежить від спірного твердження; розбіжність покажи ЦИФРОЮ або цитатою з коду;
свою пропозицію дай окремо. Не «мовчки зробив по-своєму» і не «зробив буквально,
бо просили».


## Деплой і інфраструктура

**Уся процедура деплою, доступи та налаштування середовища — `docs/INFRASTRUCTURE.md`.**
Тут навмисно нічого про це немає.

Що варто знати з цього файлу:
- Гілки: розробка — `claude/dashboard-development-54e8ng`; прод тягне `claude/friendly-galileo-8pijhl`. Тримати в синхроні.
- Довгі збірки падають по таймауту в bash-tool, але серверна команда доходить — перевіряти результат окремо.
- Важкі backfill-джоби ганяти у фоні з поллером, **не паралельно з прод-синком**.
- **⚠️ ПЕРЕД будь-яким важким бекфілом — перевір залишок compute-квоти Neon і скажи власнику, скільки він з'їсть.** Бекфіл 35к угод (КРОК 1.4) випалив Neon compute-квоту за годину і поклав прод у `sync:null`. Наосліп важкі бекфіли більше не ганяти: оцінити обсяг, тротлити/дрібнити батчі або окремий compute-ендпоінт.
- **⚠️ ВАЖКИЙ `UPDATE` ПО `deals` — ПІД `job_locks` АБО ПОЗА ВІКНОМ СИНКУ.** Міграція
  `client_key_raw` (одноразовий `UPDATE` по 146 тис. рядків) впала з **дедлоком**: вона
  й `syncKommo` брали ті самі рядки в різному порядку. Повтор пройшов — але ставка тут
  не в тому, що «з другого разу спрацювало»: **дедлок відкочує ВСЮ транзакцію**, і
  напівзастосована міграція виглядала б як застосована, якби далі стояли інші
  оператори. Правило: перед масовим `UPDATE`/`DELETE` по `deals` (і будь-якій таблиці,
  що її пише синк) — або взяти `job_locks`, як роблять важкі Kommo-джоби, або ганяти
  у вікні, де синк не працює (`KOMMO_PAUSED`), або дрібнити батчами з `ORDER BY` того
  самого ключа, що й синк. Перевірити результат ОКРЕМИМ запитом, а не за виводом
  `Migration applied.` — він друкується й тоді, коли частина роботи відкотилась.

## 🔌 РЕСТАРТ ПРОДА — `kill -TERM` ЧЕРЕЗ RELAY (переписано 25.08.2026)

Виняток із правила «тут нічого про деплой»: наступний, хто виконуватиме викат, упреться
саме в цей крок, і без запису витратить на нього прохід.

🔴 **ПОПРАВКА 25.08.2026, І ВОНА СКАСОВУЄ ГОЛОВНЕ ТВЕРДЖЕННЯ НИЖЧЕ.** Заголовок цього
розділу читали як «рестарт лише через підтримку», і два чати незалежно вперлись у те, що
це неправда: **`kill -TERM` через relay працює** (заміряно на `f5f5b81` 24.08 і `9ec05dc`
25.08, health віддає новий sha за ~8-15 с). Замір 17.08 нижче — **не помилковий, а
вузький**: він робився з SSH, і для SSH усе в ньому вірне.

| канал | `ps -C node` | `ps -eo args` | `kill -TERM` |
|---|---|---|---|
| SSH | порожній | процесу не видно (інший namespace) | ❌ |
| **relay** | **порожній теж** | ✅ видно `node dist/index.js` | ✅ **працює** |

`ps -C node` порожній в ОБОХ каналах — саме він і створив враження, що pid не існує.
Команда, порядок дій і пастка `pgrep -f` (матчить власний командний рядок) — у
`docs/INFRASTRUCTURE.md` §5. Підтримка лишається ЗАПАСНИМ шляхом, кнопки Supervisor —
не тиснути.

🔁 **РЕСПАВН ПІСЛЯ `exit 1` — ЗАМІРЯНО 25.08.2026, ПИТАННЯ ЗАКРИТО НА 90%.**
Раніше тут стояло «не закривати це питання поточною поправкою» — тепер є замір.

**Головне:** `bash -c '… npm start 2>&1 | tee -a log'` віддає **0** І при `exit 1`,
І при вбивстві сигналом (заміряно в контейнері на тій самій формі; з `set -o pipefail`
дало б 1, а `PIPESTATUS=1 0` показує, що код не втрачений — він просто не стає кодом
пайпа). Отже **аварійний вихід і штатна зупинка НЕРОЗРІЗНЕННІ ззовні**, і нагляд
поводиться з `exit 1` так само, як із нашим `kill` — після якого процес підіймається.
Незалежно від цього `app_boot` за 23.08 показує **17 стартів поспіль** із каденцією
~60 с, без наростання паузи й без відмови піднімати; епізод розсмоктався сам.

**Практично:** `exit 1` НЕ означає «лежачий сайт до підтримки», а означає «лежить,
підіймається знову, цикл ≈31 с ретраїв + до ~60 с, вичухується сам, щойно Neon
відповість». Fail-closed роль-кеш свою умову існування має.

⚠️ **Решта 10%, названа чесно:** `app_boot` веде записи лише з 21.08.2026, і **всі 17
крахів — це ОДИН епізод**. Доведено «17 разів поспіль не здався»; НЕ доведено, що
порога відмови немає взагалі, і що причиною того епізоду був саме `exit 1`, а не
вбивство дубля процесу хостом (інтервал 1-2 хв збігається). Деталі — `docs/INFRASTRUCTURE.md` §5.

**Ані SSH, ані кнопки панелі процес не рестартують.** Заміряно під час викату `878600b`:

| спосіб | що сталось |
|---|---|
| `kill -TERM $(pgrep -n node)` із SSH | **неможливий:** `ps -C node` порожній — процес в ІНШОМУ namespace, SSH його не бачить |
| Supervisor → «Відновити» / «Перезапустити» / «Зупинити» | кнопки **не подіяли** на процес |
| **звернення в техпідтримку adm.tools** | ✅ **єдиний робочий спосіб** — так і зробили |

🔴 **І НАЙВАЖЛИВІШЕ: ПРОБНИЙ РЕСТАРТ ЦИМИ Ж КНОПКАМИ «ПРОЙШОВ».** Напередодні на
`9fec609` кнопка «Відновити» відпрацювала, health віддав 200, застосунок повернувся —
і саме тому в план викату вона потрапила як доведений механізм. На справжньому викаті
не спрацювала. Тобто механізм **ненадійний, а не відсутній**, і його власний зелений
відгук нічого не доводить — рівно той самий клас, що «успіх за 0 мс» і «папка бекапу є,
копії немає».

✅ **ЄДИНЕ ДОСТОВІРНЕ ПІДТВЕРДЖЕННЯ РЕСТАРТУ — `version` у `/api/health`.** Не «кнопка
позеленіла», не «pid змінився» (pid ми взагалі не бачимо), а sha в health, що дорівнює
щойно зібраному. Поки він старий — рестарту НЕ БУЛО, скільки б разів кнопка не
відзвітувала про успіх. Проміжний сигнал того ж: `buildStale: true` між `npm run build`
і справжнім рестартом.

⚠️ **ПЛАНУВАТИ ВИКАТ З ЦИМ КРОКОМ ЯК ІЗ ЗОВНІШНЬОЮ ЗАЛЕЖНІСТЮ.** Між збіркою й
рестартом прод крутить старий код при новому `dist` на диску, і тривалість цього вікна
залежить від чужого часу відповіді. Отже: збирати тоді, коли є з ким зв'язатись, а не
«зберу зараз, рестартну колись».

❓ **ВІДКРИТЕ ПИТАННЯ, ЯКЕ ЦЕ ПОРОДИЛО:** нижче в цьому файлі (розділ про fail-closed
роль-кеш) записано, що умова існування рішення `loadRolesOrDie()` → `exit 1` — це
супервізор, який підніме процес заново. **Тепер це НЕ перевірено.** Якщо кнопки не
діють, невідомо, чи спрацьовує автоматичний респавн після падіння — а якщо ні, то
`exit 1` означає лежачий сайт до звернення в підтримку. Не «полагодити» здогадом:
з'ясувати в adm.tools, як саме керується процес, і аж тоді вирішувати.

## Структура фронту

`Dashboard.tsx` — контейнер (увесь стейт/ефекти/хендлери). Секції винесені в `pages/dashboard/sections/*` (Overview, Report, Teams, Managers, Loyalty, Receivables, Tasks), спільне — `pages/dashboard/{format.ts, constants.ts, taskForm.ts, widgets.tsx}`. statistics/leadgen/settings/messenger/news/training лишаються інлайн у контейнері.

## Ключові бізнес-правила

### ГРОШОВІ МЕТРИКИ ✅ (виправлено КРОК 2, 14.07.2026 — анкер по даті входу, знімки прибрано)

#### 🟢 ПРАВИЛО ВЛАСНИКА (02.08.2026): ДВІ МЕТРИКИ, ① — ОСНОВНА СКРІЗЬ

Дослівно, з причиною:

> **① РЕЗУЛЬТАТ МЕНЕДЖЕРА = виключно етап «успішно реалізовано» (142).**
> Причина: частково оплачені угоди автоматично переносяться в наступний місяць.
> Зараховувати їх людині зараз = рахувати незавершене. Це метрика **ОЦІНКИ РОБОТИ**.
>
> **② ГРОШІ КОМПАНІЇ ЗАРАЗ = «оплата отримана» ∪ «успішно реалізовано».**
> Причина: для розуміння ситуації важливо, скільки коштів фактично в компанії.
> Це метрика **СТАНУ**.
>
> Уточнення того ж дня: **орієнтуємось ЗАВЖДИ на «успішно реалізовано»**. ② — не друга
> рівноправна метрика, а **допоміжний орієнтир**, щоб бачити картину загалом.

**Розбіжність між ними — НЕ баг**, доки кожен екран чесно підписаний, якою саме
метрикою рахує. Без підпису дві правильні метрики читаються як поломка.

| | функція ядра | де |
|---|---|---|
| **①** | `successMoney` · `successByMgr` · `successByTeam` · `successByBucket` · `successByManagerBucket` · `successByManagerWeek` · `successByManagerMonth` | Звіт (факт/%/світлофор/needPerDay/прогноз) · plans-grid · Огляд «Виконано плану» й «Історія за 3 місяці» · `/personal` (місяць, поденно, 12 міс, прогноз) · `/loyalty` «Замовлень/Сума (міс.)» · `/managers` (тижні) · лідерборд месенджера |
| **②** | `receivedMoney` + 11 розрізів | **лише** Огляд «Отримані кошти» (підписана як орієнтир) · AI `get_metric` (три метрики названі окремо) |

**② більше ніде не з'являється.** Натрапив на місце, де вона потрібна за сенсом, —
**не додавай, спитай.**

⚠️ Це рішення власника від 02.08.2026; **може переглядатись**, якщо з'являться
розбіжності. Записано дослівно саме тому, що торік ми вже плутали ці два сенси.

#### 🔴 ЗМІНА 06.08.2026: КАРТКА МЕНЕДЖЕРА ПЕРЕВЕДЕНА НА ② (скасовує п. вище для Звіту)

**Рішення власника, і воно ЗМІНЮЄ формулу від 02.08, а не уточнює її.** Записано
поруч, а не замість, — щоб було видно, що саме переглянуто і чому.

> **Факт місяця = ① + «оплата отримана», тобто ② з дедупом.**
> **Прогноз місяця = факт + очікування з ПЛАНОВОЮ ДАТОЮ ОПЛАТИ в цьому ж місяці.**

**ЩО ЦЕ ЗЛАМАЛО, І ЧОМУ ЖОДЕН ТЕСТ НЕ ЧЕРВОНІВ.** Картка Антипенка за серпень 2026
показувала факт місяця **0 ₴**, тимчасом як тижневий блок ТОГО САМОГО екрана
показував **23 632 ₴**: десять угод оплачені, жодна не переведена в 142. Місяць
рахував ①, тиждень — ② (`plans.effectiveWeekTargets` завжди був на `receivedByMgr`).
Незавершена міграція коміту `989644f`. **Кожна метрика ОКРЕМО була правильна** —
тому ловити не було чим, доки не спитали «а це взагалі ОДНА метрика?».

**🔴 ЦЕ БУВ НЕ ОДИН ВИПАДОК.** Замір на живому проді 06.08: **12 із 31** менеджера
мали тиждень > місяця. Шевчук Назар — місяць 0 при **71 896 ₴** оплачених,
Семенюк Дмитро — 0 при 39 215 ₴. Тобто «дивна картка однієї людини» була
симптомом системного розсинхрону, а не її особистим випадком.

⚠️ **НЕ ПЛУТАТИ ЦІ «12» З ІНШИМИ ЛЮДЬМИ — ЦЕ ДВА РІЗНІ ЗАМІРИ, І ми вже раз їх
сплутали** (07.08, обидві сторони, у промті приймання):
- **12 із 31** — у кого ТИЖДЕНЬ > МІСЯЦЯ на СТАРІЙ формулі (симптом розсинхрону ①/②);
- **6 із 31** — у кого «гроші є · не закрито» (`factSuccess = 0`, `factPaid > 0`)
  на НОВІЙ формулі. Заміряно 07.08 на живому проді: Мокляк, Шаврова, Антипенко,
  Свіржевський, Семенюк, Шевчук.

Перетин є (Семенюк, Шевчук), але множини різні, і критерій різний. Приймання
відкату рахується по **шести**.

**Що поїхало разом із фактом (навмисно, інакше екран знову розказував би двома
голосами):** %, світлофор, `needPerDay`, прогноз, **спарклайн** (`successByManagerBucket`
→ `receivedByManagerBucket`).

**🟡 ПОБІЧНИЙ НАСЛІДОК, НАЗВАНИЙ ВГОЛОС: добір нового бізнесу випав із прогнозу.**
Стара формула була `факт + УСЯ зона (без дати) + добір`; нова — `факт + очікування
цього місяця (за плановою датою)`. Контрольне число власника (Антипенко ≈68к)
сходиться **лише без добору**: 23 632 + 44 282 = **67 914**. Добір у Антипенка —
**67 847 ₴**, по компанії 28 менеджерів на **1 599 273 ₴**, тож це не нуль і не
округлення. Реалізовано за цифрою власника, але добір лишився ВИДИМИМ полем
(`dobir`) — повернути його в прогноз можна одним словом.

#### 🧺 КОРЗИНИ ГРОШЕЙ — `core/moneyBuckets.ts` + гейт `#45`

Кожен статус обох FC-воронок належить **рівно одній** корзині: `closed` (142) ·
`received` (етап 9) · `awaiting` (`EXPECT_ZONE`) · `operational` · `lost` (143).
Статус без корзини = **наступний Антипенко**, і саме це `#45` і ловить (саботаж
доведено: прибрати `69716460` з «гроші прийшли» → червоне з назвою «Оплата отримана»).

- Реєстр **нічого не імпортує** з `money.ts`/`metrics.ts` — ті тягнуть `db/pool.js`
  → `config.js`, який кидає без `DATABASE_URL` ще НА ІМПОРТІ. Щоб це не було
  забороненою перевіркою «A = A», окремий `#45c` (лінивий імпорт) звіряє реєстр із
  реальними константами ядра.
- **Операційні перелічені ПОШТУЧНО, без catch-all.** Catch-all зробив би гейт
  беззубим: новий money-статус тихо осів би в «операційних».

**🟢 ЗАКРИТО 06.08.2026 (рішення власника): `100274340` «Виставлення рахунку»
ЛИШАЄТЬСЯ В ОЧІКУВАННІ.** Ціна питання заміряна перед рішенням: серпень по
компанії **578 801 ₴ з нею проти 329 248 ₴ без неї**; липень — 427 160 проти
392 796. Разом із рішенням — **обовʼязковий видимий розріз заторів**: із 578 801 ₴
очікувань **249 553 ₴ (43%)** сидять саме на цій стадії, а в Шаврової це **100%**
її очікувань. Поки рахунок не виставлений, дата оплати — **намір, а не
домовленість**, і екран мусить це казати (`core/reportCuts.invoicingJamByManager`,
картка «Затор» у верхньому підсумку).

#### 🗓 ДИНАМІЧНИЙ ПЛАН ТИЖНЯ — БАЗИС РОБОЧІ ДНІ + ЗАМОРОЖЕННЯ (рішення власника 06.08.2026)

```
planWeek = залишок × (робочих днів у цьому тижні)
                   ÷ (робочих днів від початку тижня до кінця місяця)
залишок  = план місяця − факт ② на ПОЧАТОК тижня
```

🔴 **НЕ «÷ тижні, що лишились».** Останній «тиждень» серпня 2026 — це ОДИН день
(31.08). Поділ на тижні дав би йому ПОВНУ тижневу норму, тобто свідомо недосяжну
ціль. Спостережний наслідок, на якому стоїть саботаж-гейт `#48b`: за робочими
днями **денний темп цілі сталий** у всіх тижнях, за тижнями — вилітає саме на
обрізаному. Приклад власника (план 100 000, 21 роб. день) звірено дослівно:
Т1 = 23 810 · Т2 = 25 000 · Т3 = 25 000 · Т5 (1 день) = своя одноденна частка.

🧊 **ЗАМОРОЖУВАННЯ.** План тижня фіксується РАЗ у понеділок 00:00 **за Києвом**
(`weekly_plan_snapshots`, `ON CONFLICT DO NOTHING`) і всередині тижня не
змінюється: ціль, що повзе, наздогнати неможливо в принципі. Другий, важливіший
сенс — **минулі тижні не переписуються заднім числом**. Тримає `#48e`.
- `source='live'` — зафіксовано в момент; **`source='backfill'` — ВІДНОВЛЕНО
  ретроспективно**, і UI зобовʼязаний це показати (`reconstructed`). Факт «на
  початок тижня» відновлюється з ПОТОЧНОГО стану подій, тож для угод, що відтоді
  переїхали, число відрізняється від баченого тоді. Приховати це = тихо збрехати
  про історію.
- Перевиконання: залишок ≤ 0 → план 0 + «понад план +N ₴» (`#48d`), без ділення
  на нуль і без порожньої шкали.
- Підміна стоїть у `plans.effectiveWeekTargets` — **ЄДИНОМУ** виразі тижневої цілі
  для `/report-plan`, `/kvp-report` і `/manager-report`. Правити три роути окремо
  означало б завести три цілі, які розійдуться мовчки.
- `plans-grid` (місячний план) НЕ чіпали: тижневий похідний від нього.

#### 🧾 БОРГ: ДВА КАЛЕНДАРІ РОБОЧИХ ДНІВ (зафіксовано 06.08.2026, рішення власника — ЛИШАЄМО)

У продукті **два різні означення «робочого дня»**, і вони дають різні числа:

| де | функція | Пн–Пт | держсвята (`company_holidays`) | погоджені відсутності |
|---|---|---|---|---|
| «треба ₴/день», «N дн.», **план тижня**, темп місяця | `core/dates.workingDaysBetween` | ✅ | ❌ **не враховує** | ❌ |
| денна ціль (`decomposeTarget.dayTarget`), `presentDaysLeftWeek` | `plans.presentWorkingDaysByManager` | ✅ | ✅ враховує | ✅ віднімає |

**Рішення власника: лишаємо як є.** Розбіжність існувала ДО роботи над планом
тижня і нею **не створена** — план тижня свідомо став на той самий календар, що
вже живить «треба ₴/день», щоб не заводити ТРЕТІЙ.

🔴 **Чому вирівнювання — окрема зміна зі своїми замірами, а не «дописати свята»:**
`workingDaysBetween` стоїть під **темпом місяця, міткою на смузі, `needPerDay`,
розподілом місячного плану по днях і планом тижня одночасно**. Додавання свят
зрушить їх ВСІ в один момент — тобто змінить цифри на кожному екрані, включно з
історичними періодами, де свята вже минули. Це зміна поведінки з власним
прийманням, а не технічна правка. Роботи планувати окремо; тут — лише запис, щоб
через півроку ніхто не «полагодив» це побічним ефектом.

⚠️ Практичний наслідок, який треба памʼятати вже зараз: у місяці зі святами
`needPerDay` і план тижня **занижені** (дні рахуються, хоч працювати ніхто не
буде), а `dayTarget` — ні. Тобто дві цифри на одному екрані розходяться саме в
такі місяці, і це ВІДОМО, а не баг.

#### 🔴 ЗВІЛЬНЕНІ З ГРІШМИ — ЗГОРНУТИЙ РЯДОК, А НЕ ЗНИКНЕННЯ (гейт `#50`)

Ростер Звіту фільтрує `m.is_active`, а грошові функції ядра — **ні** (правило
«`is_active` керує списками, а не історичними сумами»). Тобто щойно менеджера
деактивують у Kommo, його рядок зникає з екрана, **а гроші лишаються в
`receivedByTeam`** — і `Σ(менеджери)` перестає дорівнювати команді. Ламалось би
це ТИХО: жодне окреме число не виглядало б дивним.

Тому `/report-plan` віддає окремий масив `dismissed`: **без плану, %, світлофора
й темпу** (ставити план людині, якої немає, безглуздо), але їхні `①`/`⑨`
**входять** у `glance` і в суму команди. Гейт `#50` перевіряє сильніше твердження
— `Σ по екрану == ядру`; якби рядок прибрали, він почервонів би сам.

📐 **Заміряно 06.08.2026: список ПОРОЖНІЙ, і це не помилка.** Жодна угода на етапі
«оплата отримана» не висить на деактивованому акаунті; `②` за серпень на
деактивованих = **0 ₴** (при 171 деактивованому менеджері в базі). Механізм
стоїть **заздалегідь** і вмикається сам. Дзеркало `#50b` окремо доводить, що
предикат не вироджений — деактивовані в базі є, тож нуль означає «немає грошей»,
а не «немає кого перевіряти».

#### 🔴 АКТИВНІСТЬ МЕНЕДЖЕРА МАЄ **ДВА** ДЖЕРЕЛА (знайдено 06.08.2026)

Це не помилка обчислення, а **два визначення однієї речі** — той самий клас, що
`NULL` у `CHECK` і несвіжий `dist`.

| прапорець | таблиця | хто пише | що вимикає |
|---|---|---|---|
| `managers.is_active` | `managers` | `syncKommo` з Kommo `/users` | **усю аналітику**: ростери, списки, вибір виконавця |
| `users.is_active` | `users` | **адмін у «Налаштуваннях»** (`PATCH /settings/users/:id`) | **вхід** у дашборд · трекер · месенджер · списки налаштувань · admin-guard |

**ЯК МИ НА ЦЕ НАСТУПИЛИ.** Власник деактивував Шевчука Назара **в
Налаштуваннях** (`users.is_active=false`, 06.08 11:11, «деактивовано адміном»),
а в Kommo він лишався активним. Я дивився на `managers.is_active`, бачив `true`
і не міг зрозуміти, чому власник каже «звільнився». **Обидві таблиці казали
правду — про свій прапорець.**

🔴 **І ПАСТКА ВИЯВИЛАСЬ ДЗЕРКАЛЬНОЮ ДО ОЧІКУВАНОЇ.** Побоювання було «гроші
зникнуть з екрана, лишившись у сумі команди». Перевірено поіменно: `users.is_active`
не згадується **в ЖОДНОМУ аналітичному запиті** — лише `routes/auth.ts:42` (вхід),
`routes/tracker.ts:50`, `routes/messages.ts:53`, списки в `settings.ts` і
`auth/adminGuard.ts`. Тобто деактивація в дашборді **не прибирала людину зі Звіту
взагалі**: вона стояла з планом, відсотком і світлофором, а її нуль тягнув команду
вниз. **Дія власника просто не мала наслідків там, де він їх очікував.**

**Виправлено:** ростер Звіту переведено на `core/activeManager.activeManagerSql()`
— «активний за ОБОМА джерелами». Той, у кого знято будь-який прапорець, випадає з
ростера й потрапляє в рядок «звільнені» **разом зі своїми грошима**.
- Менеджер **без логіна взагалі** лишається активним: інакше з ростера випали б
  усі, кому просто не заводили доступ.
- ⚠️ **МЕЖА СВІДОМА:** предикат підставлено **лише** в ростер `/report-plan`.
  `managers.is_active` фігурує ще у ~20 запитах — переводити їх усі на подвійну
  ознаку це окрема зміна поведінки з власним прийманням.
- Тримають `#51` (чиста функція) · `#51b` (SQL == функція на живих даних) ·
  `#51c` і `#50c` (**саботаж саме деактивацією в дашборді**, у транзакції з
  гарантованим ROLLBACK).

#### 🖱 ЗЕЛЕНІ ГЕЙТИ ТРИЧІ ПОСПІЛЬ ПРОПУСТИЛИ ТЕ, ЩО ВИДНО ЗА СЕКУНДУ (06.08.2026)

| що зламалось | чим ловилось | чим НЕ ловилось |
|---|---|---|
| сторінка без стилів | скріншот приймання | health 200, версія, усі гейти |
| плитка «31 менеджер», чипи на 37 | погляд на екран | `#50` (гроші), `#46` (періоди), матриця |
| **клік не розгортає деталь** | **клік у Playwright** | **202 тести** |

Спільне: перевірялось те, що ЛЕГКО перевірити, а не те, чим людина користується.

🔴 **У ПРИЙМАННЯ НАЗАВЖДИ ДОДАНО SMOKE КЛІКОМ** (`#57`/`#57b`): відкрити Звіт →
розгорнути рядок → переконатись, що деталь **із даними**, а не порожня. `200` із
порожнім тілом виглядав би так само зелено, як робочий екран.

📐 Причина регресу 06.08 названа поіменно: псевдонім `day` **без `AS`** у двох
нових запитах `core/reportCuts.ts`. `day` — ключове слово Postgres, тож
`to_char(…) day` дає `syntax error at or near "day"`; роут віддавав **500**, а
стрілка лишалась на місці. Сусідній `metrics.expectedByManagerDay` увесь час
стояв із `AS day` — правильний зразок лежав поруч.

#### 🧾 БОРГ: ІСТОРІЇ ПРОГОНІВ ДЖОБ НЕМАЄ (записано 06.08.2026)

`job_runs` — таблиця **поточного стану**: `PK = name`, один рядок на джобу
(`last_success_at`, `last_error`, `last_duration_ms`). **Історії немає**, тож
питання «скільки разів джоба відпрацювала за N годин» **не має відповіді в
принципі** — видно лише вік останнього успіху.

Супутній факт, виведений із коду: `syncKommo` **останній** у сходах відкладеного
старту (15 джоб, перша через 150 с, далі +45 с) → **boot + 13 хв**. Отже при
рестартах частіше ніж раз на 13 хвилин догоняючий старт **не встигає жодного
разу**, і лишається тільки cron-тік.

🟢 Що при цьому ПЕРЕВІРЕНО і працює: `node-cron` `*/30 * * * *` прив'язаний до
**стінного годинника** — рестарт розклад НЕ зсуває (замір: `syncKommo` о `17:00:29`,
`freshnessWatch` `:50`, `syncStageEvents` `:45` — усі на своїх хвилинах).
Сигнал «синк мовчить» теж є: `freshnessWatch` щогодини `:50` будить Telegram при
відставанні >2× частоти.

#### 🔒 ЗАДАЧНИК — ЗОВНІШНЯ ДЛЯ ЗВІТУ СИСТЕМА (вимога власника 06.08.2026)

> **Її поля й логіка не змінюються правками Звіту; будь-яка зміна контракту —
> окрема задача з окремим прийманням.**

**НАПРЯМОК ОДНОСТОРОННІЙ: Звіт ЧИТАЄ, ніколи не пише.** Перевірено — у тілі
`/report-plan` **нуль** `INSERT/UPDATE/DELETE` по `tasks`. Замикає `#56c`.

**Що приходить із Задачника** (6 полів × 2 блоки на картці менеджера):

| поле | план (target) | факт |
|---|---|---|
| реклама · лідоген · авто · чек · конв | `tasks.metrics_json` → `ads_count`, `leadgen_count`, `dispatch_count`, `avg_check`, `conversion` | ядро (`metrics`/`money`) |
| гроші тижня | `payment_amount` з `kpi_period`-парасольки **або** динамічний план | `money.receivedByMgr` |

- **План** — `tasks`: парасолька `task_type='kpi_period'` + приховані діти
  `daily_kpi`. Гібрид: де є парасолька — її таргет, де немає — Σ дітей
  (`routes/dashboard.ts`, `accum`/`planByMgr`).
- **Ручна ціль тижня**: `plans.effectiveWeekTargets` → `target = manual ?? dynamic`,
  де `manual` = `payment_amount` з парасольки, що **покриває сьогодні**.
  📐 Заміряно 06.08: ручну ціль мають **17 із 31**, і в них `manual ≠ dynamic`
  (Андрусенко 135 000 проти 32 143) — тобто підміна виразу одразу видима.
- ⚠️ **ПІДПИС ПЕРЕПИСАНО НА ЧЕСНИЙ** (рішення власника 06.08.2026): було
  «синхронізовано із Задачником» — і це неправда, живої двосторонньої
  синхронізації немає й не було, це статичний `<span>`. Стало **«дані з Задачника ·
  лише читання»**. Той самий клас, що «знімок відновлено» на ручній цілі: підпис
  правдоподібний, а величина за ним інша. Тримає `#56`.

**Файли контракту — у наступних проходах НЕ редагуються** (торкнувся → спершу
скажи чому): `routes/dashboard.ts` (`/report-plan`: `umbRows`/`dayRows`/`accum`) ·
`core/plans.ts` (`effectiveWeekTargets`, `dynamicTarget`, `decomposeTarget`) ·
`routes/tasks.ts` · `jobs/evaluateKpiTasks.ts` · блоки «Тиждень · задача з
Задачника» і «Місяць · показники» в `ReportPlanSection.tsx`.

**Замок `#56`/`#56b`/`#56c`** — контракт, а не текст коду. Перевіряє І API,
І **джерело фронта**: поле може лишитись у відповіді й зникнути з екрана — це
дзеркальна пастка до `#55` (де API не бачив дубля у верстці).
🧨 Саботаж доведено **дією, в обидва боки**: прибрав «лідоген» із блоку →
`#56` червоніє; підмінив `manual ?? dynamic` на `dynamic` у КОПІЇ прод-збірки й
прогнав проти живої БД → **17 із 17** менеджерів дали `target ≠ manual`.
🧾 Чесна межа: «дійшло до пікселя» вимагає браузера, а на прод-сервері його немає,
тож перевіряється «поле рендериться в компоненті».

#### 📏 ЖОДЕН ПОКАЗНИК НЕ МАЄ ДВОХ ДЖЕРЕЛ НА ОДНОМУ ЕКРАНІ (урок; гейта `#55` більше немає)

⚠️ **ГЕЙТ `#55`/`#55b` ЗНЯТО 07.08.2026 разом із плитками, які його породили.**
Він забороняв блоки «Команда за місяць» і «Очікуємо за план. датою» — а відкат
вигляду до 31.07 повернув їх як ЄДИНЕ джерело. Дубля більше немає, отже й предмета
гейта; до того ж він читав `ReportSummary.tsx`, якого вже не існує. **Урок нижче
лишається чинним і застосовується руками — гейта на нього тепер немає.**

Знайдено оком власника на проді 06.08.2026, уже після викату. Під новими плитками
лишились старі блоки з тими самими числами:
- «Команда за місяць» — **12%**, плитка «Виконано · місяць» — **11.8%**: одне
  число, два округлення, один екран;
- «Очікуємо за план. датою» дублювало плитку «Ще не надійшли»;
- 🔴 найгірше: у старому блоці стани людей лишались **ТРИ** (18+1+6 = 25 із 31) —
  тобто подвійний рахунок, який щойно виправили в новій плитці, **жив поруч у
  старій**. Обидві копії брали числа з ТОГО САМОГО `glance`.

**Механізм, який треба памʼятати:** виправлення лягає в одну копію, друга тихо
відстає, і жоден API-гейт цього не бачить — бо джерело даних спільне, різна лише
верстка. Тому перевіряти дубль треба по **ДЖЕРЕЛУ** фронта, а не по HTTP.

**Прибираючи дубль, унікальні числа ПЕРЕНОСЯТЬ у відповідну плитку, а не лишають
окремим блоком** — окремий блок заради двох показників і є той спосіб, яким дублі
відроджуються.

#### 🔵 СТАНИ МЕНЕДЖЕРІВ — ТРИ, І ВОНИ ПОКРИВАЮТЬ УСІХ (рішення власника 07.08.2026)

`glance.statusCounts = {g, a, r}` над **усіма** менеджерами. Σ чипів МУСИТЬ
дорівнювати числу під підписом «N менеджерів». Еталон — скріншот власника 31.07:
**6 + 4 + 22 = 32**, усі до одного.

06.08 тут був **четвертий** стан «гроші є · не закрито» (`collectedNotClosed`):
людей із `factSuccess = 0` і `factPaid > 0` виносили з `r/a/g`, бо при факті = ①
вони мали НУЛЬ і читались як «зрив» без власної провини. **Підстава зникла разом
із формулою:** факт тепер = ②, а `statusOf(fact, plan)` бачить `fact > 0`, тож у
«Зрив» через нуль ніхто не потрапляє — лише за відставання від темпу, як усі.

🔴 **Чому четвертий стан не можна було лишити «про всяк випадок».** Верстка 31.07
його не малює, тож чипи мовчки не добирали б до кількості людей. Це той самий
клас, що «31 менеджер, чипи на 37», лише в інший бік — і **без жодного видимого
сліду**. Гейт `#54` знято разом зі станом.

#### 💸 ГРОШІ ЗВІЛЬНЕНИХ — У СУМІ Є, РЯДКА НЕМАЄ (будильник `#59`)

`glance.fact` включає гроші звільнених (`dismissed`) — і це **правильно**, бо
інакше Σ по екрану розійшлася б з ядром. Але верстка 31.07 масиву `dismissed` не
знає, тож кільце показує суму, частину якої не пояснює жоден видимий рядок.

**Рішення власника: не ховати, а поставити будильник.** `#59` мовчить, поки
`dismissed` порожній (сьогодні так — Шевчука повернуто), і червоніє першої ж
деактивації менеджера з грішми. Заміряно 06.08, поки Шевчук був деактивований:
**71 896 ₴** у сумі компанії без рядка на екрані.

⚠️ **Саботаж зроблено НЕ деактивацією в транзакції з ROLLBACK, і це принципово:**
`#59` читає HTTP, а сервер ходить у БД **своїм** зʼєднанням і незакомічену
транзакцію не бачить у принципі — тест був би зелений незалежно від того, працює
предикат чи ні. Тому предикат винесено в чисту функцію `unexplainedDismissedMoney`
і саботується її ВХІД. Та сама логіка, тільки чесно.

#### 🖐 РУЧНА ЦІЛЬ ПЕРЕКРИВАЄ ДИНАМІЧНУ — І МУСИТЬ ЧИТАТИСЬ ЯК РУЧНА

`plans.effectiveWeekTargets`: `target = manual ?? dynamic`. Конфлікту немає, ручна
виграє беззастережно. **Але знімок усе одно рахується й морозиться** (він лишається
фолбеком та історією), тож підпис «знімок відновлено» поруч із ручною ціллю казав
би, що показане число реконструйоване, — хоча його ввели руками.

Той самий клас, що два «очікуємо»: **підпис правдивий, але поставлений не до тієї
величини**. Тепер «знімок відновлено» показується ЛИШЕ для динамічної цілі; для
ручної — «ціль задана вручну у Задачнику — не рахувалась», із динамічною поруч для
порівняння.

#### 📏 ДІЯ, ДОСТУПНА В ІНТЕРФЕЙСІ, МУСИТЬ БУТИ СКАСОВНОЮ ТИМ САМИМ ІНТЕРФЕЙСОМ

**Незворотна кнопка — це пастка, навіть коли вона працює правильно.**
(правило власника 06.08.2026)

Привід: `POST /settings/users/:id/reactivate` відмовляв CRM-менеджерам із текстом
«CRM-менеджер відновлюється поверненням у CRM (синк активує автоматично)».
**Текст був неправдивий** — `syncKommo` таблицю `users` НЕ ЧІПАЄ взагалі (`users`
там означає користувачів Kommo, пишуться вони в `managers`). Фронт дзеркалив ту
саму помилку: замість кнопки показував напис «через CRM».

Тобто деактивація CRM-менеджера була **незворотною через інтерфейс**: «вимкнути»
працювало, «увімкнути» — ні, а обіцяного автоматичного механізму не існувало.
Повертати Шевчука Назара довелось SQL-ом по проду.

**Guard прибрано в ОБОХ шарах** (роут + `SettingsSection.tsx`). Тримає `#52`
(цикл деактивація→реактивація повертає стан ДО БАЙТА, включно з
`deactivated_at = NULL`) і `#52b` — саботаж читає ДЖЕРЕЛО роута й червоніє на
повернення guard-а; доведено падінням і поверненням.

⚠️ Чому `#52b` дивиться у файл, а не робить HTTP-пробу: для проби потрібні живий
сервер, деактивований CRM-менеджер і право `manage_users` — три умови, кожна з
яких дала б `skip`, тобто гейт мовчки не виконувався б. А він стереже РІШЕННЯ
власника, тож має триматись у звичайному `npm test`.

📐 **Аудит асиметрії решти дій (06.08.2026): інших випадків НЕМАЄ.**
`PATCH /users/:id` крутить `isActive` в обидва боки · банківський рахунок
вимикається `PATCH {isActive}` і видно в списку (`ORDER BY is_active DESC`), тож
вмикається так само · `DELETE /accounts/:id` жорсткий, але захищений історією
транзакцій · `hidden-payees` і `roles` мають парні POST/DELETE · архів клієнтів,
`force_regular` і відкіт злиття (`revoke`) — усі з парною дією.

#### 🔴 ПЛАН КОМАНДИ = Σ ПЛАНІВ ЇЇ МЕНЕДЖЕРІВ → БЕЗПЛАНОВІ ПІДНІМАЮТЬ % (гейт `#53`)

Гроші менеджера, якому цілі не ставили, потрапляють у **чисельник**, не потрапивши
у **знаменник**. Заміряно на проді 06.08.2026:

| | план | факт | з них безплан. | % як є | % без них | Δ |
|---|---|---|---|---|---|---|
| **РПК-Яцика** | 865 000 | 215 053 | **71 896** | **24.9%** | 16.5% | **+8.3 п.п.** |
| решта 4 команд | — | — | **0** | — | — | 0.0 |
| **КОМПАНІЯ** | 2 759 000 | 319 614 | 71 896 | 11.6% | 9.0% | **+2.6 п.п.** |

**22.5% усього факту компанії** приходить від безпланових — усе в одній людині.

🔴 **І ЦЕ НЕ РОЗСМОКТУЄТЬСЯ ПІСЛЯ ЗВІЛЬНЕННЯ.** Я спершу написав протилежне — і
помилявся. Симуляція деактивації в транзакції з `ROLLBACK`:
`факт 217 873 → 217 873 · план 865 000 → 865 000 · % 25.2% → 25.2%`.
Гроші лише переїжджають із рядка активного в рядок «звільнені», **обидва з яких
входять у суму команди**. Тобто звільнення дає підпис, а не зменшення відсотка.

**Рішення власника: числа НЕ чіпаємо, показуємо складову.** `glance.factNoPlan` +
рядок «у т.ч. N ₴ від менеджерів без плану» — у таблиці команд і в рядку
менеджера, **ЛИШЕ коли ≠ 0**: у чотирьох із пʼяти команд там нуль, і підпис у
кожному рядку перетворив би сигнал на шум. У ⓘ колонки «Виконано · темп» —
пояснення механізму з цифрою.

Тримає `#53` (поле == Σ рядків без плану + звільнені) і `#53b` — саботаж:
нуль при ЖИВОМУ безплановому червоніє, а дзеркало вимагає нуля, коли безпланових
справді немає (інакше поле вигадувало б величину).

#### 🧾 ЧЕРГА: ПЕРЕБУДОВА РЯДКА МЕНЕДЖЕРА В ТАБЛИЦЮ (макет `zvit-1`, рішення власника 06.08.2026)

**Не тим викатом, що ніс формулу + ростер + міграцію** — рішення власника, причина:
при проблемі не було б зрозуміло, що саме відкочувати. Дані на екрані вже є;
бракує ШАПОК, і без них кожне число доводиться розшифровувати щоразу.

📐 **Оцінка (зафіксована, щоб не переоцінювати заново): ~600 рядків FE, 4 компоненти**
— `MgrStrip`, `TrajBlock`, `Stat`, `DayDrill`. Ядро НЕ чіпається, тож ризик для цифр
нульовий; **увесь ризик у верстці, а її не бачить жоден гейт**.

**Чотири умови приймання (рішення власника, узгодити ДО початку, не після):**
1. **Адаптив — липка перша колонка (ПІБ) + горизонтальний скрол решти.**
   Згортання смуг на вузькому екрані **НЕ робимо**: людина не має здогадуватись,
   що частину колонок сховано.
   ⚠️ Доповнення від виконавця: разом із липкою колонкою треба зробити **липкою й
   шапку смуг** (`position: sticky; top: 0`) — інакше при скролі вправо видно числа
   без назви групи, тобто рівно та проблема, від якої шапки й заводяться.
2. **`DayDrill` лягає в ТУ САМУ сітку, що й рядок менеджера.** Дві сітки на одному
   екрані — це наступний дубль, тільки у верстці.
   ⚠️ Уточнення: за макетом при розгортці шапка «Місяць» перемикається на
   «Сер. чек ②» — тобто сітка та сама, а ЗАГОЛОВОК однієї колонки змінюється разом
   зі змістом. Спільна сітка не означає незмінних підписів.
3. **`BandHead` — основа, але спершу перевірити відповідність макету.**
   📐 Перевірено 06.08.2026: у теперішньому вигляді **НЕ годиться** — це `<div>`, а
   шапка групи колонок у таблиці мусить бути `<th colSpan={N}>` у `<thead>`, інакше
   не тримається над своїми колонками при скролі. Наступного проходу це **переписати**,
   а не переюзати. `BandHead`/`BAND_BG`/`PaceCell` написані наперед і **позначені в
   коді як ще не використовувані** — щоб не повторити історію з мертвим `expected`.
   🔴 **ТЕРМІН ВІД ВЛАСНИКА (06.08.2026): якщо прохід не почато до 13.08.2026 —
   ВИДАЛИТИ їх і написати заново під `<th>`.** Причина названа власником і вона
   правильна: переписувати їх усе одно доведеться (`<div>` під табличну шапку не
   годиться), а тримати наперед написаний код, який ТОЧНО не підійде, сенсу немає —
   він читається як робочий рівно так само, як читався `expected` у `/teams`.
4. **Приймання: скріншоти широкого і вузького екрана, з розгорнутим днем, ДО й ПІСЛЯ**
   + звірка, що суми в шапках і рядках збігаються з теперішнім продом.
   **Верстка не має права змінити жодне число.**

#### 🧾 ЧЕРГА: перевести решту запитів на подвійну ознаку активності

`activeManagerSql()` підставлено **лише** в ростер `/report-plan`. `m.is_active`
далі стоїть окремо у **~20 місцях** — усі в `routes/dashboard.ts`: рядки 1534,
2473, 2591, 2815, 3238, 3312, 3343, 3691, 3853, 3888, 4084, 5255, 5285, 5294,
5310, 5474, 5946 (+ `managers.is_active` всередині `core/plans.managerPlan` і
`core/metrics` активних агрегатів). Це «застряглі угоди», нічийні клієнти,
рейтинги, лідогенерація, списки виконавців.

**Робити ОКРЕМОЮ задачею зі своїм прийманням:** переведення змінить склад кожного
з цих списків одночасно, тобто це зміна поведінки, а не технічна правка.

#### 🔴 ВКЛАДЕНИЙ ПЕРІОД НЕ МОЖЕ ПЕРЕВИЩУВАТИ ТОЙ, ЩО ЙОГО МІСТИТЬ (гейт `#46`)

Тиждень ≤ місяця — твердження, яке **не залежить від того, яка формула правильна**.
Саме тому воно й ловить те, чого не ловить жоден гейт про самі метрики: дві РІЗНІ
метрики під ОДНАКОВИМИ підписами на одному екрані. Дзеркало `#46b` вимагає
`факт == успішно + оплачено`, інакше нерівність зеленіла б для будь-якої достатньо
великої величини.

#### 🔴 ДВА «ОЧІКУЄМО» — РІЗНІ ПІДПИСИ (формули не змінено)

- `expect` (`expectedZoneByScope`) — **знімок усієї зони, БЕЗ прив'язки до дати**
  (Антипенко 51 782 ₴). На екрані: «у зоні очікування …, без прив'язки до дати».
- `expectThisMonth` (`expectedPaymentsByPlanned`) — **за плановою датою оплати**
  (44 282 ₴). На екрані: «за план. датою». Саме воно входить у прогноз.

Обидві були правильні й обидві звались «очікуємо» — тож 52к і 44к на одному екрані
читались як поломка. **Планова дата — реальне поле Kommo `2097273`**, не похідна:
заповнена у **100%** угод, що ЗАРАЗ у зоні (424 з 424), але лише 7.6% усієї
FC-бази — бо `backfillPlannedPayment` цілиться саме в зону. Розподіл по днях тижня
(211 із 424 — п'ятниці) підтверджує, що це ручна домовленість, а не автопідстановка.

#### 🟢 ГРОШІ, ЩО ЗАКРИВАЮТЬСЯ ПОЗА FC-ВОРОНКАМИ — ІГНОРУЄМО (рішення власника 06.08.2026)

За 12 міс **377 угод на 1 108 487 ₴** мають статус 142 **поза** `FC_PIPELINES` —
переважно в Продзвіні (`8921936`: 364 угоди / 1 033 663 ₴). Це **лідгени**, і в
грошові метрики вони **не входять**. Питання закрите; запис тут існує саме тому,
що воно вже піднімалось двічі.
⚠️ Туди ж: `8921936/69693688` тримає **262 млн ₴** на 120 угодах («Оптоволокно»,
«Частотні перетворювачі», усі створені 02.06.2026) — разовий імпорт тендерної
номенклатури. Не перевезення; у метрики не входить і входити не має.

#### 🟢 МІНУС НА «ПЕРЕВЕЗЕННЯ ЗАВЕРШЕНО» — КОРЕКТНЕ СТОРНО (перевірено 06.08.2026)

Σ стадії `8921932/98470988` відʼємна (−141 137 ₴ на момент заміру) — і це **не
загублений мінус**. Із 98 угод **60 позначені полем CRM «Мінусова угода»**, і знак
застосований бездоганно в обидва боки: `is_minus=true, price>0` — **0 рядків**,
`is_minus=false, price<0` — теж **0**. Тобто відʼємна сума — це нетто плюсових
(103 073 ₴) і сторно (−244 210 ₴), рівно як задумано. Нічого не робимо.

#### 🧾 ДОБІР — ОКРЕМА КОЛОНКА, І РОЗКЛАДАЄТЬСЯ ЧАСТКОЮ (рішення власника 06.08.2026)

У прогноз входить лише підкріплене документами (факт ② + рахунки з датою оплати
цього місяця). Добір показуємо **поруч** колонкою «зазвичай добирає» з ⓘ «середнє
за 3 місяці, а не угоди».

🔴 **СЕРЕДНЄ НЕ АДИТИВНЕ, і ми на цьому вже спіймались.**
`newBusinessDobirByManager` рахував кожному власне середнє (`raw_m ÷ місяців_m`),
тож Σ по менеджерах давала **1 599 273 ₴** проти справжніх **1 511 318 ₴** по
відділу — завищення на **87 955 ₴ (105.8%)**. Кожне окреме число при цьому було
правильне. Правильний розклад — через **ЧАСТКУ** (`dobirByManager`:
`dobir_відділу × raw_m ÷ raw_відділу`), Σ ваг = 1 → Δ = 0 (заміряно по всіх
командах). Тримають `#49` (адитивність) і `#49b` (саботаж: повторне усереднення
має розходитись).


**🎯 ЄДИНЕ ДЖЕРЕЛО — `backend/src/core/money.ts`. Рахувати гроші деінде (свій SQL по виручці/чеку) — ЗАБОРОНЕНО.**
Усі грошові метрики анкеряться по **ДАТІ ВХОДУ В ЕТАП** (`deal_stage_events`) + **дедуплікація по угоді**. **⏱ Свіжість грошей = частота `syncStageEvents` (30 хв).** Джерело подій оновлюється цією джобою; сповільниш її — гроші «за сьогодні» відставатимуть на стільки ж. Жодних недатованих знімків у сумах виручки (знімок мутував минулі місяці — «за червень» мінялось щодня).

- **«Отримані кошти»** = угоди, що ввійшли в **етап 9 (Оплата отримана) АБО 10 (Успішна)** у періоді, порахувати **РАЗ** (DISTINCT по угоді — етапи 9 і 10 це ОДНІ Й ТІ САМІ гроші; без дедупу виручка подвоюється). Статуси: етап 9 = `69716460`(8921932)/`60412544`(155304); етап 10 = `142`. Розклад без подвійного рахунку: `received = success(лише 142) ⊎ paidOnly(етап 9, ще не 142)`.
- **«Очікування оплат»** = **етап 8 «Очікуємо оплату від замовника (перевізник оплачений)»**: статуси `69716312`(8921932)/`25044997`(155304), анкер по даті входу. ⚠️ **ВИПРАВЛЕНО 06.08.2026:** етап 4 «Виставлення рахунку» (`100274340`) **ВХОДИТЬ у зону очікування** (`EXPECT_ZONE`) — рішення власника. Попереднє формулювання («операційний, у грошові метрики НЕ входить взагалі») розходилось із кодом і **було неправильним**: `EXPECT_ZONE` містила цю стадію завжди. Вона й далі НЕ входить у вузький `STAGE_EXPECTED` (етап 8) і в `CHAIN_INFLIGHT` — саме тому «очікування» на різних екранах може означати різні множини, і кожен екран мусить підписувати, котру бере.
- **ДВА СЕРЕДНІ ЧЕКИ — складати ЗАБОРОНЕНО:** **фактичний** = маржа(received, етапи 9∪10, дедуп) ÷ угоди цієї множини; **очікуваний** = маржа(етап 8) ÷ угоди цієї множини. Окремо `avg_check_success_only` = маржа(лише 142) ÷ успішні угоди — саме її звіряємо з листом: діапазон **2600–2900** (НЕ 5313, що давала стара формула зі знімками ÷ машини). Еталон: **РПК-Яцика серпень 2025 = 242 угоди / 680 655 ₴** (наше 243 / 681 356, Δ 0.1%).
- **Знімок «станом на зараз»** (угоди, що ЗАРАЗ стоять на етапах 8+9) — окрема ПРОГНОЗ-картка з підписом «станом на зараз», **НІКОЛИ** не сумується у виручку періоду (`money.awaitingNowSnapshot`).
- **⚠️ РЕОУПЕНИ ТРАПЛЯЮТЬСЯ РЕГУЛЯРНО** (не «1-2 виключення» — доведено: 10 угод за місяць в однієї команди; масовий перевід у 142 31.03 з відкотом). Угода може входити в 142 кілька разів. Рахується **РАЗ, за ФІНАЛЬНИМ станом**: success = угода ЗАРАЗ у 142, анкер = `closed_at`; paidOnly = ЗАРАЗ у етапі 9, анкер = останній вхід у 9. Якщо угоду повернули з успіху — у попередньому місяці вона **НЕ була виграна** (не рахувати). Без цього виручка подвоюється МІЖ місяцями (виправлено КРОК 4, опція Б).

### Звірка ядра — `core/reconcile.ts` + `reconcileNightly` ✅ (КРОК 4)

**Два розклади** (`reconcileNightly(months)`): **щоночі 03:35 — 2 міс** (свіжа дормантність, швидко/дешево) + **1-го числа 02:35 — 12 міс** (страховка). ⏰ Час :35 (не :00/:30 і не 05:00) — щоб СТАРТ не збігся з тіком `syncKommo(*/30)` і не наклався на 05:00-пік (`syncCarriers`+синк); поки звірка біжить — job_locks тримає synKommo осторонь. Дірка 35к була РАЗОВИМ артефактом (синк стартував пізно), не течею — прямий підрахунок дав **1 дормантну за 12 міс** → 12-міс скан щоночі надлишковий (Kommo API + Neon-квота заради ~0.1 угоди/міс).

Три рівні + свіжість (стан у `/api/health/reconciliation`, алерт у Telegram, таблиця `reconciliation_runs`):
1. **Цілісність**: кожен `deal_id` з `deal_stage_events` є в `deals` (норма 0 сиріт).
2. **Синк**: `deals`(status 142, closed_at) ↔ Kommo API напряму.
3. **Дашборд**: `core/money.ts` ↔ `deals` (ловить баг логіки ядра).
4. **🕰 СВІЖІСТЬ вотермарків** (`checkFreshness`, окремий `freshnessWatch` **щогодини :50**, БЕЗ Kommo): `last_event_at`/`last_success_at`/`last_activity_note_at`/`last_transfer_at` відстає **>2× частоти джоби** → Telegram-алерт (дедуп по епізоду). **Навіщо:** застряглий вотермарк ТИХО ламає метрики — `last_event_at` застряг 09.07 (all-or-nothing) → липневі гроші недорахувались на **62 315 ₴**, а рівні 1-3 цього НЕ бачили (поточний місяць виключено з pass/fail). `last_event_at` — money-критичний (живить `core/money.ts`).

- **🔴 ПОТОЧНИЙ МІСЯЦЬ ПОПЕРЕДЖАЄ, не мовчить** (`currentMonthWarnings`): матеріальні розбіжності синку/дашборду за поточний ym → ⚠️ Telegram-warning (у `ok` НЕ входить — місяць неповний). Раніше поточний місяць мовчки випадав із pass/fail — саме там застрягав вотермарк непоміченим.

- **🔴 ПРАВИЛО (leak-check):** розбіжність `deals`↔Kommo (навіть на 1 угоду) — це **той самий клас, що й дірка на 35 тис. угод**, лише менша. Не «дрібниця»: питання не в угоді, а в тому, **чи протікає досі механізм** (синк по `updated_at`, що пропускає «заснулі» угоди). З'ясовувати **причину**, не гасити. Якщо угода змінилась у CRM після нашого синку і ніщо її не «торкнуло» → дірка відкрита повільно → треба **періодичний reconcile-фікс**, а не разовий бекфіл.
- **🔴 ПРАВИЛО (поріг = % + абсолют):** алерт будить лише коли дельта **>0.5%** (синк) / **>2%** (дашборд) **І** матеріальна: **>2 угод АБО >20 000 ₴** (`MATERIAL_DEALS`/`MATERIAL_REVENUE`). Один дил у малого менеджера = 3% → без абсолютного мінімуму алерт червонів би щоночі й за 2 тижні став би марним. **НЕ піднімати %-поріг** — саме матеріальність відсіює шум, не втрачаючи справжні поломки. Дрібні (`syncMinor`/`dashMinor`) логуються, але не будять.

### AUTO-HEAL — самолікування дормантних угод ✅ (перед КРОКОМ 5)

Той самий leak-механізм (синк по `updated_at` пропускає «заснулі» виграні угоди) вимагає **періодичного фіксу, а не разового бекфілу**. Реалізовано в `reconcileNightly` (працює в ОБОХ розкладах — 2-міс щоночі і 12-міс щомісяця):
- **Дормантна угода** = Kommo вважає її виграною (142, повний цикл, `closed_at` у місяці за київським бакетом), а в `deals` її **немає взагалі**. `runReconcile` віддає їх переліком (`missingWonIds`/`missingWonByMonth`).
- **Хіл**: `healDealsByIds` (спільне ядро з `backfillMissingDeals`) = `fetchLeadsByIds` батчами по 250 (OOM-safe) + `upsertDeal`; далі `reclassifyAdChannel` + `backfillClientKey` — **лише коли реально щось дотягнули** (інакше дормантні мали б грубий канал і зламані сегменти).
- Після хілу — **перезапуск звірки** по тих самих 12 міс, щоб підтвердити, що дірку закрито (`stillMissing`).
- **🔴 ПРАВИЛО (не ховати хіл):** `healed_count` пишеться в `reconciliation_runs` **завжди**. Без цього авто-хіл ховав би сам себе — звірка була б вічно зелена, а дірка мовчки текла б. Видно в `/api/health/reconciliation` (`healedCount`).
- **🔴 ПРАВИЛО (сплеск = поломка):** якщо вилікувано **>10 угод за ніч** (`HEAL_ALERT_THRESHOLD`) — **окремий алерт**, навіть якщо звірка зелена. «Тихе самолікування великої діри гірше за червону звірку»: десятки на ніч = синк тече, а не крапельниця → шукати причину, а не радіти зеленому.

### Воронка «Повний цикл» — реальні етапи

1. Умови узгоджені → 2. Документи підписані → 3. Контроль перед завантаженням → 4. **Виставлення рахунку** (операційний!) → 5. Авто працює → 6. Перевезення завершено → 7. Дзвінок після розвантаження → 8. **💰 Очікуємо оплату від замовника** → 9. **💰 Оплата отримана** → 10. **💰 Успішна угода** (142) → 11. Закрито і не реалізовано (143)

- **Етапи 9 і 10 — ОДНІ Й ТІ САМІ ГРОШІ.** Рахувати РАЗ. Без дедуплікації виручка подвоюється.
- **Воронка ДИНАМІЧНА:** угоди з реклами проходять усі етапи; угоди від постійних клієнтів менеджер може створити **одразу на «Авто працює»**, пропустивши початок. Це пояснює конверсії >100% у ручному дашборді. Не будувати логіку на припущенні, що етапи йдуть строго послідовно.
- **Готівка НЕ проходить «Виставлення рахунку» і не має акта.** Рахунок і акт — тільки про безнал.
- **Воронка «Кваліфікація» — це ДЖЕРЕЛА, а не кроки** (Дзвінки з сайту / eLogist / Дзвінки на мобільні / Від лідогенератора). Рахувати «конверсію між її етапами» безглуздо. ⚠️ «Дзвінки на мобільні» (~2700) і «Не цільові» (~7300) — звалище.

### Угода = відправлене авто (1:1)

**Поля з кількістю авто в CRM НЕМАЄ** (перевірено всі 261 поле — `CRM_SCHEMA.md`). І воно не потрібне: **одна угода = одне відправлене авто**. Тому «машини» = COUNT(угод) — правильно.
Ручний лист має колонки «успішні авто 321» і «поставлені машини 305» — це дві **ручні** цифри, відтворити їх з CRM неможливо і не треба.

### Два якорі дати (Правило №1 глосарію)

| Якір | Дата | Метрики |
|---|---|---|
| **ПРОДАЖІ** | перехід у «Успішна угода» (142) | дохід, машини, чек, конверсії, плани |
| **ФІНАНСИ** | дата акту (поле `463253`, **тільки безнал**) | дохід по акту, cash flow, дебіторка |

**Не змішувати в одній таблиці. Підписувати якір.** Вони не збігаються в межах місяця — і це нормально.
⚠️ Фінансовий якір для **готівки** — ВІДКРИТЕ ПИТАННЯ (акта немає в принципі).

### Мертві поля

`«Клиент оплатил»`, `«Дата опл. клиент»` — 2 і 0 заповнень. **Фактичної дати оплати в CRM не існує.** Факт оплати = **тільки стадія**. Тому анкер по даті входу в стадію — єдине можливе рішення.

### Решта правил

- **Угоди (KPI «рахунок→реалізовано»)** = `funnel_stage IN ('invoiced','paid')`, за датою створення.
- **Воронка продажів** = лише пайплайни «Перевозки повний цикл»: `8921932` (New) + `155304` (старий).
- **Конверсія — це ТРИ різні метрики** (спільний чисельник — поставлені машини, різні знаменники по каналу):
  - `conversion_ads` — знаменник «Прийнято реклами» (команди РНК)
  - `conversion_leadgen` — знаменник «Отримано заявок» (лідоген)
  - `conversion_full_cycle` — знаменник «Створено угод у Повному циклі»
  Показувати «Конверсію» без підпису каналу — **заборонено**. `INNER JOIN psm` у знаменнику — **заборонено** (немапнені ліди зникали → давало 67% замість чесних 42%).
- **Канали команд** (`CRM_SCHEMA.md` §4): **РНК** (Безпамʼятний, Михальчевська) = реклама · **РПК** (Яцик, Дмитрук, Шаврова) + Шевчук = повний цикл · **Ковтонюк** = лідогенерація.
- **Реатрибуція каналів = «останній дотик» ПО КОЖНІЙ угоді** (`reclassifyAdChannel` щосинку, ідемпотентна, вміє знижувати канал; правило власника 08.07.2026 — лідген може реактивувати клієнта З РЕКЛАМИ і передати в прорахунок): канал угоди повного циклу = найсвіжіший дотик клієнта (`client_key`) ПЕРЕД створенням угоди (+3 дні слеку). **Лідоген-дотик** = передача (`lead_transfer_events`) / вхід у «Нова заявка від лідогенератора» `69716164`/`63019380` / лід зараз на цьому етапі. **Рекламний дотик** = створення ліда Кваліфікації (`8921928`/`7336928`) БЕЗ лідоген-маркерів (крім 143). Поле «Лидогенератор» на угоді завжди → leadgen; фолбек — utm/«Источник клиента» → ad.
  **РЕАКТИВАЦІЯ Б'Є МІТКИ:** клієнт з реклами, якого реактивував лідоген → канал = **leadgen**, навіть якщо на угоді старі utm/geo-мітки.
- **Ліди з реклами — це ТРИ метрики, не одна:**
  - `web_tagged_leads` («Ліди з веб-мітками», ~1352/міс) — 3 поля, ВСІ воронки, без фільтра стадії. **НЕ називати «реклама»** — туди входить органіка (~87) і direct.
  - `paid_leads` («Платні ліди», cpc) — підмножина з `TRAF_TYPE='cpc'`, **тільки перший дотик** (реактивовані виключаються, інакше бюджет списується на клієнта двічі). **Єдина метрика, на яку можна ділити рекламний бюджет.**
  - `ads_accepted` («Прийнято реклами», ~912/міс) — `adDealSql`. Знаменник для `conversion_ads`.
- **📞 КОНТАКТ = РОЗМОВА (`billsec > 0`)** — рішення власника 04.08.2026. Недодзвони НЕ контакт: вони показуються окремо як **«спроби»** (ті, що ПІСЛЯ останньої розмови; без розмови — усі), щоб було видно, що менеджер працює, хоча контакту ще не було. Складати їх в одну цифру ЗАБОРОНЕНО — це відповіді на різні питання. Тримає `#25d`.
- **🎯 РЕКЛАМНИЙ ДОТИК = звернення з UTM-МІТКОЮ АБО від НОВОГО клієнта** (рішення власника 04.08.2026). Пропущений дзвінок і звернення постійного клієнта — **НЕ реклама**. Гейт стоїть окремим доданком до **обох** гілок `'ad'` у `reclassifyAdChannel`, а не всередині кожної: інакше третя гілка колись зʼявиться без нього. Заміряно: 904→881 (черв.), 1307→1261 (лип.), −2.5%/−3.5%.
  ⚠️ **`adDealSql` лишає ДРУГИЙ шлях у «рекламу»** — `client_source ∈ adSources`. Він гейтом ВЛАСНИКА не накритий (правило називало `reclassifyAdChannel`). Це відкрите питання, а не недогляд.
  ⚠️ **NULL-пастка, що вже коштувала заміру:** `traf_type = 'cpc'` при NULL дає NULL, `NOT NULL` теж NULL → рядок не потрапляє В ЖОДНУ гілку і зникає з підсумку (159 угод). Скрізь `COALESCE(traf_type,'')`.
  🔴 **ТОЙ САМИЙ КЛАС У `CHECK`/`IN` — І ВІН ТИХІШИЙ (05.08.2026).** `CHECK (a IS NULL OR b IN (…))` при `b = NULL` дає **NULL**, а обмеження на NULL **ПРОХОДИТЬ** — тобто `CHECK` стоїть для вигляду й нічого не стереже. Спіймано на архіві клієнтів: БД спокійно приймала архівацію БЕЗ причини. Правильно — `b IS NOT NULL AND b IN (…)`. **Перевіряти ТЕСТОМ НА ВІДХИЛЕННЯ** (`assert.rejects`), а не очима: очима такий `CHECK` виглядає бездоганно, і саме тому це друга NULL-пастка за тиждень.
- **📞 UTM У ДЗВІНКАХ (з 04.08.2026):** `ringostat_calls.utm_source/medium/campaign/term/content`. Ringostat віддавав їх завжди — ми не просили. Заміряно після бекфілу 416 674 дзвінків: **3.2–5.2% усіх, 11–19.7% ВХІДНИХ**; мітки бувають **лише у вхідних** (коллтрекінг бере їх із веб-сесії — дзвінок на прямий мобільний сесії не має). `gclid` у Ringostat **немає**.
  🔴 **Порожній рядок ≠ мітка.** Ringostat віддає `utm_source: ""`, а не пропускає поле; `?? null` його не ловить. Перший замір після бекфілу показав **«100% дзвінків із мітками»** — саме тому замір і потрібен. Нормалізація `nz()` **на вході**, не при читанні.
- **`adSources` живе в БД** (сид у `schema.sql`, журнал `ad_sources_log`). Дефолтів у коді НЕМАЄ — порожній список = гучна конфіг-помилка, не тихий фолбек.
- **⚠️ Мінусові угоди** (сторно/повернення/корекції): Kommo-калькулятор не вміє відʼємний бюджет → показує плюсом. **Знак дає ПОЛЕ CRM «Мінусова угода» `2098529` (значення «Мінус»), а НЕ слово в назві** (виправлено; за 12 міс лише 2 зі 117 таких угод мають «мінус» у назві). `syncKommo`: `price = is_minus ? -abs(budget) : +abs(budget)` — чиста функція, тож зміна прапорця в CRM само-виправляє знак в обидва боки. **Не повертати запис price позитивним.**
  **Масштаб (замір 31.07.2026, 12 міс):** 117 угод із 9 976 (1.17%) на **−433 791 ₴**. Концентрація: РПК-Дмитрука 67, РПК-Яцика 44, Самостійні 6.
  **🟢 ЦЕ НОРМАЛЬНА БІЗНЕС-ПРАКТИКА, не помилка і не привід до розслідування.** Два типові кейси: (1) рейс свідомо відпрацьовано в мінус, щоб не підвести клієнта; (2) одне перевезення проведено ДВОМА рахунками — сильний плюс і сильний мінус. Ядро їх нетить, і це правильно.
  **🔴 Ручний лист мінуси НЕ нетить, тому систематично ЗАВИЩЕНИЙ на ~3.14%** (не разова аномалія!). По місяцях розрив 0.0–10.3%; пік — серпень 2025 (10.3%), медіана ~3%. **Це задокументований ПОСТІЙНИЙ розрив дашборд↔ручний підрахунок**: при звірці цю дельту очікувати і віднімати, а не шукати баг у ядрі.
  **⚠️ ПОБІЧНИЙ ЕФЕКТ (знаємо, НЕ виправляємо):** у кейсі «два рахунки на одне перевезення» кількість угод рахується як ДВІ, тож середній чек трохи розмивається — близько 1% обсягу. Свідоме рішення власника 31.07.2026 лишити як є. Записано, щоб через півроку тут не шукали баг.
- **Нові клієнти** = перша оплата за всю історію припадає на період. **Постійні клієнти** = 2+ оплачених перевезень (**lifetime**) **ТА активні** (замовляли в межах `sleepingWindowMonths`, дефолт 6міс, або є в дебіторці). Замовклі падають у sleeping/lost для реактивації. У `/loyalty` кожен клієнт рахується ОДИН раз і кріпиться до основного менеджера. **Тип клієнта**: компанія (ключ по нормалізованій назві) vs фізособа (ключ по телефону). **Нормалізація `client_key`**: `normalizeClientName` join('') — spacing/апостроф варіанти однієї компанії дають ОДИН ключ. Бекфіл виконано (23к угод).
  - **✅ НАЗВА↔ТЕЛЕФОН ЗЛИТО (03.08.2026)** через реєстр `client_key_alias` (доказ = спільний контакт Kommo): **151 клієнт / 176 псевдонімів** + 2 пари другого порядку = **178**; топ-30 після поштучного огляду власником, решта пакетом. `client_key_raw` — сире значення від синку, аліаси його НЕ чіпають; `client_key` — похідна `COALESCE(активний аліас, raw)`, матеріалізована джобою `recomputeClientKeys` (щогодини :05). **Відкіт = `revoked_at` + перерахунок, без бекапу.**
  - **🔴 КОЖЕН РАУНД ЗЛИТТЯ ПОРОДЖУЄ НАСТУПНИЙ — і це НЕ привід ганяти «поки не збіжиться».** Механізм: контакт звʼязує ДВА телефони; щойно один переїхав під назву фірми, другий стає схожим на пару «телефон↔назва». **Виміряно, не припущено:** хвиля 1 (151 клієнт) → хвиля 2 (**2 пари**, внесені окремим рішенням) → хвиля 3 (**8 пар / 5 клієнтів на 11 009 ₴**, НЕ внесені). Жодна пара наступної хвилі не зустрічалась у переглянутому списку — ані парою, ані окремим `alias_key`.
    **Доказ щоразу на крок слабший:** контакт вказує вже не на фірму, а на телефон, який сам щойно став фірмою. У хвилі 3 половина канонічних ключів — фізособи й імена (`басог`, `гарбузмаринаєвгенівна`), де спільний контакт легко означає експедитора, а не одного клієнта.
    **Тому процес збігається не до порожньої вибірки, а до рішення людини: нова хвиля = новий огляд власника.** Автодобору немає і не буде.
  - ⚠️ ЩЕ НЕ злиті (клас B, не зливається): контакт-людина при компанії, Latin/Cyrillic гомогліфи, телефон, що веде до ДВОХ різних фірм (транзитивне склеювання заборонено тригером `client_key_alias_no_chain`).
  - ⚠️ **`repeat_client_plans` перерахунок НЕ чіпає** (він рухає лише `deals.client_key`). План на злитому ключі осиротіє й зникне з екрана — переносити руками, це рішення власника, а не побічний ефект джоби.
- **🎯 ХТО Є ПОСТІЙНИМ — ДВОШЛЯХОВА КВАЛІФІКАЦІЯ (рішення власника 05.08.2026, підтверджене).**
  · **БЕЗНАЛ:** 2+ оплати в межах 30 днів **АБО** 3+ за всю історію;
  · **ГОТІВКА:** 3+ оплати в межах 30 днів **АБО** 3+ за історію;
  · **ЗМІШАНІ форми — за БЕЗНАЛЬНИМ правилом** (м'якшим);
  · хто не проходить — **РАЗОВИЙ**: ні в плануванні, ні в реактивації, але НАЗВАНИЙ
  числом на екрані («разових N»), інакше 1 137 → 795 читалось би як втрата даних.
  Правило — чиста функція `qualifiesAsRepeat` у `core/reactivationRules.ts`; факти —
  `core/clientSegments.ts`. Гейти `#25g` (два випадки, названі власником) і `#30n`.
  **Готівкова гілка «3+ у вікні» вкладається в «3+ за історію»**, тож у коді її немає
  окремо — і це не спрощення, а усунення мертвої гілки (доказ у коментарі ядра).
  📐 **Заміряно на проді 05.08.2026:** база 2+ = 1 137 → кваліфіковано **795**,
  разових **342**; активних **164**, сплячих **194**, втрачених **437** (з них «давно втрачених» 279).
- **🗄 «АРХІВ» ≠ «ДАВНО ВТРАЧЕНИЙ» — ДВІ РІЗНІ РЕЧІ, І ОДНА НАЗВА НА ОБИДВІ ВЖЕ РАЗ
  ЗБИЛА З ПАНТЕЛИКУ** (перейменування 05.08.2026, рішення власника).
  **Архів** = РУЧНА дія з причиною, датою й автором (реєстр, вкладка «Архів»,
  `loyalty_overrides.archived_at`). **«Давно втрачений»** = ВІК без оплат понад
  365 днів (похідна від дати останньої оплати, підмножина втрачених).
  На сусідніх екранах вони показували «архів 152» і «Архів 0» — обидві цифри
  правильні, назва одна. Вікова родина тепер зветься `LONG_LAPSED_DAYS` /
  `longLapsed` / `longLapsedCount` / `longLapsedDays`, на екрані —
  «Давно втрачені · понад рік». Слово «архів» лишилось ЛИШЕ за реєстром.
  ⚠️ Не «повертати» назад заради коротшого імені: це рівно та поломка, від якої
  береже правило «підписуй, якою метрикою рахуєш».
- **🧭 КВАЛІФІКАЦІЯ ≠ СЕГМЕНТ ≠ СТАН — три різні питання, і плутати їх не можна.**
  Кваліфікація: «чи він узагалі наш постійний» (правило вище). Сегмент: «з якою
  частотою замовляє» — медіана інтервалу, **потрібно 3+ оплат**, інакше `unknown`
  («недостатньо історії»), бо медіана по ОДНОМУ інтервалу — це не частота.
  Стан: «чи пора реактивувати» — від ДАТИ останньої оплати, поріг **залежить від
  сегмента** (рішення власника 05.08.2026): ⚡ВІП 14 · 🔁Регулярний 30 · 🌙Епізодичний 60 ·
  без історії 60; втрачений — 180 для всіх; **«давно втрачений» — 365** (ПІДМНОЖИНА втрачених,
  у Σ не додається). Наслідок, який треба памʼятати: клієнт із 2 оплатами може
  кваліфікуватись, маючи сегмент `unknown` і поріг 60.
- **⭐ «ВВАЖАТИ ПОСТІЙНИМ ПОПРИ ПРАВИЛО» — РУЧНИЙ ВИНЯТОК КВП** (рішення власника
  05.08.2026). Після двошляхової кваліфікації **342** клієнти стали разовими
  АВТОМАТИЧНО; правило без винятків жорсткіше за реальність, і КВП має могти
  сказати «цей постійний, я знаю чому». Дія в КАРТЦІ клієнта, права —
  `isAdminScope` (КВП/ОД/адмін, та сама межа, що архівація).
  **ПРИМІТКА ОБОВʼЯЗКОВА і стережеться `CHECK`-ом у БД**, не валідацією роуту:
  `CHECK (force_regular IS NOT TRUE OR (note IS NOT NULL AND btrim(note) <> ''))`.
  У списках такий клієнт має позначку **⭐ вручну** з приміткою в підказці —
  позначка без «чому» через місяць нічим не відрізняється від помилки.
  🔴 **Виняток живе в `qualifiesAsRepeat`, а не на екрані.** Прапорець
  `force_regular` існував у БД і впливав ЛИШЕ на легасі-сегментацію старого
  `/loyalty`; нова кваліфікація його не бачила взагалі — вхід у UI без правки
  ядра дав би «брехливу кнопку». Тримає `#40` (саботаж в обидві гілки).
  📐 Замір перед реалізацією: `force_regular = true` мали **0** клієнтів — дія
  нікого не тримала мовчки.
- **🖥 ЖОРСТКИЙ ПОДІЛ ЕКРАНІВ (рішення власника 05.08.2026):** у плануванні —
  ТІЛЬКИ активні; сплячі й втрачені — в реактивації. Обидва екрани беруть сегмент і
  стан з ОДНОГО джерела (`loadClientSegments`), інакше клієнт зник би з обох або
  зʼявився на обох. Інваріант `активні + сплячі + втрачені + разові = база 2+`
  тримає `#30n` — без нього «клієнтів стало менше» не відрізнити від втрати даних.
- **📵 ТЕЛЕФОННІ ДЖЕНЕРИКИ в реактивації (рішення власника 05.08.2026):** ключ-телефон
  геть, **КРІМ безготівкових** («Безнал з ПДВ» / «Безнал без ПДВ» лишаються). Заміряно:
  388 у базі → 66 у видачі. Дзеркальний гейт `#30o` (і прибирає, і лишає).
- **⚠️ ГОРИЗОНТ ДАНИХ = 12 місяців.** Бекфіл і часові ряди — тільки за останні 12 міс. Глибше — легасі, не перераховуємо.
  **Горизонт НЕ стосується lifetime-фактів** (постійні клієнти, лояльність, реактивація). **Таблицю `deals` НЕ обрізати.**
- **План по постійних клієнтах** (`RepeatPlanGrid`, admin/team_lead): місячний таргет виручки З постійних по менеджеру (`plans` metric=`repeat_payment_amount`). Залишок динамічно розкидається на майбутні робочі дні. Ендпоінт `/dashboard/repeat-plans-grid`.
  - **Плани по КОЖНОМУ постійному клієнту**: клієнт зʼявляється АВТОМАТИЧНО, щойно має 2+ оплати lifetime і активний. Табл. `repeat_client_plans` (PK client_key+month): `plan`, `forecast` (same/up/down), `realization_pct`, `international`, `we_do`, `call_link`, `comment`. Перемикач «💤 замовклі» (`includeInactive`).
  - **Воркфлоу затвердження**: план ставить **менеджер** (бачить лише свій рядок) → «Надіслати» = `status='pending'` → **тімлід/адмін** «Затвердити» → `status='approved'` (їхні правки одразу approved). `POST /dashboard/repeat-client-plan` + `/approve`.
- **ЕКРАН КЛІЄНТІВ — подача (04.08.2026, пакет із 5 правок власника).** Скоупи НЕ змінювались — змінилось, як подано.
  - **Ієрархія «команда → менеджер → клієнти»** (`ClientPlansSection`, усі, крім `manager`): за замовчуванням згорнуто до команд; **тімліду його команда розгорнута одразу**; менеджер бачить плаский список, як бачив. Підсумок рівня = сума ВИДИМИХ рядків (згортання не змінює жодної цифри). Бекенд додав лише `teamId`/`teamName` на рядок. Інваріант тримає `#30c` (Σ по командах == Σ плоского списку, клієнт не роздвоюється).
  - **Відповідальний біля КОЖНОГО клієнта** (планування · реактивація · пошук). Джерело одне для всіх трьох екранів і для форми передачі: `COALESCE(loyalty_overrides.pinned_manager_id, основний за оплатами)`; 📌 = спрацювала перша гілка. **Не заводити другу формулу «менеджера клієнта».**
  - **Пошук клієнта `GET /dashboard/client-search`** (право `merge_clients`) — форми обʼєднання й передачі більше не вимагають знати канонічний ключ напамʼять (`вкавтострада`). Поріг 2 символи, вибір ЛИШЕ зі списку (руками ключ не вводиться). **Поріг «постійного» тут `>= 1` оплата, а не 2** — і це свідомо: обʼєднують саме хвости (телефон з однією оплатою, що виявився тією ж фірмою).
  - **Картка клієнта `GET /dashboard/client-card`** — 12 міс. ① (`money.successByClientBucket(scope,'month',clientKey)`, канонічний ключ) + останні 15 угод. **ДВА РІЗНІ ЯКОРІ, обидва підписані в UI:** стовпчики = гроші ① (анкер = вхід у стадію), список = журнал угод (дата закриття; для незакритих — створення). Σ списку ≠ Σ стовпчиків, і зводити їх не треба.
  - **Сортування ієрархії — ПЕРЕМИКАЧ** на обох рівнях: дефолт «найгірші зверху» (% плану — екран існує, щоб бачити проблеми), другий режим «найбільші зверху» (факт ①). Рівень БЕЗ плану % не має, тому йде ПІСЛЯ тих, у кого план є: інакше «найгірші» очолили б ті, кому плану просто не поставили.
  - **🔗 ОБʼЄДНАННЯ КЛІЄНТІВ — ДВА РІВНІ ПРАВА (рішення власника 04.08.2026).** `merge_clients` (КВП/ОД/адмін) — будь-яка пара; **тімлід — лише коли ОБИДВА боки його команди**. Рішення — чиста функція `auth/mergeScope.ts`, кламп стоїть **ПЕРШИМ значущим оператором** у `/client-merge`, `/client-merge/preview` і `/revoke`. **⚠️ ПОРЯДОК ОПЕРАТОРІВ — НЕ ДРІБНИЦЯ:** валідація тіла ПЕРЕД гейтом дає 400 замість 403 і ламає гарантію зліпка #11 («403 = спрацював гейт») — на цьому ми вже раз відкотили прод (04.08.2026). Тримає `#30j`. Канонічний бік для `revoke` береться з РЕЄСТРУ, не з тіла; `clientOwnerTeam` дивиться і на `client_key`, і на `client_key_raw` — інакше тімлід не відкотив би ВЛАСНЕ злиття. **Передача відповідального (`/client-manager`) лишилась за `merge_clients`.**
  - **🔁 ЕКРАН РЕАКТИВАЦІЇ — ієрархія, дві дати, підписане правило (04.08.2026).** Ті самі три зміни, що на екрані планів, і тим самим механізмом (спільна поведінка, а не «схожа»): **ієрархія команда → менеджер → клієнти** (адмін/ОД/КВП — згорнуто; тімліду його команда розгорнута; менеджер — плаский список), підсумок рівня = сума ВИДИМИХ рядків (💤 сплячі · ❌ втрачені · 🔧 у роботі · Σ потенціалу). **ДВІ ДАТИ В РЯДКУ:** «остання оплата» з підписом **джерело стану** і «останній дзвінок» (`ringostat_calls` по канонічному ключу) з підписом **довідка, на стан не впливає**; нема дзвінків — «дзвінків не знайдено», і це ВІДПОВІДЬ, а не порожнє місце. **Стан рахується ВИКЛЮЧНО від оплати** (`SLEEPING_DAYS=60` / `LOST_DAYS=180` з `core/reactivationRules.ts`), правило написане над списком, а пороги приїхали з ядра (`data.thresholds`) — зашитий текст «60/180» почав би брехати мовчки при зміні порогу. Тримають `#25d` (дзвінок НЕ рухає стан + дзеркало «клієнт без дзвінків лишається у списку») і `#30k`.
  - **🗑 «ПРИБРАТИ З ПОСТІЙНИХ» — вхід повернуто в КАРТКУ КЛІЄНТА** (рішення власника 04.08.2026), право віддає сервер (`canHide` = той самий `isAdminScope`, що гейтить роут), дзеркало `#30l`. Дві поломки, знайдені **дією**, а не читанням:
    1. **`POST /loyalty-override` затирав НЕпередані поля.** Тіло кнопки — `{clientKey, hidden}`, тож старий upsert тихо знімав би `pinned_manager_id` — «відповідального біля клієнта», оголошеного ЄДИНИМ джерелом для трьох екранів. Тепер оновлюються **лише передані** поля (`core/loyaltyOverride.ts`), а `pinnedManagerId: null` лишається ЯВНИМ «зняти закріплення». Тримає `#32` проти справжньої БД.
    2. **🔴 `hidden` НІКОЛИ НЕ ПРАЦЮВАВ.** `LEFT JOIN loyalty_overrides lo ON … AND NOT lo.hidden` разом із `WHERE COALESCE(lo.hidden,false)=false` — **порожня операція**: для прихованого клієнта join не дає рядка → `lo.hidden` = NULL → `COALESCE(NULL,false)=false` → умова ІСТИННА. Заміряно на живому сервері: `hidden=true`, а клієнт у видачі **обох** екранів. Умову з JOIN прибрано в `core/reactivation.ts` і в `/client-plans`; тримає `#25e` (з дзеркалом «повернення працює» — інакше фікс міг би різати всіх підряд). ⚠️ **Перед викатом порахувати, скільки клієнтів у проді мають `hidden=true`**: вони зникнуть з екранів, і це зміна цифр, про яку команду треба попередити.
  - **🪦 Прибрано три блоки старого покоління** (реактиваційний грід «1-й/2-й контакт», «Усі постійні клієнти», «Динаміка повторних оплат») + кнопки «➕ в реактивацію», які писали в невидиму вже таблицю. Роути живі, стоять у `DEAD_ROUTE_CANDIDATES` (перегляд після 01.10.2026). **Дані `reactivation_clients` НЕ чіпали: 6 рядків, усі «в роботі», без дат/результатів/коментарів — рішення власника 04.08.2026: СПИСАТИ, не переносити.** Разом із карткам менеджерів (теж прибрані як «дубль без дій») пішов і `GET /dashboard/loyalty`. ⚠️ Дія «🗑 прибрати з постійних» (`POST /loyalty-override`) лишилась БЕЗ входу в UI — роут живий і навмисно НЕ в реєстрі мертвих: це втрачений вхід до потрібної функції, а не мертвий код.
- **Дебіторка — деталізація рахунків**: клік на клієнта → неоплачені рахунки з вкладки «выгрузка» файлу дебіторки. Табл. `receivable_invoices` (TRUNCATE+insert у `syncReceivables`), `GET /dashboard/receivables/invoices?clientKey=` (role-scoped). Коментар+дата оплати тімліда — `receivable_notes` (окрема таблиця, бо `receivables` TRUNCATE-иться).
- **Готівкові клієнти в дебіторці (з CRM, не з файлу)**: готівкові платять кешем → їх борг не в безнал-файлі. `syncReceivables.insertCashReceivables` додає угоди повного циклу, де рахунок виставлено, але кошти ЩЕ не надійшли, price>0. Список у `CASH_RECEIVABLE_CLIENTS`. Рядок перераховується щосинку → зникає автоматом при оплаті. **Сума = ПРИХОД, не бюджет**: `extractIncomeAmount` (поле «Розрахунок приходів» `2097649`, фолбек «Приход 1» `2097627`) — бюджет (`price`) сильно занижує реальний дохід готівкового (МГЕР: бюджет 52.5к vs приход 221к).
  - 💵 **ВІДПОВІДАЛЬНИЙ ЗА ГОТІВКОВИЙ РЯДОК — `owner_source='cash-invoice'`**
    (виправлено 23.08.2026). Менеджер приходить із **УГОД CRM**, а не з рахунків
    дебіторки, тож це ОКРЕМЕ джерело, а не `auto-majority`: мажоритара по
    рахунках для нього ніхто не рахував, і `majority_manager_id` тут завжди
    `NULL`. Готівка без менеджера — чесний `none` в **обох** місцях.
    🔴 **Що було зламано і як це виглядало.** Рядок «МГЕР (готівка)» показував
    «без відповідального · немає менеджера в рахунках», а підсумок «По
    відповідальних» ТОЙ САМИЙ рядок зараховував Семенюку Дмитру — **224 917 ₴**.
    Обидва числа з одного рядка `receivables`: список читав `owner_source`
    (дефолтний `none`), підсумок — `manager_id` (заповнений із CRM). Підпис і
    сума розповідали різне про одну людину. **Знайшло око на живому екрані.**
    🔴 **Причин було ДВІ, і кожна окремо лишала баг живим:** `recomputeOwners`
    стояв у синку ПЕРЕД `insertCashReceivables` (рядка ще не існувало) і мав
    `WHERE … AND source = 'sheet'`. Тримають `#136` (інваріант проти живої БД) ·
    `#136b` (дзеркало) · `#136c` (Δ0) · `#136d` (порядок у синку) · `#136e`
    (чиста функція). Сухий прогін проти прода: змінився РІВНО один рядок,
    Σ 10 617 873.51 → 10 617 873.51.
    ⚠️ Ручне призначення для готівки і далі **не приймається** — її щосинку
    перебудовує CRM, тож override відкотився б сам.
- **Передані заявки → Успішно** (`overview.transferred`): виключає лідоген-команди; «успішно» = передана заявка, чий КЛІЄНТ дійшов до виграної угоди повного циклу (крос-пайплайн by `client_key`) — НЕ всі виграні угоди команди (давало конверсію >100%).
- **Лідогенерація**: пайплайни Продзвін `8921936`/`7337048` + Реактивація `8921948`. Команда лідогенів — назва містить «лідоген».
- **🔴 `is_active` КЕРУЄ СПИСКАМИ Й ВИБОРОМ, А НЕ ІСТОРИЧНИМИ СУМАМИ**
  (рішення власника 05.08.2026). Деактивований у CRM менеджер зникає зі СПИСКІВ і
  з ВИБОРУ (призначення виконавця, селектори, «кому поставити задачу», поточні
  рейтинги активності), але його **гроші лишаються в усіх історичних розрізах**:
  зароблені тоді, коли людина працювала, і заднім числом не зникають. У розрізі
  такий рядок ПІДПИСАНИЙ «звільнений» (`isActive:false` у `MgrRow`) — цифри чесні,
  і водночас видно, що людини вже немає.
  **Чому це записано саме тут:** правило про списки існувало, про суми — ні, і
  через це питання й виникло. 05.08.2026 менеджера деактивували в Kommo, а угоди
  перепризначили лише за пів години: у цю щілину `Σ(менеджери)` стала на
  **16 567 ₴** меншою за `Σ(команди)` (по менеджерах фільтр стояв, по командах ні),
  і два гейти справедливо почервоніли. Тримає `#37` (деактивація не рухає жодної
  історичної суми; доведено саботажем: з фільтром Σ падає 16 500 → 0).
- **🟢 «САМОСТІЙНІ» РОЗФОРМОВАНО (рішення власника 05.08.2026).** Шевчук Назар
  (`kommo_user_id 7181916`) переходить під **РПК-Яцика** повністю і заднім числом.
  **У CRM його ПОКИ НЕ рухаємо**, тож дашборд показує НЕ те, що CRM:
  `TEAM_OVERRIDES` у `syncKommo.ts` (той самий механізм, що `NAME_OVERRIDES`/
  `TEAM_LEAD_OVERRIDES`, ключ = `kommo_user_id`) підміняє групу ПІСЛЯ читання з
  Kommo, тож переживає кожен синк. **Ця розбіжність CRM↔дашборд — НЕ баг.**
  Переведуть у CRM — оверрайд **зняти**, інакше він мовчки дублюватиме групу.
  ⚠️ Ручний `UPDATE managers SET team_id` тут НЕ працює: синк робить
  `team_id = EXCLUDED.team_id` кожні 30 хв і відкотив би правку.
  **Історію переносити не треба** — командний розріз рахується за ПОТОЧНОЮ
  прив'язкою (варіант A), тож минуле перерахується саме.
  **Команда зникає з екранів САМА:** `routes/teams.ts` ховає команди без активних
  менеджерів (той самий фільтр давно ховає порожню `team 4 «Самостійний»`).
  **Серію «Самостійні» у Статистиках НЕ зносимо** — вона тримає історію; підписана
  «розформовано · з 08.2026 у Яцика», правило R2 не чіпали.
- **⚠️ Σ(команди) ≠ Σ(компанія) — ВІДОМЕ ВІДХИЛЕННЯ, штучно НЕ закриваємо** (рішення
  власника 05.08.2026). Причина заміряна: **службовий акаунт «Операційний директор»**
  (`kommo_user_id 904923`, у `syncKommo` він же з `NAME_OVERRIDES` і позначкою
  `was "Admin"`) має `team_id IS NULL` і тримає **346 клієнтів на 2 781 851 ₴**
  (611 586 ₴ за 12 міс). Усі його гроші випадають із розрізу по командах — це і є
  розрив (у квітні 2026 він давав 2 000 ₴).
  **Це ОДНА проблема, а не дві:** той самий акаунт робить і дірку в Σ, і «клієнтів,
  яких ніде немає». «168 менеджерів без команди» — переважно деактивовані й
  нешкідливі; шкоду робить ОДИН активний службовий акаунт.
  **Механізм розсмоктування — пул нічийних:** коли клієнтів розберуть по живих
  менеджерах, гроші самі підуть у команди. Вузол «Без команди» в Σ не заводимо.
  📐 Означення нічийного — ЄДИНЕ, в `core/orphanClients.ts` (тести `#30…#30d`).
  🔴 «Службовий» — ЯВНИЙ СПИСОК `kommo_user_id`, а НЕ евристика по пошті: перший
  критерій «немає пошти @uts.ua» затягнув у службові звільнену **Мельник Лілію**
  (пошта в картці просто не заповнена). Службовий і звільнений — РІЗНІ ознаки з
  різними діями: адмінських клієнтів треба РОЗДАТИ, клієнтів звільнених —
  ПЕРЕПРИЗНАЧИТИ.
  🟢 Лідогени й фінансист нічийними НЕ вважаються (50 клієнтів, 93 957 ₴) —
  лідоген, що довів клієнта до оплати, це нормальний хід.
- **Командний розріз історії — на ПОТОЧНІЙ прив'язці менеджера** (варіант A, 14.07.2026). Kommo НЕ веде історію переходів менеджерів між командами (events API — лише сутності; `/users` — тільки поточна група), тож за минуле відновити неможливо. `manager_team_history` (`syncKommo` пише снапшот при зміні `team_id`) робить розріз точним лише З 14.07.2026. **Артефакт «Дмитрук +13%» у командному розрізі — НЕ баг**, зачіпає ЛИШЕ команди; загальні по відділу і по менеджеру — точні. Позначено підказкою в UI «Рейтингу команд».
- **Дебіторка ↔ Реактивація**: клієнт у `receivables` (активний боржник) НЕ потрапляє в реактивацію.
- **`funnel_stage` values**: `lead_taken`, `quote_requested`, `approved`, `invoiced`, `paid` (`backend/src/db/seedKommoMapping.sql`).
- **⚠️ Дати ЗАВЖДИ по-київськи, включно з обома кінцями**: усі фільтри періоду = `(col AT TIME ZONE 'Europe/Kyiv')::date BETWEEN $from AND $to`. Був баг: `col <= $to` РІЗАВ увесь останній день місяця (для червня ховав 251 угоду). **Не повертати `col <= $to`.**
- **Звірка успіху (ЗАВЕРШЕНИЙ червень 2026)**: «успішно реалізовано» за датою закриття = **951 угода / 2.78млн**. ⚠️ Стара цифра «670 угод / 1.81млн» — була середина місяця + баг межі. **Це і є доказ, що знімок мутує.**
- **📖 СЛОВНИК МЕТРИК — НОРМАТИВНИЙ**: `docs/METRICS_GLOSSARY.md`. Код розходиться зі словником = баг коду.

## Kommo

- Воронка «Реактивація» = `8921948`; етап «Автододані (UTSAI)» = `108224932`.
- Custom-поля лідів: utm_source `481993`, «Лидогенератор» `2098037`, «Источник клиента» `2098035`, «Приход 1 Тип оплаты» `2097629` (Наличные / Безнал с НДС / Безнал без НДС / ВАЛЮТА → `deals.payment_type`), **«Дата выгрузки (Дата акта)» `463253`** (фінансовий якір, тільки безнал).
- API лише читання + точкові write (`kommoWrite`). Видалення лідів недоступне → нейтралізувати статусом 143.
- Фетч історії: тягнути по id (`fetchLeadsByIds`/`fetchCompaniesByIds`/`fetchContactsByIds`), **НЕ всі записи** (обриває з'єднання, OOM).
- **Глибина подій**: Kommo events API капить пагінацію ~20тис **на один прохід** — це ліміт проходу, **не глибини**. Помісячний бекфіл відновлює історію (перевірено: жовтень 2025 віддається).
- **🟢 ВОРОНКИ ПОЗА `pipeline_stage_map` — НЕ ПОМИЛКА МАПІНГУ, НЕ ЛАГОДИТИ.**
  **Рішення власника 05.08.2026**, звірено з Kommo API. Питання «а чому ця воронка не
  в метриках» має закриватись ЦИМ списком, а не новим розслідуванням. Мапити жодну
  з них не треба — мапінг змінив би історичні цифри:
  - **`7341740` «Автосделка» — ІГНОРУЄМО.** 14 730 угод, 13 469 «виграних», але **всі по 0 ₴**
    (Σ виграних за всю історію 521 270 ₴, сучасні — нулі). Це **технічний запит на
    оплату ПЕРЕВІЗНИКА**, обов'язкова умова роботи системи; до продажів стосунку не
    має (підтверджено власником). Працюють у ній не продажні менеджери.
    ⚠️ Саме звідси беруться оманливі «виграні угоди» у деактивованих людей: замір
    «Мельник Лілія — 7 виграних за 90 днів» був шумом із цієї воронки.
  - **`7491756` «Адмін» — ІГНОРУЄМО.** Архівна (`is_archive=true`), останній людський рух
    травень 2024. Ті 944 414 ₴ — це **не виручка**: 835 259 ₴ лежать у 614 угодах на
    етапі **«Проблемні угоди»**, ще 104 229 ₴ — «Угоди Науменка». Виграних немає тому,
    що ці етапи не є успіхом. ⚠️ «Рух за 90 днів: 152 угоди» рахувався по
    `updated_at_kommo` — його бампить синк/Salesbot, тож це НЕ ознака роботи людей.
  - **`13905312` «Get_Cargo» — ІГНОРУЄМО.** Створена 04.08.2026, 103 угоди, **всі по 0 ₴**,
    веде Фесенко Ольга. Етапи НОВИЙ → СПРОБА ЗВ'ЯЗКУ → НА ЗВ'ЯЗКУ → ЗАЦІКАВЛЕНИЙ →
    ЗАРЕЄСРОВАНИЙ → АКТИВНИЙ, назви угод «Андрій перевізник», «Перевізник, Вега Авто».
    Це **реєстрація ПЕРЕВІЗНИКІВ**, а не клієнтів — у грошові метрики не входить.
  - `8921928` Кваліфікація і `8921948` Реактивація — джерела/реактивація, свідомо поза
    грошима (див. правила вище).
- **🪤 ПАСТКА ЗАМІРУ: `updated_at_kommo` — НЕ ознака людської активності.** Його бампить
  синк і Salesbot, тож «N угод змінено за 90 днів» не означає, що там хтось працює.
  На цьому мало не побудували висновок: архівна воронка «Адмін» показала «рух за 90
  днів — 152 угоди», хоча реального руху немає з травня 2024. Для людської активності
  є `deals.last_activity_at` (нотатки з `created_by <> 0`) — саме на ньому стоять
  «застряглі угоди». Це той самий урок, що й «зелена джоба ≠ дані йдуть»: поле, яке
  ворушить автоматика, не свідчить про людину.
- Повний опис CRM (31 воронка, 261 поле, заповненість) — **`docs/CRM_SCHEMA.md`**.

## 🔁 ВІДКАТ КОДУ ≠ ВІДКАТ НАСЛІДКІВ (урок 05.08.2026)

Хвилю 1 відкотили `revert -m 1`; дерево стало **байт-у-байт** як до викату. І все
одно приймання червонило: еталон РПК-Яцика показав 594 296 замість 681 356.

**Причина:** поки змерджений код жив ~15 хвилин, `syncKommo` о 13:30 відпрацював
із `TEAM_OVERRIDES` і записав `managers.team_id = 5` для Шевчука. Це **дані**, і
revert коду їх не чіпає. Видно в `manager_team_history`: `13:30 → team 5`.
Полагодилось ручним смиканням синку (`POST /api/dashboard/sync`) — CRM повернула
`36283` о 13:38, еталон зійшовся.

🔴 **ПРАВИЛО:** перед відкатом і одразу після нього — перевірити, чи встигли
джоби щось **записати** за час життя коду (`manager_team_history`, `job_runs`,
watermarks, `loyalty_overrides`). Відкочувати наслідки ОКРЕМО. «Код той самий»
не означає «стан той самий».

## 🪞 GOLDEN-MASTER — ВИКЛИКОМ ОБРОБНИКА В ПРОЦЕСІ, А НЕ ДРУГИМ СЕРВЕРОМ (24.08.2026)

Порівняти «старий код проти нового на ТИХ САМИХ живих даних» — правильний і
потрібний прийом (він єдиний довів, що перехід лідгену на канал зачепив рівно
дві задумані речі: `/report-plan` 26 полів `kpi.leadgen.fact`, `/kvp-report` 6
полів знаменника РПК, `/overview` — нуль). Але спосіб має значення.

🔴 **НЕ ПІДНІМАТИ ДРУГУ ЗБІРКУ СЕРВЕРОМ ПРОТИ ПРОД-БД.** Вона стартує з усім
своїм розкладом і ЩОСЬ РОБИТЬ у бойовій базі. Заміряно на собі: за одну хвилину
життя другий процес виконав `syncFirstTouch` (506 рядків), `syncLeadgenRegistry`
(1102 рядки) і `alertPush` — **надіслав РЕАЛЬНУ тривогу в телеграм**
(`check_failed:version`), якої не було.

🔴 **І ЛИШИВ ФАЛЬШИВИЙ `app_boot`.** Другий процес записав старт свого sha
(`kind='deploy'`). Наслідок відкладений і підступний: наступний СПРАВЖНІЙ старт
того самого sha порівняє себе з ним, побачить `prevSha === sha` і класифікується
як **КРАХ** — тобто тест підкладає фальшиву аварію в майбутнє. Прибирати запис
доводиться руками.

✅ **ПРАВИЛЬНО:** імпортувати `dashboardRouter` з ОБОХ `dist` і викликати
потрібний layer напряму з мок-`req`/`res`. Жодна крон-джоба не стартує, у прод
не пишеться нічого, а порівнюються ті самі обробники на тих самих даних.
Нову збірку класти в ОКРЕМИЙ `git worktree` — прод-чекаут не чіпається взагалі.

⚠️ Той самий висновок стосується будь-якого «підніму копію поруч, щоб перевірити»:
копія застосунку — це не пасивний читач, це другий працівник у тій самій базі.

## 🚦 ОДНАКОВА ПОМИЛКА З ОБОХ БОКІВ ДИФУ — ЦЕ НЕ «ЗБІГАЄТЬСЯ» (24.08.2026)

Перший прогін golden-master дав `/kvp-report`: **403 / 403 → «0 розбіжностей»**.
Виглядало як ідеальний результат, а насправді порівнялись два відмови.

🔴 **ПЕРЕД ДИФОМ ПЕРЕКОНАЙСЯ, ЩО ОБИДВА БОКИ ВІДДАЛИ 200.** Рівність помилок
доводить рівно нічого — це рідня «порожній результат = провал» і «успіх за 0 мс».

📐 Конкретна пастка тут: `roleHasTab` **fail-closed** при незавантаженому
роль-кеші, тож без `refreshRoles()` будь-який захищений роут віддає 403. Кеш
модульний, отже кликати треба **окремо для КОЖНОЇ збірки** — один виклик не
лікує другий `dist`.

## 🧪 ФРОНТ-TSC — ЛИШЕ `tsc -b`. `npx tsc --noEmit` ТУТ НЕ ПЕРЕВІРЯЄ НІЧОГО (24.08.2026)

`frontend/tsconfig.json` — це **порожній кореневий конфіг із посиланнями**:
`"files": []` + `references` на `tsconfig.app.json` і `tsconfig.node.json`. Тому
`npx tsc --noEmit` не бере в роботу ЖОДНОГО файлу і виходить з кодом **0** завжди.

🔴 **Це не теорія — заміряно.** Я двічі поспіль доповідав «tsc фронту чистий», і
обидва рази це не означало нічого. Спіймано підкидуванням завідомо зламаного
рядка в `TasksSection.tsx`:

```
const ЗАВІДОМО_ЗЛАМАНО: number = "рядок";   →  npx tsc --noEmit  ⇒  код 0
                                            →  npx tsc -b        ⇒  9 помилок
```

✅ **Правильно:** `npx tsc -b` (саме його робить `npm run build`: `tsc -b && vite build`).
⚠️ `vite build` САМ типи не перевіряє — він транспілює через esbuild. Тобто «збірка
пройшла» без `tsc -b` теж нічого не доводить.

Той самий клас, що «успіх за 0 мс» і «папка бекапу є, копії немає»: **інструмент
бадьоро відзвітував про роботу, якої не виконував**. Перевіряй не код виходу, а те,
що перевірка МАЛА ЩО перевіряти.

## 🧹 НЕСВІЖИЙ `dist` ДАЄ ФАЛЬШИВІ ПАДІННЯ (той самий день)

Після реверту в `dist` лишились скомпільовані тести вже видалених файлів
(`orphanClients.test.js`, `adTouch.test.js`, `autodeal.test.js`). Глоб їх знаходив,
маніфест справедливо червонів на «незареєстрованих» — 4 падіння, з яких 3 були
**привидами**. `rm -rf dist && npm run build` прибрав їх.

🔴 **ПРАВИЛО:** перед БУДЬ-ЯКИМ прийманням — `rm -rf dist && npm run build`.
`tsc` не видаляє вихідні файли для зниклих джерел, тож інкрементальна збірка
після revert/rename несе сміття. Це стосується і деплою, і локального прогону.

## 🧾 ВІДКЛАДЕНИЙ БОРГ: 4 пари однотипних запитів в Огляді (05.08.2026)

У `/overview` лишились **4 пари** «той самий SQL, інші параметри» (різні періоди/
грейни). Злиття кожної пари в один запит із групуванням дало б **≈1 с** сумарно.

🔴 **СВІДОМО НЕ РОБИМО** (рішення власника 05.08.2026): кожне таке злиття — це
переписування ЯДРОВОГО запиту з ризиком розійтись у цифрах, а Огляд уже 2.7 с.
Ціна ризику вища за виграш. Повертатись сюди — лише якщо порогів `#36` перестане
вистачати, і тоді з обовʼязковою поштучною звіркою результатів (як робили для
OR-предиката: 6 місяців · 9 напрямків · 34 менеджери).

## 🧪 ТЕСТ, ЩО ПІДНІМАЄ ВЛАСНИЙ КЛАСТЕР, КЛАДЕ СПІЛЬНИЙ ПУЛ (05.08.2026)

`db/pool.js` — модульний **синглтон**: створюється на ПЕРШОМУ імпорті з тим
`DATABASE_URL`, що був тоді. Новий тест-файл із власним `provisionScratch()`
перевизначає змінну ПІСЛЯ того, як пул уже вказує на чужу базу, а `pool.end()`
у його `finally` закриває пул **усім**. Заміряно: один такий файл поклав
**13 тестів** — від `#8 міграція` до `version.json`, жоден із яких до нього
стосунку не має.

🔴 **ПРАВИЛО: один scratch-кластер на прогін.** Потрібен тест проти справжньої
БД — додавай його **підтестом** у файл, який кластер уже тримає
(`core/reactivation.test.ts`), і сій власні дані з власними id, а не заводь
другий `provisionScratch`. Так зроблено `#37`.

⚠️ Симптом оманливий: падає НЕ той файл, який ти щойно написав, а десяток
чужих. Побачив масове падіння після додавання БД-тесту — шукай другий кластер,
а не регресію в ядрі.

## 🧨 ВИДАЛЕННЯ «СТАРОГО БАНДЛА» ЗНЕСЛО ЖИВИЙ CSS (05.08.2026)

Крок 4 процедури деплою закінчується прибиранням старого бандла:
`rm assets/index-<OLD>.js assets/index-<OLD>.css`. У малому викаті двох підписів
змінився лише JS, а **CSS зібрався з ТИМ САМИМ хешем** — тобто `OLD` і `NEW` для
css збіглися, і я видалив рівно той файл, на який показує новий `index.html`.
Прод ~4 хвилини віддавав сторінку **без стилів**. Спіймав скріншот приймання,
а не тест: гейти зелені, health 200, версія правильна — верстки просто немає.

🔴 **ПРАВИЛО: прибирати старий асет ЛИШЕ якщо його імені НЕМАЄ в новому
`index.html`.** Не «якщо ім'я змінилось», а саме перевіркою входження:
```
grep -q "$OLD" index.html || rm -f "assets/$OLD"
```
Однакове ім'я — нормальна ситуація: vite хешує за ВМІСТОМ, і зміна лише в TSX
лишає css незмінним. Перевірка «old != new» цього не ловить у принципі.

## 👁 ТЕКСТ У СПИСКУ НЕ ПЕРЕВІРЯЄ ЖОДЕН ГЕЙТ (05.08.2026)

На вкладці «Архів» у колонці «Клієнт» стояло **«амсфарм»** замість «АМС ФАРМ ТОВ»:
`loyalty_overrides.client_name` не заповнює ніхто, і фолбек `?? client_key` показував
НОРМАЛІЗОВАНИЙ ключ — так було б у кожному архівному рядку.

Не спіймав ніхто: матриця доступу перевіряє **код відповіді**, `#38` — **ядро станів**,
`test:prod` — що ендпоінт живий. **Що саме написано в клітинці — не дивиться ніщо.**
Знайшла ДІЯ: архівував реального клієнта на проді й подивився на відповідь.

🔴 **ПРАВИЛО ПРИЙМАННЯ ЕКРАНІВ:** текст у списку жоден гейт не перевіряє — **дивитись
очима на реальних даних**. Не на порожньому екрані й не на сіді: підпис, що бере поле
з БД, брехатиме саме там, де поле не заповнене.

⚠️ І ще: **скріншот лишається єдиним, хто це бачить.** Ні `test:prod`, ні health,
ні версія не знають про верстку. Приймання без погляду на екран — неповне.

## ⏱ ДЕШЕВИЙ ФРАГМЕНТ ОТРУЮЄ ПЛАН ВЕЛИКОГО ЗАПИТУ (05.08.2026)

Спільний CTE сегментів клієнтів окремо коштує **77-303 мс**. Підставлений як
`WITH …` у запити екранів, той самий фрагмент дав **20 768 мс** на `/client-plans`
і **10 512 мс** на `/reactivation-list` (було 1 276 і 703). Причина не в самому
фрагменті: його оцінка рядків промахується у ~1800 разів (`rows=10` проти 18 001
фактично), і на стику з великим запитом плановик обирає катастрофічний план.
`MATERIALIZED` НЕ рятує (12 420 мс). Виніс окремим запитом — 1 870 / 1 334 мс.

🔴 **ПРАВИЛО: заміряй ЕНДПОІНТ, а не фрагмент.** «Підзапит швидкий» не означає
«роут швидкий»; це третій випадок цього класу (перші два — `array_agg` у
`/client-plans` на 21.4 с і `/overview`, що поклав сайт). І в усіх трьох мірою
була та сама межа: `REQ_TIMEOUT_MS = 20_000` в `index.ts` — за нею користувач
бачить **503**, а не «трохи довше».

⚠️ **НОМЕРИ ТЕСТІВ ЗІТКНУЛИСЬ:** `#30`/`#30b`/`#30c`/`#30d` існують ОДНОЧАСНО у
`core/orphanClients.test.ts` і `routes/clientScreens.test.ts`. Маніфест це
пропускає (повні назви різні), але у виводі падіння «#30c» більше не називає
однозначно тест — я на цьому вже витратив прохід. Наступного разу, коли
торкнемось цих файлів, — перенумерувати нічийних (напр. `#33*`).

## 💼 «ФИНАНСОВЫЙ ОТДЕЛ» У РОЗРІЗІ КЛІЄНТІВ — ЛИШАЄМО ЯК Є (рішення власника 06.08.2026)

У списках клієнтів трапляються рядки, де команда менеджера — **«Финансовый отдел»**
(напр. МХП, UAB Brandelektra за Лисенко Інною). Виглядає як аномалія того ж класу,
що клієнти на службовому акаунті ОД, — **але це НЕ вона**.

🔴 **Рішення власника: нічого не чіпаємо, у пул «Нічийні» НЕ відправляємо.**
Записано саме тому, що наступного разу цей рядок почнуть досліджувати заново як
поломку. Він не поломка.

## 🕳 «БЕЗ КОМПАНІЇ» ЗАЛЕЖИТЬ ВІД ГОРИЗОНТУ БЕКФІЛУ, А НЕ ЛИШЕ ВІД CRM (06.08.2026)

Список «клієнт без прикріпленої компанії» **не можна віддавати людям сирим**.
Бекфіл `deal_companies` покрив 12 місяців, тож клієнт, чиї угоди старші, потрапляє
туди не через прогалину в CRM, а через **наш горизонт**.

Заміряно: із 367 постійних без жодного лінка — **209 мають угоди за 12 міс**
(бекфіл їх бачив → компанії справді немає) і **158 мають лише старіші угоди**
(ми просто не дивились, 2 493 030 ₴).

🔴 **ПРАВИЛО:** будь-який список «чогось немає» звіряти з тим, чи ми взагалі туди
дивились. Інакше людину посилають шукати те, чого ми самі не перевіряли — той
самий клас, що «код дає 1 пару» на зламаному мосту.

## 🧭 ОД — КЛІЄНТИ ЙДУТЬ У ПУЛ, А НЕ В КОМАНДУ (рішення власника 05.08.2026)

Операційний директор нікого не веде — клієнти висять на ньому історично.
Рішення: **не давати йому команду**, а віддати цих клієнтів у пул «Нічийні», щоб
тімліди розібрали їх на живих менеджерів. Після розбору дірка закривається сама —
це і є механізм, а не разова правка.

**Заміряно 05.08.2026:** за ОД **371** клієнт за весь час, **315** у робочому зрізі
пулу (18 міс), **261** з оплатою за 12 міс на **622 288 ₴**. Механізм уже працює:
`904923` є в `SERVICE_KOMMO_USER_IDS`, тож правило пулу («основний відповідальний —
службовий акаунт») забирає їх **усіх**; 18 міс — це лише дефолтний ЗРІЗ ЕКРАНА,
повний список відкриває перемикач «показати всіх».

🔴 **Поки не розібрані — у розрізі по командах окремий рядок «Поза командами ·
Операційний директор» із сумою.** Без нього 622 288 ₴ не входять у Σ команд і
зникають мовчки, хоча в загальному по відділу вони є. Інваріант:
`Σ команд + «поза командами» == Σ відділу`.

## 🎧 ЗАПИСИ ДЗВІНКІВ — ПРЯМИЙ ЛІНК, БЕЗ ПРОКСІ (рішення власника 05.08.2026)

Записи Ringostat відкриваються **без логіна** (перевірено власником 04.08.2026).
Проксі через наш бекенд дав би **ілюзію захисту** ціною аудіотрафіку крізь наш
сервер: URL усе одно існує й доступний. Тому плеєр грає з прямого лінка.

⚠️ Наслідок, який приймаємо свідомо: URL запису потрапляє в HTML сторінки й в
історію браузера. Це не погіршує наявний стан — він рівно такий і зараз.

## 💳 БОРГ: ДУБЛІ КОРИСТУВАЧІВ У KOMMO (05.08.2026)

**8 пар користувачів з ОДНАКОВИМ імʼям і різними `id`.** У двох парах активна
сторона має гроші: «Янчевський Едуард» (281 угода) і «Пехньо Ксенія Олександрівна»
(46). Решта — обидві сторони звільнені.

**Чим це шкодить:** на екранах видно, наприклад, «Пехньо» **без команди** — і це
збиває з пантелику, бо жива «Пехньо Олександра» насправді в РПК-Шаврової, а без
команди висить **звільнений дубль Ксенії**. Тобто екран не бреше, але читається
як помилка.

Прибирати дублі — у CRM, не в дашборді. Наша частина: не показувати звільнених
там, де вони створюють враження прогалини.

⚠️ **Незмаплені групи Kommo (6 шт.) НЕ мапимо** — у всіх шести лише звільнені,
0 угод, 0 ₴. Мапа має відповідати живій структурі, а не історії.

## ❓ ВІДКРИТЕ ПИТАННЯ: «РЕАЛІЗАЦІЯ % ПЕРЕВЕЗЕНЬ» (05.08.2026)

Власник дасть визначення окремо. **Нічого не рахувати «схоже»** і не виводити з
наявних полів: у нас немає ознаки, яка відрізняє «перевезення реалізоване» від
«угода виграна», а вигадана формула через місяць стане цифрою, якій вірять.
Поки визначення немає — метрики не існує.

## 👁 НЕВІДОМЕ МАЄ ЧИТАТИСЬ ЯК НЕВІДОМЕ (05.08.2026)

Порожня назва, порожня клітинка, «—» без пояснення — це не «немає даних», це
**невидима втрата**: рядок на 618 648 ₴ без імені читався як порожній, і гроші
виглядали зниклими, хоча ядро їх не втрачало (Σ команд == Σ відділу, Δ=0).

🔴 **ПРАВИЛО:** якщо значення невідоме — пиши це словами («Поза командами»,
«Команда #id», «код не заповнено в CRM»), а не лишай порожнім. Порожнє місце
читається як «нічого немає», а не як «ми не знаємо».

⚠️ Зворотний бік, теж заміряний: коли невідомих **більшість** (77% клієнтів без
коду), підпис у кожному рядку перетворюється на шпалери, які перестають читати.
Тоді прогалина має стати **ЧИСЛОМ у шапці + фільтром**, а не написом у рядку
(рішення власника 05.08.2026). Тобто правило не «підписувати завжди», а
«невідоме має бути видимим — там, де його видно».

## 🧩 «ЗІБРАВ І НЕ ПЕРЕЗАПУСТИВ» — 49 ХВИЛИН СТАРОГО КОДУ ПРИ ЗДОРОВОМУ HEALTH (05.08.2026)

`version.json` читається процесом **один раз на старті**. Тому `npm run build` на
проді БЕЗ рестарту лишає рантайм і копію різними, і жоден зовнішній симптом цього
не показує: health 200, версія є, сайт працює.

🔴 **ПРАВИЛО: будь-яка збірка на проді завершується рестартом.** «Коміт чіпає лише
фонову джобу» — не виняток, а рівно той привід, через який рестарт і пропустили.

🟢 **Панель у цьому НЕ винна** (перевірено по живому ланцюгу процесів): вона робить
`npm start` у живому checkout, тож автоматичний респавн підніме НАЙНОВІШУ збірку,
а не стару. Деталі — `docs/INFRASTRUCTURE.md` §6e.

🛡 Запобіжник структурний, а не «бути уважним»: `/api/health.buildStale` порівнює
завантажену збірку з тією, що на диску, при кожному запиті. Гейт `#42` + дзеркало `#42b`.

⚠️ **Приймання не можна ганяти під важкою джобою.** Того ж дня `test:prod` дав три
червоні (`#9.1` синк, `#9.2` вотермарки, `#36` час) — усі три через бекфіл, який
тримав `job_locks`, тож `syncKommo` штатно пропускав проходи. Це не регрес: спершу
дочекатись джоби, потім приймати.

## 🧨 `ON CONFLICT` — СПЕКУЛЯТИВНА ВСТАВКА, І `CHECK` БАЧИТЬ НЕ ТОЙ РЯДОК (05.08.2026)

`INSERT … ON CONFLICT DO UPDATE` перевіряє `CHECK` на **ЗАПРОПОНОВАНОМУ** рядку
(EXCLUDED) ДО того, як побачить конфлікт і змерджить його з наявним. Наслідок,
що коштував проходу: часткове оновлення `{clientKey, forceRegular: true}` падає
на `CHECK (force_regular IS NOT TRUE OR note IS NOT NULL)` навіть тоді, коли
примітка **вже лежить у рядку** і нікуди не дінеться — бо в EXCLUDED вона NULL.

🔴 **ПРАВИЛО: крос-колонковий `CHECK` + частковий upsert — несумісні.** Або поле
й його умова їдуть ОДНИМ запитом (так зроблено: UI шле прапорець разом із
приміткою), або умову треба перевіряти тригером `BEFORE UPDATE` по фінальному
рядку. Мовчки «полагодити», прибравши `CHECK`, — заборонено: саме він і робить
примітку обовʼязковою.

⚠️ Симптом оманливий: помилка каже «new row … violates check», хоча рядок уже
існує і в ньому все гаразд.

## ⏱ БОРГ: `/overview` ×4 = 4 938 мс при порозі 5 000 (05.08.2026)

Запас **62 мс**. Наступне повільне доповнення до Огляду впреться саме сюди.
Коли впреться — повертаємось до злиття 4 пар однотипних запитів, яке свідомо
відклали (див. розділ «ВІДКЛАДЕНИЙ БОРГ»), з обовʼязковою поштучною звіркою
цифр. Не раніше: ціна ризику розійтись у цифрах вища за виграш.

🔴 **МЕЖУ ПЕРЕЙДЕНО 23.08.2026 — «коли впреться» настало.** Заміряно на прогрітому
проді (процес 12 хв на ногах, БД тиха, `SELECT 1` = 30 мс): `/overview` ×4 дав
**4 940 · 5 084 · 5 040 · 4 964 мс** — тобто `#36` тепер червоніє на порозі 5 000.
Одиничний запит при цьому проходить (~2.35 с), отже це саме одночасність, як і
було описано. Запасу 62 мс більше немає.

⚠️ **Поріг НЕ піднімати** — над самим тестом стоїть заборона робити це вдруге без
нового рішення власника, і вона чинна. Це борг на окремий прохід, а не привід
пофарбувати гейт у зелене.

📐 Заміряно тим самим днем і те, що знімає підозру з викату `c470c90`: він чіпав
лише сигналізацію (`/api/health`, `app_boot`), `/overview` у діфі немає взагалі,
а БД у момент заміру віддавала важкий грошовий запит за 49 мс.

### 📉 ПЕРФ-ПРОХІД A (24.08.2026): ЗЛИТТЯ ЗАПИТІВ ДАЛО 5%, А ТРЕБА БУЛО 35%

Викочено в `a3079de`. **Результат чесний і малий**, записаний саме тому, що спокуса
переказати його як перемогу велика:

| | до | після | |
|---|---|---|---|
| числа у відповіді | — | — | **0 розбіжностей на 4103 полях** (6 зрізів) |
| запитів до БД | 34 | **29** | детерміновано, тримає `#137d` |
| робота БД (медіана) | 2984 мс | 2843 мс | **1.05×** — тобто ~5% |

**Мета не досягнута:** для здорового запасу під ×4 треба зрізати **~35%**.

🔴 **A1 (злиття 4 викликів `handoffByMonth` в один запит) ВІДКОЧЕНО — воно
ПОВІЛЬНІШЕ в 1.62×** (437 → 710 мс). Числа давало байт-у-байт ті самі, тобто гейт
на еквівалентність був зелений; повільність спіймав лише окремий замір. Причина:
`IN (SELECT pipe FROM cfg_pipes WHERE idx = c.idx)` втрачає індексний доступ проти
`= ANY($1)`. **«Менше запитів» саме по собі не означає «швидше»** — один запит, що
перебирає таблицю, дорожчий за чотири, що йдуть по індексу.

🧭 **Єдиний важіль потрібного масштабу — кеш: 53% часу БД не залежить від періоду.**
Він свідомо НЕ зроблений у цьому проході (правило «кожне число байт-у-байт» стає
складнішим: треба доводити, що кеш не віддає застаріле). Наступний захід — сюди.

### 🗑 ГЕЙТ НА АБСОЛЮТНИЙ ЧАС, ЩО ХОДИТЬ ПО HTTP, МІРЯЄ НЕ ТВОЮ ЗМІНУ (#137e знято)

Написав гейт «×4 під стелею 4200 мс» — і він виявився марним двічі:
1. **До викату він фізично не бачить нового коду** (ходить до ЖИВОГО сервера), тобто
   в QA перевіряє чинний прод, а не те, що приймають.
2. **Після викату його число визначає завантаження Neon, а не ми.** На ОДНОМУ коді за
   добу: ×4 = 2894 / 4386 / 5224 / 6464 мс; за одну годину — спершу зелено, далі
   «медіана 4315 при стелі 4200».

Гейт, що червоніє не з нашої вини, за два тижні починають ігнорувати — той самий
висновок, що й про поріг «% + абсолют» у звірці. **Знято 24.08.2026 (рішення
власника).** Час у прийманні деплою міряє `#36`; durable-гарантія перф-проходу —
**структурний** `#137d` (кількість запитів, детермінована).

## 🔑 ВІДНОВЛЕННЯ ДОСТУПУ ПІСЛЯ ПЕРЕСТВОРЕННЯ СЕРЕДОВИЩА (07.08.2026)

Середовище асистента — ефемерне: контейнер перестворюється без попередження.
07.08.2026 це сталось уперше посеред роботи, і виглядало як втрата всього.

### 🔬 ПРАВИЛО: ПЕРЕД ДІЄЮ, ЩО ЩОСЬ ВІДНОВЛЮЄ, ДОВЕДИ, ЩО ВОНО ЗЛАМАНЕ

Сформульовано власником 07.08.2026 за наслідками цього ж випадку. Ланцюг був
такий: я подивився на два відсутні файли й доповів «доступ втрачено» → постановник
взяв діагноз на віру й написав власникові інструкцію генерувати ssh-ключ → власник
готувався пускати новий канал на прод. **Три кроки, і жоден не перевірив першого.**
Доступ був живий увесь час — один виклик `relay2.sh 'whoami'` це показував.

Це той самий клас, що «незастосована міграція»: там правило — не застосовувати
автоматично, спершу перевірити, чи намір досі правильний. Тут — не відновлювати
автоматично, спершу перевірити, чи справді зламано. **Відновлення дорожче за
перевірку і майже завжди створює новий доступ/стан, який потім живе сам.**

**🔴 ПЕРШЕ, ЩО РОБИТИ, — НЕ ПРОСИТИ ДОСТУП, А ПЕРЕВІРИТИ, ЧИ ВІН Є.** Того разу
власник уже готував ssh-ключ, а доступ був живий: **скретчпад пережив, git-checkout —
ні**. Тобто зникло не все й не однаково.

| Що | Пережило перестворення 07.08 | Звідки відновлюється |
|---|---|---|
| `git`-дерево (код, тести, `dist`) | ❌ відкотилось на старий коміт | `git fetch` + `merge --ff-only origin/<гілка>` — усе, що запушено, ціле |
| скретчпад (`relay2.sh`, `.relay_token`) | ✅ лишився | — |
| `backend/.env`, JWT-токени | ❌ їх тут ніколи й не було | **не відновлюються тут** — живуть на сервері |

**ПОРЯДОК ПЕРЕВІРКИ (2 хвилини, до будь-яких прохань до власника):**
1. `git log --oneline -1` — якщо HEAD старіший за очікуваний, `git fetch origin <гілка>`
   і `git merge --ff-only`. Втрати немає, якщо було запушено.
2. `<скретчпад>/relay2.sh 'whoami; pwd'` — якщо повертає `evraziat` і шлях прода,
   **доступ живий, просити нічого не треба**.
3. Лише якщо relay мовчить — тоді питання до власника.

### 🔒 ЩО НІКОЛИ НЕ ПЕРЕДАЄТЬСЯ В ЧАТ

Значення з `backend/.env` (`DATABASE_URL`, `JWT_SECRET`, `KOMMO_API_TOKEN`,
`KOMMO_BASE_URL` та решта 13), підписані JWT, relay-токен, приватні ключі.
**Публічний** ssh-ключ — не секрет, його переслати можна.

### ✅ ПРАВИЛЬНИЙ СПОСІБ РАХУВАТИ: НА СЕРВЕРІ, А НЕ ТУТ

Заміри й `npm run test:prod` виконуються **через relay, на прод-сервері**, де `.env`
уже лежить. Скрипт кладеться у `/tmp` на сервері (base64 через relay, щоб не воювати
з лапками), читає `.env` ТАМ і друкує **лише агреговані числа**.

```
cd /home/evraziat/uts.ua/dashboard/backend && set -a && . ./.env && set +a \
  && API_BASE=https://dashboard.uts.ua npm run test:prod
```

**Навіщо саме так:** секрети не розмножуються, копії в ефемерному середовищі не
зʼявляється, і наступне перестворення контейнера не вимагає відновлювати нічого.
Заміряно 07.08: `node v26.6.0`, `npm 11.18.0`, `.env` 17 змінних — сервер усе вміє сам.

⚠️ **МЕЖА, ЯКУ ТРЕБА ЗНАТИ НАПЕРЕД:** на прод-сервері **немає браузера** (ані
chromium, ані playwright) — скріншот приймання зняти там **неможливо**. Для нього
потрібен bearer-токен у цьому контейнері. Токен — не `JWT_SECRET`, а підписаний ним
короткоживучий перепуск; підписувати його треба **на сервері** й приносити сюди
готовим. Це все одно копія облікових даних, тож: мінімальна роль, і не лишати
у файлі довше, ніж треба.

## ✅ ЧЕК-ЛИСТ ПРИЙМАННЯ ВІДКАТУ ВИГЛЯДУ ДО 31.07 (зафіксовано 07.08.2026 ДО викату)

Складено ПЕРЕД деплоєм, а не після — інакше «приймання» перетворюється на пошук
пояснень тому, що вийшло. Вісім пунктів, усі від власника:

1. **Скріншот верху**: три блоки в один ряд · кільце з відсотком · розклад
   «= успішно X + оплачено Y» під головним числом · чипи станів.
2. **Σ трьох чипів == кількості менеджерів.** Еталон зі скріншота 31.07: 6+4+22 = 32.
3. **Ті ШЕСТЕРО** («гроші є · не закрито»): у кожного факт > 0, жодного «Зрив» через нуль.
4. **Мокляк і Шаврова лишаються в «Зрив»** — 7% і 2% плану. **Назвати їх у звіті
   ОКРЕМО**: без цього власник прочитає очікуване як регрес.
5. **Рядок менеджера і розгортка по днях — у вигляді 31.07.**
6. **Сер.чек у розгортці — від УГОД, не від авто** (гейт `#58`).
7. **Smoke `#57` кліком** — розгортка справді відкривається.
8. **`npm run test:prod` НА СЕРВЕРІ** (не в контейнері — див. розділ про доступ).

🔑 **Bearer для скріншота** (браузера на проді немає): підписується НА СЕРВЕРІ,
мінімальна роль, мінімальний час життя, **після зняття скріншота видаляється**.

## 🚚 ВИКАТ НЕ ЗАКІНЧУЄТЬСЯ РЕСТАРТОМ — ЗАКІНЧУЄТЬСЯ ПУШЕМ У ПРОД-ГІЛКУ (24.08.2026)

**Останній крок процедури: `git push origin <задеплоєний sha>:claude/friendly-galileo-8pijhl`,
і перевірити `ahead 0 / behind 0`.** Не «колись синхронізуємо» — у тому самому проході.

**Чому це крок процедури, а не добра звичка.** Борг №11 («гілка відстає від сервера»)
закрили 17.08 — і 24.08 він був уже знову: інший чат виклав `5e21043` у прод, у гілці
лишався `1395d32`. Тобто **розрив не лагодиться разово**: він відроджується щоразу, коли
викочує не той, хто тримає гілку. Одноразове закриття лікує симптом.

🔴 **Ціна розриву — не охайність, а неможливість відкату.** Поки гілка відстає, «повернути
як було» означає повернути НЕ те, що працювало: єдина копія робочого стану лежить у
робочому дереві прод-сервера, тобто там, де її зносить перший же `merge --ff-only`
наступного чату. Той самий клас, що «папка бекапу є, копії немає».

⚠️ Тримати в голові при вимірі: `git rev-list --count origin/<гілка>..HEAD` НА СЕРВЕРІ
бреше, поки там не зроблено `git fetch` — рахує проти застарілого tracking-ref. Перший
замір 24.08 дав «6 комітів» розриву при справжньому нулі. **Спершу `fetch`, потім число**
(та сама пастка, що дала фальшиві «36» і «79» у записі №11).

## 🔔 markDeploy ПЕРЕД КОЖНИМ `kill` — ІНАКШЕ ВИКАТ ПРИХОДИТЬ ЯК «АВАРІЯ» (23.08.2026)

**Правило власника: на КОЖНОМУ деплої, перед `kill`, виконати**
```
node dist/tools/markDeploy.js --note="викат <sha>"
```

**Чому.** Наш метод деплою sha **не міняє**: збірка на місці + `kill -TERM` +
респавн конвеєром. Для `classifyBoot` це `prevSha === sha`, тобто «процес пішов і
піднявся без нашої участі», — і банер «Застосунок перезапустився без викату»
кричав користувачам на кожному викаті. Заміряно 23.08: `app_boot` за добу —
`deploy 1, crash 17`.

🔴 **НАМІР ЗАБИРАЄ РІВНО ОДИН СТАРТ, а не мовчить усе вікно.** «Вікно тиші» мало б
дірку саме там, де болить: петля рестартів, що почалась усередині викату, була б
невидима. Другий і подальші старти в тому самому вікні знову `crash`.

🔴 **Стеля 15 хв обовʼязкова.** Намір, що не згорає, — це прапорець без стелі,
той самий клас, що коштував 14 год 52 хв простою 10.08.

⚠️ **Порядок жорсткий:** `build` → `migrate` (якщо є) → `markDeploy` → `kill`.
Після `kill` намір забирати нікому — він просто провисить і згорить.

⚠️ **Якщо викат МІНЯЄ sha (звичайний випадок), `kind` буде `deploy`, а не
`deploy-intent`** — намір спрацьовує лише коли код той самий. Це не помилка;
`deploy-intent` побачите на рестарті БЕЗ нового коду.

Чинне вікно видно в `/api/health` → `deployIntent`. Механізм, що глушить
сигналізацію, зобовʼязаний показувати себе сам.

## 🥶 ПЕРШИЙ ЗАПИТ ПІСЛЯ РЕСТАРТУ — ХОЛОДНИЙ (05.08.2026)

Заміряно: `/overview` одразу після рестарту — **12 133 мс**, через хвилину
2 432-3 355 мс. Приймання, запущене одразу після рестарту, червонитиме `#36`
без жодного регресу. Тому в процедуру деплою входить **прогрів**: по два
запити на `/overview` і `/report` перед `test:prod`. Це виправлення ВИМІРУ,
не порога — пороги власника недоторкані.

🔴 **ДВОХ ЗАПИТІВ МАЛО — ТРЕБА ~10 ХВИЛИН (уточнено 23.08.2026).** Прогрів за
процедурою зробили, і `test:prod` усе одно червонив `#36` на одиничному
`/overview` (3 027 мс). Далі заміряно по хвилинах: процес віком 5 хв → 3.0-4.2 с,
10 хв → 3.1-4.2 с, **12 хв → 2.35 с**, і аж тоді одиничний критерій позеленів.
Тобто «через хвилину 2.4-3.4 с» із заміру 05.08 описує НЕ повний прогрів.

⚠️ **І окремо: приймання не можна ганяти в перші ~15 хвилин після рестарту ще й
тому, що там іде сходинка відкладених джоб.** Заміряно 23.08: о 16:30 одночасно
відпрацювали `syncKommo` (43 с), `evaluateKpiTasks` (42 с), `syncReceivables`
(20 с) — і `/overview` у цю мить віддав **503 за 20 227 мс** (вартовий
`REQ_TIMEOUT_MS`). Це не регрес коду, це вимір під навантаженням, яке ми самі
щойно створили рестартом.

✅ **Практично:** після рестарту — зачекати ~10-15 хв (сходинка джоб добігає,
кеші прогріваються), потім прогрів, потім `test:prod`.

## 🗝 SQL НЕ ТИПІЗУЄТЬСЯ: `deals.id` НЕ ІСНУЄ, `deal_stage_events.deal_id` ТЕЖ (07.08.2026)

Ключі, які ЛЕГКО написати неправильно, бо вони «очевидні»:

| Здається | Насправді |
|---|---|
| `deals.id` | **`deals.kommo_id`** — інших PK у таблиці немає |
| `deal_stage_events.deal_id` | **`deal_stage_events.kommo_id`** |

Тобто джойн подій до угод — `JOIN deals d ON d.kommo_id = e.kommo_id`, і дедуп
теж по `d.kommo_id`.

🔴 **ЧОМУ ЦЕ ЗАПИСАНО ОКРЕМО.** `core/dayItems.ts` у першій редакції джойнив
`d.id = e.deal_id`. Це:
- **пройшло `tsc`** — SQL усередині шаблонного рядка не типізується взагалі;
- **пройшло `npm test`** — усі 240 тестів зелені, бо гейт `#60` потребує живого
  API і без нього чесно скіпається;
- **впало б на першому ж кліку користувача** — рівно як `day` без `AS` двома
  днями раніше.

Спіймав **прогін предикатів проти живої бази**, зроблений ДО викату. Це і є
висновок: якщо гейт не може побігти без прода, його логіку треба прогнати проти
прод-БД окремим скриптом, а не викочувати «бо тести зелені». Зелений набір тут
означав лише «нічого з того, що вміє перевірятись без API, не зламано».

⚠️ Той самий скрипт спіймав `day` без `AS` **у собі** — вже втретє за тиждень.
Прийом, що працює: писати `AS` завжди, навіть де здається зайвим.

## ⏳ БОРГ З УМОВОЮ СПРАЦЮВАННЯ: ЗНАМЕННИК `/lead-recommendation` (24.08.2026)

**Це НЕ рядок у загальній черзі, і саме тому він тут окремим розділом.** У решти
боргів термін — «колись»; у цього термін — **подія**: «до того, як функціонал
вмикатимуть у роботу». Борг, у якого умова спрацювання, а не дата, у спільній
таблиці розчиняється — його там читають як «ще один пункт унизу».

🔴 **УМОВА: полагодити ДО того, як «скільки лідів треба взяти» піде в експлуатацію.**
Якщо він поїде з нинішнім знаменником, **перше ж рішення по ньому буде хибним**:
Антипенку видадуть **9** лідів замість **138**.

**Чому воно таке зараз (і чому це свідомо).** 24.08.2026 знаменник переведено з
реєстру лідоген-бота на канал (`564bee0`). Разом із джерелом **змінився СЕНС**:

| | знаменник | що це означає |
|---|---|---|
| було | `leadgen_touch` за `transfer_date` | скільки лідів менеджеру **передали** — вхідна подія, як і в реклами |
| стало | FC + `lead_channel='leadgen'` за `created_at_kommo` | скільки угод каналу він **створив** — включно з тими, що завів сам постійному клієнту одразу на «Авто працює» |

📐 Заміряно по компанії (01.06-31.08): канал усі угоди **1323/199 = 15.0%**,
лише нові 1134/83 = 7.3%, реклама тим самим `won`-предикатом 2389/268 = 11.2%,
реєстр — 999 і той обрізаний. По РПК викиди: Антипенко 4.3%→**71.6%**, Дмитрук
0%→**74.1%**; «треба лідів» 138→9 і «—»→10; у Возовича й Самохвалова зʼявилась
позначка «недосяжно».

**Рішення власника — ВАРІАНТ B (як є), і логіка важливіша за команду:** варіант
«лише нові» ламає рівність зі Звітом (`#160b`) і повертає «два лідгени», а
семантично правильний варіант «передані ліди» спирається на дані, **зламані
обвалом бота**. Тобто правильне за сенсом недостовірне, а достовірне — не про те.
B — найменше зло, поки функціонал не в роботі.

🟢 **Операційного ризику ЗАРАЗ немає** — власник підтвердив, що функціоналом ще не
користуються. ⚠️ Але роут **не «лише API»**: `PlansSection.tsx` тягне його на
КОЖНЕ відкриття екрана «Плани», а показує в розкривному блоці рядка менеджера
(`openRec`). Тобто цифра вже на екрані й чекає першого, хто на неї подивиться.

**Разом із цим боргом лагодиться ВІДКРИТА АСИМЕТРІЯ:** реклама рахує знаменником
лише **платні нові** ліди (`paidAdSql` + `segment='new'`), лідген — **усі** угоди
каналу. Два канали на одному екрані міряються різними ситами, і множаться в одній
формулі «залишок ÷ (конверсія × чек)».

### 🧭 ФОРМА цього боргу — уточнення власника 24.08.2026

**ФАКТ і РЕКОМЕНДАЦІЯ підпорядковуються різним правилам, і плутати їх не можна:**

- **ФАКТ («що менеджер РОБИВ») від типу команди НЕ залежить.** Три канали +
  зріз новий/постійний показуємо ВСІМ однаково.
- **РЕКОМЕНДАЦІЯ («на що орієнтуватись») від типу команди залежить ЛЕГІТИМНО:**
  РПК — лідгени **+ постійні клієнти**; РНК — пріоритет реклама, постійні в них
  теж бувають і теж враховуються.

🔴 **НАСЛІДОК ДЛЯ СПИСКУ (B) З АУДИТУ: `isRnk`/`kvpTeamKind` прибирати ЛИШЕ там,
де воно підміняє ФАКТ.** У рушії рекомендації воно лишається **за задумом** —
не викорінювати сліпо. Задача була сформульована надто широко; це поправка.

**Тому рекомендація для РПК має стати ДВОЦІЛЬОВОЮ** (лідген-ліди + робота з
наявною базою), а не одним числом «видати N лідів». Це збігається із заміром:
**84% лідген-угод і 73% «іншого» — постійні клієнти**, тобто робота з базою вже
і є основним обсягом РПК, просто невидимим.

❓ **ВІДКРИТЕ НА ТОЙ ПРОХІД (до власника, не наше рішення):** одне зведене число
чи два окремі орієнтири, і **як міряти «скільки постійних треба відпрацювати»**.

## 📦 ДОКРУТ: НУЛЬ ПОКОЛІНЬ АСЕТІВ — І ЦЕ ТРИМАЄТЬСЯ НА ОДНОФАЙЛОВОМУ БАНДЛІ (25.08.2026)

**Зберігання нульове.** Крок CSS-guard у процедурі деплою видаляє будь-який асет,
чийого імені немає в НОВОМУ `index.html`, і працює на кожному викаті. Заміряно на
проді: у докруті **2 файли, 1.5 МБ, нуль сиріт**; запит старого чанка віддає **404**
(`text/plain`, 15 байт). Докрут не заростає — він виголюється дочиста.

**Чому це прийнятно:** бандл **однофайловий** (code-splitting немає, vite лається
«chunks larger than 500 kB» у кожній збірці). Вкладка, яка бандл уже завантажила,
нічого не довантажує й спокійно переживає викат; 404 ловить лише той, хто тягне
сторінку саме в мить викату, і це лікується перезавантаженням.

🔴 **ЩО ЗМІНЮЄ ВИСНОВОК: поява ДРУГОГО чанка.** Тоді те саме нульове зберігання
почне давати 404 на ДОВАНТАЖЕННІ — білий екран у людини, яка просто не перезавантажила
сторінку. І другий чанк зʼявляється не лише з конфігу: vite ділить бандл автоматично
на будь-якому динамічному `import()` / `React.lazy`, тобто звичайна «ліниво підвантажимо
важкий екран» вмикає розбиття, не торкаючись `vite.config.ts`.

**Тому політику зберігання НЕ заводимо, а міну знешкоджуємо гейтом** `#225`/`#225b`:
збірка мусить давати рівно один JS-чанк, інакше червоне з прямою причиною. Тримати два
покоління означало б змінити критерій видалення в CSS-guard — механізмі, який лише за
одну добу сім разів урятував прод на НЕЗМІНЕНОМУ хеші CSS, і на чиїй логіці 05.08.2026
прод чотири хвилини стояв без стилів. Ризик у найнадійнішому запобіжнику за вікно в
кілька секунд — поганий обмін (рішення власника 25.08.2026).

## 🧨 САБОТАЖ, ЩО НЕ ЗАСТОСУВАВСЯ, ДАЄ ЗЕЛЕНЕ — І ЦЕ НАЙГІРШИЙ ВИСНОВОК (27.08.2026)

**Заміряна частота, не побоювання: за одну добу ТРИ саботажі в двох чатах не
застосувались** (екранування в `python -c`, `sed` без збігу) — і кожен дав зелений
прогін. Два різні стани виглядають однаково:

| що сталось | як виглядає |
|---|---|
| саботаж застосувався, гейт беззубий | зелено |
| **саботаж не застосувався** | **теж зелено** |

🔴 Розрізняє їх **лише доказ зміни файла, зроблений ДО прогону**. Без нього з
зеленого кольору робиться висновок «дзеркало беззубе» — і з нього йде робота, якої
ніхто не замовляв.

**Інструмент:** `node dist/tools/sabotage.js <файл> --find= --replace= [--expect=1]`,
знімається `--restore`. Він **не запускає тестів і не судить про колір** — його єдина
робота: довести, що файл змінився рівно N разів, або **відмовити**. Відмовляє на:
нуль збігів · збігів більше, ніж очікували · `find == replace` · рядок лишився на
місці після запису (перечитує з диска, а не вірить у вдалий запис).

⚠️ Підрахунок — **по підрядку, без регулярок**: саме екранування й ламало заміни.
⚠️ Після `--restore` обовʼязково `rm -rf dist && npm run build` — інкрементальний
`tsc` лишає саботовану версію в `dist`, і «повернено» буде неправдою.

Гейти: `#234e` (без збігу — відмова, і текст каже, що зелене означало б «нічого не
ламали») · `#234f` (дзеркало: справжній саботаж проходить — інакше обгортка
забороняла б усе, і саботажі просто перестали б робитись).

## 🧮 «КОД 0» ЯК КРИТЕРІЙ ЗРОБИВ ІНСТРУМЕНТ НЕВИКОНУВАНИМ — І ЙОГО ОБХОДИЛИ (26.08.2026)

Крок `test` у `deploy:check` вимагав від `npm test` **коду 0**. Виконати це неможливо в
жодному середовищі: у контейнері **104 падіння** через відсутній `backend/.env`, на
прод-сервері скіпаються scratch-гейти, заради яких крок і існує. Наслідок не «крок
червоний», а гірший: **головний інструмент дисципліни обходять запасним шляхом, і робить
це не порушник, а сумлінний виконавець.** Інструмент, який неможливо виконати, не
захищає нікого — його просто перестають запускати.

**Критерій став трискладовим, і кожна частина закриває свій стан правила про три:**

```
① приріст падінь == 0     (по НОРМАЛІЗОВАНИХ іменах)
② виконане не звузилось   (жоден тест не став скіпом і не зник)
③ жоден гейт не зник      (gateCount --base=<sha з health.version>)
```

🔴 **② І ③ — НЕ НАДЛИШОК, І ЦЕ ДОВЕДЕНО САБОТАЖЕМ.** «Полагоджене» оточення
(`JWT_SECRET=dummy`, знятий `DATABASE_URL`) дає бездоганні **459 ✔ / 0 падінь** —
і **не виконує двохсот тестів**. ① цього не бачить (тест не падає), ③ теж (оголошення
на місці). Дзеркально: гейт, скіпнутий на ОБОХ боках, зникає безслідно для ① і ② —
його називає лише ③.

🔴 **ПОРІВНЯННЯ ПО ІМЕНАХ БЕЗ НОРМАЛІЗАЦІЇ — НЕ КРАЩЕ ЗА ПОРІВНЯННЯ ПО ЧИСЛУ, А НОВИЙ
СПОСІБ ЗБРЕХАТИ В ОБИДВА БОКИ ОДНОЧАСНО.** Заміряно на ОДНАКОВОМУ коді: **8 зі 104**
падінь — це смерть ФАЙЛУ, і TAP називає її **абсолютним шляхом**; база лежить у
worktree, дерево в клоні. Сирі імена дали «8 зникло, 8 зʼявилось»; після нормалізації —
**0 і 0**. Плюс TAP екранує решітку (`\#6 …`).
⚠️ Звідси ж окреме правило: **імʼя тесту, що потребує екранування, ламає кожен реєстр,
який зберігає імена** — гейт `#230b` спершу мав бекслеш у власній назві й повалив
маніфест. Прибирати треба причину (перейменувати), а не додавати ще один шар екранування.

🔴 **ЗНИКЛЕ ПАДІННЯ НЕ ЗАРАХОВУЄТЬСЯ ЯК ПЕРЕМОГА.** Воно означає або чужий фікс, або
**нетотожні середовища**, і з імен розрізнити їх неможливо — крок НАЗИВАЄ обидві причини
(`#230g`). Голе число вже одного разу сховало регресію під виглядом поліпшення
(«105 проти 106» читалось як мінус одне падіння, а насправді там було справжнє нове).

⚠️ **`DATABASE_URL` у контейнері вказує на БОЙОВУ Neon-базу, роль `kk_ro`** (SELECT-only,
перевірено читанням рядка). `testMode.HAS_DB` вмикає БД-тести самою наявністю змінної;
спиняє їх лише відсутній `JWT_SECRET`. Ціна помилки — падіння, не зіпсований прод, **але
тримається це на самому рядку підключення**: `readOnlyGuard` інертний у dev-режимі.
Тому «полагодити оточення повним `.env`» — небезпечна відповідь, а приріст падінь — безпечна.

📐 Ціна: **+78 с** на прогін (worktree 12.4 с + прогін бази 65.8 с). Не «~50 с» — це була
оцінка, яку я ж і назвав, а потім заміряв.

## 🔍 ВОРОТА БАЧИЛИ 50 ЗАПИТІВ ІЗ 358 — І БУЛИ ЗЕЛЕНІ САМЕ ТОМУ (26.08.2026)

`#17c` («метрика мимо ядра не проходить») діставав SQL регуляркою
`` /`([^`]{20,8000})`/g ``. Вона не знає, який бектик **відкривальний**, а підстановки
має **386 із 659** шаблонних літералів у `dist/routes`. Заміряно на однаковому вході,
незалежним оракулом (позиція виклику `.query(` з шаблонним аргументом):

| | викликів | блок починається рівно там |
|---|---|---|
| регулярка | 358 | **50 (14%)** |
| сканер (`core`-стилю, `auth/sqlLex.ts`) | 358 | **358 (100%)** |

🔴 **І ламало це В ОБИДВА боки, не лише в бік пропуску.** З 20 блоків, які ворота
класифікували, **шість не були літералами взагалі** — обрізки й чистий JS
(`, [managerId]); if (chk.rows[0]?.team_id !== auth.teamId) return res.status(403)…`).
Тобто ворота рахували керуючий потік як «законний SQL». Пропуск видно як тишу,
хибний позитив — як «назви когорту», якої не існує.

📐 **Прозрівши, розбір показав 52 класифіковані блоки: 38 lifetime · 2 названі
когорти · 5 грошей повз ядро · 7 неназваних когорт.** Дванадцять знахідок існували
й раніше — їх просто не було кому побачити.

🔴 **ЯКІР РЕЄСТРУ — ФРАГМЕНТ SQL, А НЕ НОМЕР РЯДКА.** Борг записаний **по порушенню**
(`KNOWN_SQL_DEBT`: файл + якір + клас), а не винятком по файлу — саме виняток по файлу
колись і осліпив `#17c` на 5000 рядків `dashboard.ts`. Побічний ефект, вартий уваги:
рівність «записів у реєстрі == знайдено в коді» сама стала пасткою на регрес розбору —
повернеш стару регулярку, і гейт скаже «у реєстрі 12, у коді 0».

⚠️ **РЕГУЛЯРНІ ЛІТЕРАЛИ — ВИМІРЯНА ВИМОГА ДО СКАНЕРА, НЕ ПЕДАНТИЗМ.** У
`routes/rates.js` лежить ``/[’'`ʼ]/`` — регулярка з **бектиком усередині**. Сканер, що
не розрізняє ділення й регулярку, з цього місця читає решту файла зі зсунутою парністю.
Саботаж «прибрати гілку регулярок» валить **чотири** гейти одразу.

🟢 **Зрізання коментарів регуляркою для цього гейта більше не потрібне** — сканер знає,
що таке коментар. `strip()` ще й псував розбір (2 помилки синтаксису в `training.js`),
а числа з ним і без нього однакові до одиниці. Для `#17`/`#17e`/`#17e2` він лишається:
міняти їхню поведінку — окрема задача.

🧨 **Фікстура саботажу спершу не відтворювала поломку, і це видно лише саботажем.**
Перша редакція `#228c` ділила запит там, де **обидва** сигнали лишались у хвості, —
стара регулярка бачила його так само добре, тобто гейт був би зелений на зламаному
сканері. Правильна форма — підстановка **МІЖ** грошовою ознакою і фільтром періоду
(`${KYIV}` посеред запиту, як у `/repeat-plans-grid`). Той самий урок, що з патчем
`pg-copy-streams`: **саботаж підтверджує лише те, що фікстура моделює**.

Гейти: `#228` (незакрите кажe про себе) · `#228b` (бектик у регулярці) · `#228c`
(вкладена підстановка = один запит) · `#228d` (покриття проти незалежного оракула) ·
`#228e` (реєстр боргу: мертвий і неоднозначний якір).

## 🚫 СПІЛЬНИЙ ТРАНСПІЛЯТОР ДЛЯ ТЕСТІВ — НЕ РОБИМО (замір 25.08.2026)

Шість тест-файлів піднімають компілятор TypeScript кожен свій. Виглядає як очевидна
економія — і нею не є. **Заміряно стелю, а не оцінку:** `import("typescript")` на
проді коштує **185 мс**, файлів **6**, отже спільний транспілятор зекономив би
щонайбільше **5 × 185 мс ≈ 0.9 с** у прогоні на **77 с** — 1.2%, і це ВЕРХНЯ межа.
Реальний виграш по стінному часу ще менший: файли розкидані по 12 хвилях, а хвиля
обмежена своїм найповільнішим файлом; самі вони по 310-462 мс, тоді як критичний
шлях прогону — `#5.3` (**30 742 мс**, 40% усього часу), `#6` (17 с), `#5.5` (8.5 с).

🔴 **Обидві підстави, заради яких це затівалось, ЗНИКЛИ, поки лікували причини:**
памʼять прибрала `--test-concurrency=8` (три прогони поспіль по нулю смертей файлів),
час прибрало зняття 30-секундного хвоста пула (77 с проти 373 с). Проти 0.9 с стояв би
ризик у шести гейтах, що читають ДЖЕРЕЛО фронта.

⚠️ **УМОВА ПЕРЕГЛЯДУ (без неї «не робити» через місяць стає «ми це вже вирішили», і
ніхто не памʼятає чому):** міряти заново, якщо доведеться ПІДНЯТИ паралельність назад
заради швидкості, або якщо файлів із компілятором стане суттєво більше — тоді памʼять
знову стане межею. Поки прогін коштує 77 с, приводу немає.

## 📋 УХВАЛЕНО, АЛЕ НЕ ВИКОНАНО — звірятися ПЕРЕД словом «готово» (з 06.08.2026)

**Вимога власника, дослівно:** *«Заведи список „ухвалено, але не виконано“ і
звіряйся з ним перед тим, як казати „готово“. Рішення, ухвалене й забуте, гірше
за неухвалене.»*

**Чому саме зараз.** Пункт «сер.чек ділиться на УГОДИ, а не на авто» власник
ухвалив ПЕРШИМ у серії правок Звіту — і він загубився серед пізніших промтів.
Обидві сторони вважали його закритим: я казав «готово» про прохід, власник читав
це як «готово про все, що в проході названо». Помилку знайшло око на живому
екрані (Пехньо: 2 850 ₴ ÷ «475» — знаменником стояли **авто**), а не жоден із 200+
гейтів. Гейт на це тепер є (`#58`), але гейта на **забуте рішення** не буває —
тільки список.

🔴 **ПРАВИЛО: «готово» стосується РІВНО того, що перелічено у звіті про прохід.**
Не «проходу», не «блоку», не «промту». Пункт, ухвалений і не зроблений, лишається
в цьому списку зі своєю датою, поки не викотиться.

| # | Ухвалено | Що саме | Стан |
|---|---|---|---|
| 1 | 06.08.2026 | **сер.чек дня = гроші ② ÷ угоди ②** (було ÷ авто) | ✅ 06.08, гейт `#58`/`#58b`; **перезастосовано 07.08** поверх відкату — буквальний відкат повернув би баг утретє |
| 2 | 05.08.2026 | **Свята в календарі робочих днів** (`company_holidays` не враховуються в базисі тижневого плану) — власник назвав БОРГОМ, не зміною | ⏳ не зроблено |
| 3 | 06.08.2026 | ~~Рядок менеджера → таблиця~~ | ❌ **СКАСОВАНО 07.08** відкатом вигляду до 31.07 — перебудовувати нема чого |
| 4 | 06.08.2026 | ~~Видалити `BandHead`/`BAND_BG`/`PaceCell`~~ | ✅ закрито 07.08: `ReportBands.tsx` видалено цілком разом із відкатом |
| 5 | 06.08.2026 | **Історія прогонів джоб** — `job_runs` зберігає лише останній стан, тож «N рестартів поспіль» відтворити нема з чого | ⏳ у черзі |
| 6 | 05.08.2026 | **~20 запитів перевести на `activeManagerSql`** (двопрапорцева активність) | ⏳ частково |
| 7 | 05.08.2026 | **Стейджинг** (гілка Neon + окремий хост) — без нього п.7 DoD невиконуваний у принципі | ⏳ борг |
| 8 | 05.08.2026 | **Перенумерувати `#30*`** у `core/orphanClients.test.ts` (зіткнення номерів із `routes/clientScreens.test.ts`) | ⏳ при наступному дотику |
| 9 | 07.08.2026 | **Показати звільнених із грішми окремим рядком** — поки їх немає, будильник `#59` мовчить; коли задзвонить, це треба буде зробити, а не глушити | ⏳ до першої деактивації |
| 10 | 07.08.2026 | **Звузити канал: relay → ssh, ротація relay-токена.** Relay = виконання довільного shell через HTTP зі статичним токеном; ssh вужчий. Свідомо НЕ робимо в день відкату — два ризики в одному вікні | ⏳ **наступний тиждень** |
| 12 | 14.08.2026 | ~~ЖИВИЙ ПЕРЕЗАМІР ПОРОГА 14 / 21 / 30~~ | ✅ **закрито 17.08.2026**: замір знято в Neon SQL Editor, власник обрав **21** (список 140). Дефолт у ядрі оновлено, гейти зелені. **Мерж і викат — окремо, за токеном ДЕПЛОЙ-ПРОД** |
| 13 | 14.08.2026 | **Фільтр «хто зрушив етап»** — актора немає ні в `deal_stage_events`, ні в `KommoEvent`; ботовий переїзд зараз гасить тривогу. Колонка + мапінг + бекфіл | ⏳ окремий прохід |
| 11 | 07.08.2026 | ~~`origin/claude/friendly-galileo-8pijhl` відстає від прод-сервера~~ | ✅ **закрито 17.08.2026**: `git push origin 878600b:claude/friendly-galileo-8pijhl`, перемотка `5fd2bd8..878600b` (17 комітів, без force). Origin == прод == `878600b`. ⚠️ Розрив був **не «36»**: 36 заміряли 07.08, сервер показував `ahead 79` проти застарілого tracking `cb62cb2`, а справжній був **12**. Число боргу протухає разом із боргом — **міряти в момент закриття, а не вірити запису** |
| 11b | 24.08.2026 | ~~Розрив повторився: інший чат виклав `5e21043` у прод, не запушивши в прод-гілку~~ | ✅ **закрито 24.08.2026** разом із викатом `a3079de`. **Це НЕ той самий борг, а його РЕЦИДИВ** — тобто одноразового закриття замало: розрив відроджується щоразу, коли викат робить не той чат, що тримає гілку. Запобіжник — крок «запушити задеплоєний sha у прод-гілку» ВСЕРЕДИНІ процедури деплою (нижче), а не окремою доброю звичкою |

⚠️ Список НЕ для відкритих питань — ті живуть у своїх розділах («ВІДКРИТЕ
ПИТАННЯ»). Сюди потрапляє лише те, де рішення ВЖЕ ухвалене й лишилось виконати.

## 🔍 РОЗКРИТТЯ ПОЯСНЮЄ ЧИСЛО, А НЕ СПЕРЕЧАЄТЬСЯ З НИМ (07.08.2026)

**Правило:** якщо екран показує число і дає його розкрити, склад розкриття мусить
рахуватись **ТИМ САМИМ виразом**, що й число. Не «тим самим сенсом», не «схожим
правилом» — тим самим кодом.

**Чому записано.** Рядок дня казав «створено 3 · **3н · 0п**», а розкриття того
самого числа показувало **1 нового і 2 постійних**. Лічильник рахував правильним
правилом (`CREATED_KLASS_CASE`, шість гілок), а чип у розкритті — власною
двогілковою копією: «реклама/лідоген → новий, **УСЕ ІНШЕ → постійний**». Історія
клієнта не читалась узагалі, тож `lead_channel='other'` без попередньої виграної
ставав «постійним» у чипі й «новим» у числі.

**Масштаб (заміряно на проді, не оцінка):** розходились **12.6%** угод серпня,
**11.0%** липня, **15.8%** за 12 місяців. Копій правила було дві:
`core/dayItems.ts` і `GET /report-plan/deals`.

🔴 **ЕЛЕКТРОННИЙ СЛІД ПОМИЛКИ — КОМЕНТАР «той самий сенс, що…».** У `dayItems.ts`
над скороченням стояв мій же підпис «той самий сенс, що в списку угод дня
(lead_channel)». Він був **правдою** — і саме тому нешкідливо виглядав: копія
збігалася з ІНШОЮ копією, а не з правилом. Формулювання «той самий сенс, що в X»
у коментарі — привід перевірити, чи X сам не копія.

**Як виправлено:** правило живе в `metrics.createdKlassCase(ops)` — функція від
операндів. Дві форми (CTE по `classed` і інлайн `dealKlassSql()` по `deals d`)
викликають ОДНУ функцію, тож дописати гілку в одну й забути другу неможливо.
Еквівалентність доводить гейт `#66b` — прийом уже застосований для
`hasPriorPaidSql` (`#35`).

**Третій стан видимий.** `'undef'` (ні каналу, ні ключа, ні декларації CRM) →
чип «невизначено», сірий. Раніше `null` мовчки друкувався як «постійний» — те
саме зʼїжджання невідомого в конкретну відповідь, яке ми тут і лікуємо. Обсяг
чесний: **0.8%** серпня, **0.3%** липня.

⚠️ **ЧИСЛА ЗА МИНУЛІ МІСЯЦІ НЕ ЗМІНИЛИСЬ.** Чип — суто відображення (два місця
рендеру, у жодну суму не входить). Правився саме він; лічильник рядка був
правильний і лишився тим самим. Перевірено перед фіксом — це була умова зупинки.

⚠️ **ДВА ВИЗНАЧЕННЯ «ПОСТІЙНОГО» СПІВІСНУЮТЬ, і це окреме питання** (не воно
спричинило цей баг): **A** = «раніше була ВИГРАНА угода (142) по `closed_at`»
(`classifyCte`) проти **B** = «раніше була ОПЛАТА (`funnel_stage='paid'`) по
`created_at`» (`hasPriorPaidSql`, `first_paid` у `money.ts`, `RevenueQuality`).
Розходяться: **12** угод серпня, **30** липня, **2 442** за 12 міс. Яке з них
правильне — **ВІДКРИТЕ ПИТАННЯ до власника**, не наше рішення.

## 📦 НАЯВНІСТЬ АРТЕФАКТУ НЕ Є ДОКАЗОМ ЙОГО ПРИДАТНОСТІ (10.08.2026)

**Папка бекапу існувала щодня, а повною була тричі за місяць.** 26 каталогів,
143 МБ, рівний щоденний ритм — і лише 3 з `MANIFEST.txt`; три з останніх чотирьох
взагалі порожні. Останній придатний бекап був за 12 днів до того, як ми це помітили.

**Перевіряй ВМІСТ, а не факт створення** — так само, як прогін джоби перевіряється
обсягом, а не статусом («успіх за 0 мс» коштував 15 годин простою).

Це **четвертий** випадок того самого класу за добу: `#19c` («нічого не виконалось» ≠
«0 падінь»), маніфест набору (зниклий тест ≠ пройдений), «успіх за 0 мс», і ось —
папка ≠ копія. Форма щоразу одна: **порожнеча читається як норма**.

⚙️ Практично: у кожного артефакту має бути **ознака завершеності**, що пишеться
ОСТАННЬОЮ (у бекапі це `MANIFEST.txt`), і споживач звіряє саме її. Ротація, нагляд
і звіти рахують лише артефакти з цією ознакою.

## 🧷 ПРИБИРАЄШ ІНВАРІАНТУ — ЗНАЙДИ ВСІХ, ХТО НА НЕЇ СПИРАВСЯ (10.08.2026)

**Умова буває правильною не сама по собі, а тому що хтось поруч гарантує їй вхідні
дані.** Прибрав гарантію — умова лишилась синтаксично тією самою й почала брехати.

Цей клас дав **три різні баги за одну добу**:

| що зламалось | яка гарантія зникла |
|---|---|
| чипи «новий/постійний» у розкритті | правило класифікації існувало тричі; дві копії скорочені, і збігались вони одна з одною, а не з правилом |
| банер «джоба падала після останнього успіху» | `runJob` більше не занулює `last_error` на успіху — банер став вічним і почав стверджувати неправду |
| мовчазна сигналізація 15 годин | `sendAdminAlert` мовчки виходить без `TELEGRAM_*`; вартові справно кликали гудок, якого не було |

**Практично:** перед зміною поведінки спільного місця (константа, хелпер, запис у
таблицю, ENV-контракт) — `grep` по ВСІХ читачах і питання «чи не тримався хтось із
них саме на тому, що я прибираю». Дешевше за перевірку — лише інцидент.

## ✅ DEFINITION OF DONE — обовʼязково для БУДЬ-ЯКОЇ зміни

Сім питань (6c додано 25.08.2026). Кожне з них ми вже одного разу проґавили, і
кожне коштувало інциденту — тому це не побажання, а умова «готово». Механічну частину тримають ворота `#17`/`#18`,
але вони перевіряють КОД, а не намір; решта — на тому, хто робить зміну.

1. **Цифра рахується ядром?** Гроші — лише `core/money.ts` (анкер по даті входу в етап
   + дедуп 9∪10). Свій SQL по виручці розійдеться з дашбордом через місяці. → `#17c`.
2. **Ендпоінт у матриці?** Новий роут без запису у `accessMatrix.ts` — це «зелено там,
   куди ми не дивились». І він мусить мати МЕЖУ: `ROUTE_TAB` або `requirePerm`. → `#17`.
3. **Заборона має дзеркальний дозвіл?** Односторонній тест зеленіє і тоді, коли фіча
   зламана повністю: зріж поля ВСІМ — «менеджер не бачить реквізитів» стане зеленим,
   а Виписка мертвою.
4. **Доведено САБОТАЖЕМ?** Зламай перевіряєме й покажи, що тест почервонів САМЕ на
   цьому. «Тест пройшов» без саботажу означає лише «нічого не зламано зараз».
5. **Правка справді лягла?** `grep -c` після редагування, не припущення. Двічі
   траплялось, що правка тихо не застосувалась, а звіт казав «зроблено».
6. **Зміна поведінки відокремлена від рефакторингу?** Різні коміти. Інакше через
   півроку розширення доступу читається як регресія рефакторингу — і навпаки.
6b. **Список «УХВАЛЕНО, АЛЕ НЕ ВИКОНАНО» звірено?** Перед словом «готово» —
   пройтись по таблиці вище і назвати вголос, що з ухваленого лишилось. Гейта на
   забуте рішення не буває; сер.чек ÷ авто прожив на екрані добу саме тому.
6c. **Новий гейт може СКІПАТИСЬ — реєстр про це знає? І дозвіл не накриє зайвого?**
   Правило двобічне, і друга половина важливіша за першу — саме на ній ми спіймались.
   **(а)** Додаєш гейт, що може скіпатись (scratch-кластер, браузер, живий API), —
   перевір, чи не мусиш внести його в `ALLOWED_PROD_SKIPS` (`testRunGate.ts`). Не
   внесеш — у `TEST_SCOPE=prod` скіп рахується ПАДІННЯМ, і приймання дасть падіння,
   яких ти не чекав і спишеш на щось інше.
   **(б)** Вносиш запис у реєстр — перевір, що причина йде через `skipReason()`, а не
   сирим рядком. Реєстр звіряє ІМʼЯ; природу перешкоди несе маркер усередині причини.
   Віддаси сирий рядок — дозвіл накриє й **зламане оточення**, і прогін зарахується,
   не виконавши перевірки. Тобто запис у реєстр без `skipReason()` **закриває саме ту
   діру, яку `scratchDb.ts` (Б2) відкривав**.

   🔴 **Чому це окреме питання, а не «уважність».** Реєстр дозволених скіпів став
   СПІЛЬНИМ місцем, куди пише кожен чат, а дізнаються про це ВИПАДКОВО — 25.08.2026
   про взаємодію стало відомо лише тому, що я пішов подивитись, що викотив сусід.
   Обидві взаємодії тихі: перша дає три «незрозумілі» падіння, друга — зелений
   прогін при мертвому оточенні. Кращого механізму, ніж це питання, поки немає.

   ✅ Перевіряється ВИКОНАННЯМ, не читанням — трьома викликами `evaluateRun`:
   мої скіпи → `ok:true` · незареєстрований сусід → `ok:false` (реєстр не ковдра) ·
   мій гейт, скіпнутий ЗЛАМАНИМ оточенням → `ok:false` ПОПРИ реєстрацію (це й є
   доказ, що причина їде через `skipReason`).
7. **ПОВНИЙ `test:prod` прогнаний на ЧИСТІЙ збірці ГІЛКИ — ДО викату?**
   `npm test` цього не заміняє: він пропускає все, що потребує живого API, а
   саме там 05.08.2026 і сиділи два падіння (`#30c`, `#30k`), які ми побачили
   вже на проді. Порядок: `rm -rf dist && npm run build`, далі
   `set -a && . ./.env && set +a && API_BASE=… npm run test:prod` — і лише
   потім мердж у прод-гілку.

   🔴 **УМОВУ ТИМЧАСОВО ЗНЯТО — і це рішення власника 05.08.2026, а не наша
   недбалість.** Щоб прогнати `test:prod` ДО викату, потрібен сервер, що крутить
   код ГІЛКИ. Спроба підняти його поруч із продом провалилась не через помилку в
   налаштуванні: хост **убиває другий Node-процес** за 1-2 хвилини. Доведено
   тричі (`nohup`+`setsid`, `screen` — сокети заборонені, `atd` із послідовним
   прогоном): сервер стартує (`health=200`), у його ВЛАСНОМУ лозі жодного
   fatal, а `server-після=000` і падають рівно ті тести, що ходять по HTTP.
   Той самий клас, що вбив прев'ю-сервер і дав crash-loop 15.07.

   **Тому діємо так:** `викат → НЕГАЙНО повний test:prod + test:matrix на проді
   → відкат за процедурою, якщо червоне понад очікувані переходи`. Замір, який
   можна зробити ДО викату і який ми робимо обовʼязково: чиста збірка + `npm test`
   + прогін зачеплених ендпоінтів проти ПРОД-бази в ролі `test_readonly`
   (виклик обробників у процесі, без сервера).

   🧾 **БОРГ, ПОКИ НЕ ЗРОБЛЕНИЙ: стейджинг** — окрема гілка Neon + інший хост.
   Поки його немає, пункт 7 невиконуваний у принципі, і вдавати виконання
   ЗАБОРОНЕНО: «повний прогін зелений» без стейджингу — це неправда, а не
   формальність.

⚠️ **Порожній результат = ПРОВАЛ, а не успіх.** Якщо перевірка нічого не знайшла —
спершу доведи, що їй БУЛО що знаходити.

## 🟢 ЗЕЛЕНА ДЖОБА ≠ ДАНІ ЙДУТЬ (урок 05.08.2026, рішення власника записати)

`syncCalls` годинами звітувала **успіх без помилок** — і не привозила нічого.
Вікно для Ringostat будувалось із `getUTC*`, а API читає **київський** час, тож
джоба просила проміжок, який уже на 3 години в минулому. Живий A/B проти самого
API: те саме вікно в UTC-числах → **1** дзвінок, у київських → **530**.
За добу в базу лягло **0** рядків при ~2000/добу в будні.

**Чому мовчала сигналізація:** помилки не було. Джоба чесно записала **порожню
відповідь**, `runJob` побачив успіх, банер лишився зеленим. Той самий клас
хибно-зеленого, що «порожній результат = pass» і «тест, якого немає».

🔴 **ПРАВИЛО: для джоб-ІМПОРТЕРІВ перевіряти ОБСЯГ, а не лише відсутність
помилки.** «Відпрацювала» нічого не означає, поки не сказано «привезла N».
Це стосується `syncCalls`, `syncKommo`, `syncStageEvents`, `syncTransfers`,
`syncDealActivity`, `syncAdBudget`, `syncReceivables`, `syncRingostatCalls`.

⚠️ **ВІДКРИТЕ ПИТАННЯ (запропоновано, НЕ реалізовано):** додати в
`monitoredJobs.ts` поле на кшталт `minRowsWorkday` і перевіряти його в
`freshnessWatch` (щогодини :50): нуль рядків у робочий день = **тривога, а не
тиша**. Не зроблено, бо чесний поріг вимагає рішень, яких у нас ще немає:
що вважати робочим днем (свята з `company_holidays`?), як не червоніти вночі
й у вихідні, і який baseline у кожної джоби. Ставити «нуль = алерт» наосліп
означає завести алерт, який за два тижні почнуть ігнорувати — рівно те, від
чого нас береже правило про поріг «% + абсолют» у звірці.

## 📅 ПЕРЕВІРКА, ПРИВʼЯЗАНА ДО НАЯВНОСТІ СТАНУ, ЧЕРВОНІЄ ЗА КАЛЕНДАРЕМ (25.08.2026)

**Клас, а не випадок: третій за добу** — `#96b`, `#105`, тепер `#56b`/`#61b`. Форма
щоразу одна: гейт вимагає, щоб ПРЯМО ЗАРАЗ у живих даних було ≥N чогось. Поки воно
там є — гейт зелений і виглядає робочим. Щойно бізнес-процес доходить до законної
паузи — гейт червоніє, не спіймавши жодного дефекту.

📐 **Числа цього випадку, заміряні на бойовій базі, а не оцінені.** `#56b` і `#61b`
вимагали «≥3 ручних тижневих цілей зараз». Насправді: **92.4% тижневих парасольок
(85 із 92) закінчуються В ПʼЯТНИЦЮ**, а нові тімліди заводять **щопонеділка о
08:44-09:24**. Реконструкція по днях за шість тижнів — щосуботи й щонеділі **нуль**,
у понеділок до ~09:00 теж нуль, без жодного винятку. Тобто обидва гейти були червоні
**~30% часу** через день тижня. При цьому на момент викату, якому їх приписали, у
даних було **14** ручних цілей — тобто червоний прогін стався в порожньому вікні, а
діагноз поїхав у бік невинного дифу.

🔴 **ЧОМУ ЦЕ ГІРШЕ ЗА ВІДСУТНІЙ ГЕЙТ.** Перевірку, що червоніє не з нашої вини,
за два тижні починають гортати очима — рівно так ми зняли `#137e`. Ігнорований гейт
не стереже нічого, але створює відчуття, що стереже; а поруч він знецінює сусідні
червоні, які справжні.

✅ **РЕЦЕПТ НА ТРИ КРОКИ** (застосовано в `#220`-`#220c` / `#221`-`#221b`):
1. **Механізм — на власній фікстурі**, не на живих даних. Scratch-БД, період
   сіється як `сьогодні±3` → покриття існує в БУДЬ-ЯКИЙ день, і саботаж червоніє
   в суботу так само, як у вівторок.
2. **Живі дані — РІВНІСТЮ, а не кількістю.** «Скільки рядків у базі — стільки й на
   екрані, поіменно» істинне і при 16, і при 0. «≥3» істинне лише в будні.
3. **Порожній стан — ГУЧНИЙ `skip` із названою причиною**, ніколи не тихий pass.
   Причина мусить пояснювати, ЧОМУ порожньо («тижневі задачі живуть Пн-Пт»), інакше
   наступний читач знову вирішить, що це поломка.

⚠️ **І перевірте, де новий гейт реально біжить.** Механізм на scratch-БД на
прод-сервері скіпається (бінарів PostgreSQL там немає), тож `npm test` довелося
вписати ОКРЕМИМ КРОКОМ у процедуру деплою (`docs/INFRASTRUCTURE.md` §7, крок 0) — до
того він був названий обовʼязковим лише в DoD, а в самій процедурі не згадувався
жодним рядком. Гейт, який стереже те, що можуть не запустити, — це той самий
«гудок, від'єднаний від сигналізації».

📅 **ПРОГНОЗУ ПЕРЕВІРЕНО ЧИСЛОМ, І ВОНА ЗІЙШЛАСЬ (25.08.2026).** Разом із діагнозом
було дано фальсифіковану прогнозу: «цих вихідних гейти НЕ впадуть (покриття 5 ≥ 3), а
**1 вересня о 00:00 воно стане 0**, і незмінений `#61b` почервоніє саме тоді». Замір по
днях на бойовій базі: **24.08 → 15 · 25-28.08 → 16 · 29-31.08 → 5 · з 01.09 → 0**.
Тобто обрив настає **на межі МІСЯЦЯ, у ВІВТОРОК**, а не у вихідні.

🔴 **І САМЕ ЦЕ ЗМУСИЛО ПЕРЕПИСАТИ ТЕКСТ ПРИЧИНИ.** Перша редакція скіпу пояснювала
порожнечу лише днем тижня («живуть Пн-Пт, заводять щопонеділка») — правда для вихідних
і **пастка для 01.09**: читач, побачивши цей текст у вівторок, вирішив би, що тиждень
робочий, отже це таки дефект. Причина тепер називає **обидва** джерела порожнечі й
відсилає до `#220`-`#220c`; що вона їх називає, стереже сам `#221b` (саботаж: прибрати
згадку місяця → червоніє). Урок ширший за цей випадок: **пояснення порожнечі мусить
покривати ВСІ її законні причини, інакше воно бреше рівно в той день, коли настане
причина неназвана.**

🧨 **Саботаж тут ловить і помилку в САМІЙ фікстурі.** Перша редакція `#220b` сіяла
тижневу Й місячну парасольку одночасно — і була беззубою: `ORDER BY period_start ASC`
разом із `manual.set()` віддає перемогу останньому рядку, тож тижнева вигравала навіть
із прибраним фільтром. Справжній баг («Андрусенко 0 / 135к») стався там, де тижневої
задачі не було ЗОВСІМ. Побачив це не читанням, а саботажем: гейт лишився зеленим на
зламаному коді.

## Тести — ОДИН набір, різні режими

**Точка входу одна: `npm test` (`node --test "dist/**/*.test.js"`). Другий, паралельний
механізм заводити ЗАБОРОНЕНО** — дві культури тестів через місяць розходяться, і ніхто
не знає, що саме зелене. Потрібне інше оточення — це РЕЖИМ, а не окремий набір
(`backend/src/testMode.ts`).

| Команда | Що додає |
|---|---|
| `npm test` | чисті тести; усе, що потребує БД/API/моделі — **пропускається з видимою причиною** |
| `npm run test:prod` | + перевірки проти живого API (`API_BASE`) — приймання після деплою |
| `npm run test:matrix` | + ПОВНИЙ зліпок доступу `#11` (177 роутів ≈ 1024 проби, ~7-8 хв) |
| `npm run test:ai` | + AI-оракул (платний: звертається до моделі) |

- Тест, якому бракує оточення, дає `SKIP` із поясненням, а не «pass». Мовчазний пропуск = фальшиве зелене.
- ⚠️ Імпорти в БД-тестах **ліниві** (`await import` у тілі): `db/pool.js` → `config.js` кидає
  на відсутньому `DATABASE_URL` ще НА ІМПОРТІ, тобто раніше, ніж спрацює `skip`.
- **ТЕСТ #1 — версія коду в `/api/health`** (`src/version.ts`, `dist/version.json` пишеться
  на білді). Відповідає на питання «чи прод крутить САМЕ те, що ми викотили». Health із
  самим `ok:true` одного разу 11 годин підтверджував «усе добре», поки прод крутив старий
  код (рестарт убив не той PID). Прогнати `npm run test:prod` ОДРАЗУ після викату.
- **Стандарт зеленого для AI-гейту** (рішення власника 31.07.2026): 2 прогони · ІДЕНТИЧНІ
  числа · числа == ЯДРУ. Один прогін не доказ. Еталон рахує ядро в момент прогону —
  зашитий у файл еталон протухає мовчки.
- **⏱ ЧОМУ ПОДІЛ `test:prod` ↔ `test:matrix`.** Повний зліпок доступу виріс із 65 роутів
  до 177 (крок B1), і прогін `#11` став ~7-8 хв. Тримати його в кожному прийманні означало б,
  що його почнуть пропускати — а пропущений гейт гірший за повільний. Тому названий поділ:
  `test:prod` — на КОЖЕН деплой; `test:matrix` — перед злиттям у прод-гілку і після
  будь-якої правки гейтів/ролей. Обидва в маніфесті, обидва названі; «зазвичай ганяємо
  шматок» — заборонено.
- **🔒 МАНІФЕСТ НАБОРУ — `src/testManifest.ts` + `src/manifest.test.ts`.** Перелік файлів
  і тестів, які МАЮТЬ існувати; звірка в обидва боки (зник → червоний з назвою; з'явився
  незареєстрований → теж червоний). **Навіщо:** набір показав «48 тестів, 0 падінь», коли
  двох тестів у ньому не було — правка тихо не застосувалась. Зелений вивід не відрізняє
  «пройшов» від «не існує». Додав тест → додай рядок у маніфест: склад набору змінюють
  СВІДОМО, як lock-файл.
  ⚠️ Динамічні тести (цикл по `FORBIDDEN_TABLES`) статично не видно — їх покриває окрема
  звірка джерела циклу. Маніфест не імпортує модулів із `config` і працює БЕЗ БД, інакше
  сам став би тим, що мовчки не виконується.
- **🪞 ДЗЕРКАЛЬНІСТЬ — ПРАВИЛО, не побажання.** Будь-який тест на **ЗАБОРОНУ** вимагає
  парного тесту на **ДОЗВІЛ**. Одностороння перевірка зеленіє і тоді, коли фіча зламана
  повністю: зріж поля ВСІМ — «менеджер не бачить реквізитів» стане зеленим, а Виписка
  мертвою. Стосується всього, де є межа: скоупи ролей, приховані платежі, трекер,
  реквізити, доступ до екранів. Пара: `#5.6` (не бачить) ↔ `#5.7` (бачить, кому можна`).
- **🔒 ТЕСТИ ПРОТИ ПРОДА ХОДЯТЬ ТІЛЬКИ READ-ONLY** (`src/testReadOnly.ts`, роль `test_readonly`
  у `schema.sql`). `test:prod`/`test:ai` йдуть через `--import ./dist/testReadOnly.js`, який
  ставить `PGOPTIONS=-c role=test_readonly` — роль їде в **стартовому пакеті** з'єднання.
  **✅ КОМАНДА ПРИЙМАННЯ — РІВНО `npm run test:prod`** (з `API_BASE=…`).
  **❌ НЕ `TEST_SCOPE=prod npm test`**: `npm test` не має `--import`, тож preload не
  виконується і набір пішов би в бойову базу з ПОВНИМИ правами. Цей обхід тепер
  закритий гейтом у `db/pool.ts` (падає ДО першого запиту), але команду все одно
  писати правильну — гейт це другий рубіж, а не запрошення писати абищо.
  **Навіщо саме так, а не `pool.on("connect") → SET ROLE`:** обробник `connect` у pg не
  очікується, `SET ROLE` програв би гонку першому запиту тесту → запобіжник спрацьовував би
  ЧЕРЕЗ РАЗ, а виглядав завжди зеленим.
  **Навіщо взагалі:** юніт-тест гейтів обійшов middleware і виконав справжній
  `DELETE FROM monthly_goals` проти ПРОДА — врятувало лише те, що id не існував. Третій
  поспіль випадок, коли рятує випадковість, тож межа переїхала в БД, де не залежить від
  уважності того, хто пише запит. **Не зміг обмежити → процес ПАДАЄ**, а не їде з повними
  правами. Тести: `#13` (роль активна) · `#13b` (запис по РЕАЛЬНОМУ рядку падає на правах,
  усе в транзакції з гарантованим ROLLBACK — права це перший рубіж, транзакція другий) ·
  `#13c` (дзеркало: читання працює, інакше набір мовчки осліп би).
- **🔒 ГЕЙТ У `db/pool.ts` (`readOnlyGuard.ts`) — щоб обхід був неможливий, а не лише
  небажаний.** `pool.ts` — єдине місце, крізь яке проходить БУДЬ-ЯКИЙ шлях до БД: роут,
  джоба, юніт-виклик обробника, разовий скрипт. Гейт стоїть **перед `new Pool`**, тож
  падіння стається до першого запиту. **Інертний у проді:** спрацьовує лише при
  `TEST_SCOPE=prod`, якого бойовий процес не має — перший рядок функції це й перевіряє.
  Тести обов'язково В ОБИДВА БОКИ, бо це бойовий код: `#14` (у проді не втручається) ·
  `#14b` (ловить обхід) · `#14c` (дзеркало: з правильним PGOPTIONS пропускає — інакше
  гейт міг би кидати ЗАВЖДИ, а ми б читали це як надійність) · `#14d` (`options=` у
  `DATABASE_URL` має пріоритет над PGOPTIONS і мовчки скасував би роль).
- **🔴 РОЛЬ-КЕШ RBAC — FAIL-CLOSED, І СЕРВЕР БЕЗ НЬОГО НЕ СТАРТУЄ.** Було: `roleHasTab`/
  `roleHasPerm` при порожньому кеші віддавали `true` ВСІМ, а `app.listen` стояв у
  `.finally(refreshRoles)`. Тобто збій завантаження ролей на старті лишав сервер
  працювати з **відкритими** гейтами — `reset_passwords`, `manage_users`,
  `view_balances`. Не лікувалось: `refreshRoles` кликали лише на старті й при записі
  ролі. Спрацювало б рівно під час crash-loop, коли кожен респавн гониться з БД.
  Чотири рубежі: **(а)** `loadRolesOrDie()` + `.then(listen)`, ретрай 5×(1+2+4+8+16с) —
  переживає холодний старт Neon, інакше `exit 1`; **(б)** обидві функції fail-closed;
  **(в)** періодичний `refreshRoles` раз на 10 хв; **(г)** джерело `roles` у банері.
  ⚠️ **ПОБІЧНИЙ НАСЛІДОК, записаний навмисно:** у стані «кеш не завантажився» сервер
  **не стартує**, отже банера ніхто не побачить — сигналізація живе всередині
  застосунку, який не піднявся. Джерело `roles` ловить ЛИШЕ випадок, коли кеш
  спорожнів ПІСЛЯ старту. Це прийнято свідомо: лежачий сайт помітять і без банера.
  🔒 **Умова існування цього рішення — супервізор.**
  🟢 **ПОНОВЛЕНО 25.08.2026: умова ВИКОНУЄТЬСЯ, і це заміряно.** Респавн після краху
  спостережено на проді (`app_boot` 23.08: 17 стартів поспіль, каденція ~60 с, без
  відмови піднімати), а в контейнері доведено, що `exit 1` і вбивство сигналом віддають
  ОДНАКОВИЙ код 0 крізь `| tee`, тобто ззовні нерозрізненні. Ціна `exit 1` — не лежачий
  сайт, а цикл ≈31 с ретраїв + до ~60 с, із самовідновленням. Межі заміру й залишкові
  10% — у `docs/INFRASTRUCTURE.md` §5. Абзац нижче лишається як історія питання.

  🔴 **І САМЕ ЦЮ УМОВУ ПІДІРВАНО 17.08.2026 — див. розділ «РЕСТАРТ ПРОДА» вище.** На
  викаті `878600b` кнопки Supervisor на процес НЕ подіяли, а SSH його не бачить; рестарт
  зробила техпідтримка adm.tools. Отже «панель підніме процес заново» — більше **не
  доведене твердження, а припущення**, і все нижче в цьому абзаці читати з цією поправкою.
  Далі — як було записано раніше (заміри 05.08.2026): панель adm.tools піднімає процес
  заново (тоді: pid змінюється, health 200 за ~15 с). Рестарт **не залежить від
  коду виходу**, бо команда — `npm start 2>&1 | tee -a …`, і код пайплайна = код `tee`,
  тобто **завжди 0** незалежно від того, чи node вийшов 0, 1 чи вбитий сигналом.
  Тому `exit 1` для панелі не відрізняється від рестартів, які ми робимо щодеплою.
  ⚠️ **ЗВОРОТНИЙ БІК, який треба памʼятати:** та сама властивість означає, що **код
  виходу node не бачить НІХТО** — ні панель, ні ми. Аварійне завершення й штатну
  зупинку розрізнити за кодом виходу **неможливо в принципі**, поки в команді стоїть
  `| tee`. Якщо колись знадобиться реагувати на падіння інакше, ніж на штатний стоп
  (алерт, інший бекоф, стоп-петля) — це доведеться робити **власним сигналом**:
  запис у `job_runs`/`sync_state` перед виходом, або окремий лог-маркер, який ловить
  зовнішній монітор. Не кодами виходу — вони тут мертві.
- **🧬 «МІГРАЦІЯ ПРОЙШЛА» ≠ «ЗМІНА ЗАСТОСУВАЛАСЬ»** (`db/roleDeclarations.ts` + `#15…#15e`).
  Коміт `6a274f2` написали САМЕ щоб дати HR company-scope, оформили як
  `INSERT … ON CONFLICT DO NOTHING` — роль уже існувала, тож statement не зробив нічого.
  `npm run migrate` три доби друкував «Migration applied.», а фікс не застосувався жодного
  разу. Той самий клас, що й маніфест набору, тільки для **даних**.
  **Межа точності (важлива, інакше тест став би шумом):** `PUT /settings/roles/:key`
  дозволяє адміну міняти `name`/`data_scope`, але **лише НЕвбудованим** ролям
  (`if (def.builtIn) return 403`). Тому: вбудовані — звіряємо **строго** (`#15b`);
  `built_in` — строго для всіх, його не пише жоден роут (`#15c`); кастомні — через
  **реєстр** `ACCEPTED_DIVERGENCES` (`#15d`), де розбіжність треба назвати вголос;
  `screen_access`/`permissions` — **не звіряємо взагалі** (адмін крутить тумблери щодня).
  Дзеркало `#15e` не дає реєстру стати смітником: мертвий запис глушив би справжню
  розбіжність. **Корекція в схемі — разова, з умовою на зламане значення**
  (`WHERE key='hr' AND data_scope='own'`), а не постійний синк: постійний мовчки
  відкочував би правки адміна щодеплою (перевірено: свідомий `team` лишається `team`).
- **🔴 ПРИВІЛЕЇ (`GRANT`/`REVOKE`) — ТІЛЬКИ В КІНЦІ `schema.sql`**, у блоці «ПРИВІЛЕЇ РОЛЕЙ».
  Блок `ai_readonly` перелічував `tracker_devices`, що створюються НИЖЧЕ: на живій базі
  проходило, на ПОРОЖНІЙ міграція падала з «relation does not exist». Той самий клас, що й
  сид `key_card` перед `INSERT` — видно лише з нуля.
  ⚠️ Тест #8 цього не ловив, бо без `TEST_SCRATCH_DB_URL` він просто ПРОПУСКАВСЯ — тобто
  «схема з нуля» ніколи насправді не прогонялась. **Виправлено: набір піднімає базу САМ**
  (див. нижче).
- **🧪 СКІП, ЯКИЙ НІКОЛИ НЕ ВИКОНУВАВСЯ, — НЕ СТРАХОВКА, А ІЛЮЗІЯ.** `db/scratchDb.ts`
  сам піднімає одноразовий кластер PostgreSQL (unix-сокет у своєму ж tmp-каталозі, без
  TCP), накочує `schema.sql` і зносить його у `finally`. Ручний `TEST_SCRATCH_DB_URL`
  лишився як override для CI, але більше не потрібен: `npm test` у деві реально
  перевіряє схему з нуля щоразу.
  **Межа:** на ПРОД-сервері бінарів PostgreSQL немає (БД зовнішня, Neon) → #8/#8b чесно
  пропускаються з причиною, що НАЗИВАЄ причину. Створювати службову базу на бойовому
  Neon заради тесту свідомо НЕ робимо (запис у бойовий проєкт + compute-квота).
  Дзеркало `#8b`: підкидає в копію схеми `GRANT` на неіснуючу таблицю і вимагає падіння —
  інакше #8 зеленів би й тоді, коли перевіряти нічого. **Бойовий `migrate.ts` env-хука на
  довільний файл схеми НЕ має** — #8b підміняє схему в копії `dist`, а не в проді.

## 🟢 Рішення власника по доступах (31.07.2026) — НЕ «виправляти» назад

Обидва пункти — **свідома політика**, а не недогляд. Вони виглядають як діри, тому
записані сюди: щоб ніхто (включно з нами) не «полагодив» їх удруге.

- **МЕНЕДЖЕРИ БАЧАТЬ РЕКВІЗИТИ, включно з НОМЕРОМ КЛЮЧ-КАРТИ.** `/api/bank/accounts`
  віддає всім автентифікованим: `label, company, bank, currency, iban, edrpou_ipn,
  director, legal_address, mfo, key_card`. Це **функція**: менеджер щодня виставляє
  клієнту рахунок, а ключ-карту й заводили, щоб він давав її клієнту на оплату.
  Тест #5.6 колись подав ФАКТ («поля їдуть усім») правильно — помилковим був **вирок**
  «це витік», зроблений без питання до власника; обмеження відкочено.
  **Закритим лишається те, що є грошима чи ключами, а не реквізитами:** баланси
  (`/balances`), виписка, редагування рахунків, `env_key_name`.
  🔒 Спосіб складання відповіді — з **явного списку полів** (не спредом рядка БД) —
  збережено НЕЗАЛЕЖНО від політики: нова колонка в `bank_accounts` не поїде назовні
  сама. Політика змінилась, список ширший; принцип той самий.
  Тести перевернуто, не видалено: `#5.6` (менеджер/тімлід **отримують**) ↔ `#5.7`
  (адмін/фінансист теж) ↔ `#5.8` (ключ-карта видима всім) ↔ **`#5.8b`** (без токена —
  нічого: «усі» означає «всі автентифіковані», а не «весь інтернет»).
- **ФІНАНСИСТ = РІВЕНЬ АДМІНА.** Попередній дефолт «немає вкладки → закрити ендпоінт»
  **скасовано**: 15 продажних ендпоінтів не закривали, а роль вирівняли до адмінської
  (2 екрани → 27, 3 права → 12). Робиться **екранами/правами в `roles`**, не хардкодом
  по ролі в роутах. **Два винятки** (попередні рішення власника): `reset_passwords` і
  1×1 (це право СЕО/ОД та HR; admin їх не бачить, тож і фінансист не бачить).
  ⚠️ `manage_users` теж не віддано — і це **умова існування винятків**, а не звуження:
  з ним роль редагує ролі, тобто сама собі видає `reset_passwords` і `view_all_1x1`.
  Та сама логіка вже застосована до КВП. Прибрати `- 'manage_users'` можна лише
  СВІДОМО приймаючи, що обидва винятки перестають триматись.
- **HR — `data_scope='company'` СВІДОМО.** Ухвалено 29.07.2026, але не доїхало (див.
  нижче про незастосовані міграції); відновлено 31.07. При `own` scope-clamp тиснув HR
  у `manager` з `managerId=-1` → екрани відкриті, дані порожні. Єдине реальне
  розширення — **duty**: бачить чужі відсутності, але **редагувати не може**
  (`canEdit = admin|team_lead`). Чужі особисті задачі НЕ відкрились: приватність
  тримається на `assignee_id IS NULL` у SQL, а не на scope. Доступ до 1×1 дає **право**
  `view_all_1x1`, не scope. **Доступ до ендпоінтів не змінився ЖОДНОЮ клітинкою** —
  доведено диференційною пробою 65 клітинок до/після.
  🔴 HR став `auth.role='company'` — те саме значення, що мав фінансист до підйому.
  **HR НЕ адмін**: підйом дає лише право `admin_scope`, якого в HR немає (тест `#5.13`
  звіряє профіль HR цілком і має дзеркало «фінансист МАЄ бути адміном»).
- **🔒 БАНК ДЛЯ HR ЗАКРИТИЙ СВІДОМО.** У схемі є `UPDATE … WHERE NOT (screen_access ?
  'bank')` з наміром «вкладку bank бачать УСІ ролі». Він **ніколи не спрацює** для HR
  (ключ уже є зі значенням `false`) — і це **добре**: намір тієї міграції хибний, HR
  банк бачити не має. **Не «лагодити» цей guard.** Зламаний guard випадково зберіг
  правильний стан.
- **🔴 ПРАВИЛО: знайшов незастосовану міграцію — НЕ застосовуй автоматично.** Спершу
  перевір, чи намір досі правильний. Один клас дав обидва результати: `hr.data_scope`
  — намір правильний, застосували; `bank` для HR — намір хибний, застосовувати не можна.
- **`admin_scope` — право, а не список ключів у коді.** Було зашито
  `key === "ceo" || "opdir" || "kvp"` у `scopeCompatRole`; тепер підняття до admin
  вирішує право в БД. Додати роль адмінського рівня = запис у `roles`, не правка
  `rbac.ts`. Еквівалентність доведено тестом `#5.2b`: стара логіка vs нова для кожної
  ролі, дозволена рівно одна розбіжність — `financier`.

## 🧮 «ВИКОНАЛОСЬ 450 ІЗ 466» — ЦЕ СТОП, А НЕ РЯДОК СТАТИСТИКИ (25.08.2026)

**Правило власника, для всіх чатів:** якщо `виконалось < обовʼязкових` — прогін НЕ
зарахований, і далі не йдемо. **Червоне показує, що зламалось; недобір показує, що
ми не дивились.** Друге гірше, бо читається як приймання.

📐 Привід: приймання Е3 надрукувало `ВИКОНАЛОСЬ 450 із 466 · падінь 7`, і я подав
450 числом у таблиці — не як тривогу. **16 обовʼязкових перевірок не виконались**, а
прогін виглядав як пройдений.

### 🎲 ПАДІННЯ НА РІВНІ ФАЙЛУ БЕЗ ТЕКСТУ ПОМИЛКИ — НЕДЕТЕРМІНОВАНЕ

Заміряно двома прогонами `test:prod` **на тому самому коді**:

| | прогін 1 | прогін 2 |
|---|---|---|
| `klassSliceGate` · `enps` · `oneOnOneSaveGuard` · `taskCommentKeep` | ✖ | ✔ |
| `legacyAnswers` · `reportTable` | ✖ | ✖ |
| `#124 G3 Σ 1С == Σ БАЗИ` | ✔ | ✖ (`1С 303, у базі 302`) |
| виконалось | 450 із 466 | **468 із 466** |

**Ті самі шість файлів окремо: 40 тестів, 0 падінь, жодного падіння файлу.** Отже це
властивість ПОВНОГО прогону, а не файлів. Усі підтести «впалих» файлів у самому
прогоні зелені, повідомлення — лише `'test failed'`.

⚠️ **Механізм НЕ доведено.** Ознак памʼяті чи сигналу в лозі нема (`heap`, `SIGKILL`,
`exitCode` — нуль згадок). Доведено рівно недетермінованість. Спільна ознака шістки —
усі читають джерело фронту й піднімають **компілятор TypeScript**; це підозра, не
висновок. HR міряє з іншого боку (прод — CloudLinux LVE, `nproc` 48).

🔴 **НАШ СПОСІБ ПОСИЛЮВАТИ ПЕРЕВІРКИ ПОСЛАБЛЮЄ ПРОГІН.** Кожен новий гейт, що
звіряє фронт із ядром (`#145c`, `#213`), додає ще один процес із компілятором.
Лікується **спільним транспілятором**, а не відмовою від гейтів.

### ⏱ ХВІСТ 30 с У READ-ONLY PRELOAD (знайдено 25.08.2026, лагодить HR)

`--import ./dist/testReadOnly.js` піднімає пул для самоперевірки ролі й **не закриває
його**. Процес не може вийти, поки не спрацює `db/pool.ts: idleTimeoutMillis: 30_000`.

📐 Заміряно, розкид по чотирьох файлах — 0.02 с:

```
legacyAnswers  з preload 30.36 s · без preload 0.36 s   (тести всередині — 0.29 s)
testRunGate    30.37 s   ·  jobAlert 30.36 s  ·  versionStale 30.35 s  ·  period 30.35 s
```

Тобто **кожен файл набору, який не кличе `pool.end()` сам, платить 30 секунд**. Це і
є підлога часу `test:prod`, і тепер ще й підозрюваний у падіннях вище.

⚠️ **Не плутати з «`legacyAnswers` повільний»:** замір HR (312-347 мс) робився БЕЗ
preload, мій — З ним. Обидва праві про свою команду; цінна тут УМОВА, не число.

### 🧨 САБОТАЖ ТРЕБА ЗНІМАТИ ЧЕРЕЗ `rm -rf dist`, А НЕ ЛИШЕ ПОВЕРНЕННЯМ ДЖЕРЕЛА

Прогін саботажів `#102b`/`#102c` завершився «ПОВЕРНЕНО», і `#102c` лишився
**червоним**. Джерело було відновлене (перевірено `grep` — рядок предиката на
місці, `git status` чистий), а `dist` тримав саботовану версію: інкрементальний
`tsc` не перезібрав файл. `rm -rf dist && npm run build` — зелено.

🔴 **Це небезпечно в ОБИДВА боки.** Тут вийшло фальшиве ЧЕРВОНЕ, і його видно. Але
той самий механізм дає фальшиве ЗЕЛЕНЕ: наклав саботаж, зібрав інкрементально,
гейт не почервонів — і висновок «гейт беззубий» був би неправдою. Тобто це не
незручність, а спосіб отримати неправильний вирок про сам саботаж.

Правило `rm -rf dist && npm run build` перед прийманням існувало через несвіжі
файли після revert; тепер воно стосується й **кожного кроку саботажу**.

## 🪤 ТРИ ВИДИ ГЕЙТА, ЩО ВИГЛЯДАЄ ЯК ПЕРЕВІРКА Й НЕЮ НЕ Є (25.08.2026)

Усі три спіймано **саботажем за один день**, жодного — читанням. Спільне в них те,
що кожен був зелений на ЗЛАМАНОМУ коді, і кожен читався бездоганно.

**① ГЕЙТ НА ПРИСУТНІСТЬ РЯДКА МУСИТЬ МАТИ МЕЖУ СЛОВА** (правило власника).
`assert.match(sync, /INSERT INTO leadgen_touch/)` пропускав перейменування писаря на
`INSERT INTO leadgen_touch_OFF` — підрядок збігається. Тобто дзеркало ловило **лише
повне видалення**, а найтиповіша поломка тут — саме підміна. Правильно `\b(?!_)`.
🔴 **Видалення й підміна — різні поломки, і підміна типовіша.** Перевірка на
присутність без межі слова стереже рідший випадок і мовчить про частіший.

**② ГЕЙТ, ЩО ПОРІВНЮЄ ДВА СКОУПИ, НЕ ДОВОДИТЬ НІЧОГО ПРО КЛЮЧ КЕШУ.**
`#211c` у першій редакції стверджував «тімлід A ≠ тімлід B» — мовляв, зі зламаним
ключем B побачив би числа A. Насправді нерівність тримається **незалежно від кешу**:
18 із 29 запитів `/overview` не кешуються взагалі й самі залежать від скоупу. Саботаж
власника (булеве `isScoped` замість значень) лишив гейт **зеленим**.
✅ Правильне твердження — **«з кешем» проти «без кешу» для КОЖНОГО скоупу**: відповідь
на чистому кеші == відповіді на кеші, ПРОГРІТОМУ ЧУЖИМ скоупом. Воно не залежить від
того, які поля різняться, і падає рівно тоді, коли ключ недостатній.

**③ ГЕЙТ, ЩО ПОРІВНЮЄ ДВІ КОПІЇ, НАПИСАНІ В САМОМУ ТЕСТІ.** `#214c` спершу тримав
обидві гілки SQL усередині тест-файлу — і доводив рівність двох рядків, написаних
поруч, а про продакшн-функцію не свідчив нічого. Ліворуч має стояти **виклик ядра**.
⚠️ Того ж дня в еталон того самого гейта я вписав **свою копію `MONEY_ZONE`** — і він
упав БЕЗ саботажу. Прибрав копію SQL і тут-таки завів копію КОНСТАНТИ.

🔑 **ЗАГАЛЬНЕ ПРАВИЛО ПРО КЛЮЧ (власник, 25.08.2026):** *«у ключі мусить бути все, що
змінює відповідь. Не „схоже, що досить“ — а перелічено й засаботажовано».* Тому ключ
кешу будується **з АРГУМЕНТІВ виклику** (`overviewCache.call(name, fn, ...args)`), а не
пишеться руками: забути `teamId` тоді структурно неможливо.
⚠️ Зворотний бік: **зайвий аргумент у ключі тихо вимикає кеш**. `awaitingNowSnapshot`
не читає `from`/`to`; передавши повний скоуп, ми поклали б період у ключ і не влучили б
НІКОЛИ. Тому виклик звужений — а безпечність звуження доводиться **рівністю результатів
на живих даних**, не читанням тіла функції (`#211e`).

## 🔁 ПІСЛЯ БУДЬ-ЯКОЇ ПРАВКИ — ПОВНИЙ НАБІР, А НЕ СВІЙ ФАЙЛ (25.08.2026)

Я полагодив картку менеджера, прогнав **лише свій гейт-файл** — зелено — і викотив.
`test:prod` після викату впав на `#92b` («`MgrStrip` отримує не переданий рядок»):
фікс обгорнув `narrowToSlice` і той виклик, який отримує **вже звужений** `rows[0]`,
тобто віддав КОПІЮ рядка замість самого рядка.

🔴 **«Звузити всі три» і «звузити правильно» — різні речі**, і перше ламає сусідній
гейт. Помилка не в інструменті: я сам звузив перевірку до власних гейтів після того,
як змінив код, якого мої гейти не покривають.

📐 І наступний круг на тому ж місці: пояснювальний коментар, поставлений МІЖ
`renderCard={(m) => (` і `<MgrStrip`, повалив `#92` — той звіряє джерело регуляркою
`renderCard=\{\(m\) => \(\s*<MgrStrip`. Гейт на джерело фронта чутливий до
форматування; коментар — у блок ВИЩЕ.

## Джоби (backend/src/jobs)

> ⚠️ **Частоти нижче — АКТУАЛЬНІ (з коду `index.ts`), не «намір».** Після IP-бану 08.07.2026 темп свідомо знижено заради стабільності доступу до Kommo (див. коментарі в `index.ts`). Реальні розклади ≠ старим «5хв/10хв».
> 🔒 **job_locks-замок (КРОК 5):** поки біжить важка Kommo-джоба (звірка / бекфіл / auto-heal), `syncKommo` бачить свіжий heartbeat (`job_locks`, <2 хв) і **пропускає прохід** — щоб потоки не наклались і не пробили ліміт Kommo. У БД (важкі джоби біжать окремим nohup-процесом), heartbeat раз на 30с → мертвий замок знімається сам. `jobLock.ts`. НЕ плутати з ручним `KOMMO_PAUSED`.

- `syncKommo` — **кожні 30хв** (`*/30`) + старт (угоди, менеджери). ⚠️ Тягне контакти/компанії ЛИШЕ по id угод вікна (батчі по 250) — `fetchAllContacts/fetchAllCompanies` OOM-вбивали job. **НЕ повертати fetchAll\*.** Захищений: in-process guard, таймаут 60с, watermark рухається ЛИШЕ при успіху, + job_locks-skip. Нічна реконсиляція **04:00** (`reconcileDays:10`). З КРОК 5 пише `manager_team_history` при зміні team_id.
- `syncReceivables` — кожні 15хв (Google Sheet дебіторки, TRUNCATE+insert).
- `syncStageEvents` — **кожні 30 хв (:15/:45)** (події `lead_status_changed` → `deal_stage_events`). Backfill: `--months=N`. **🔴 АКТУАЛЬНІСТЬ «ОТРИМАНИХ КОШТІВ» = ЧАСТОТА ЦІЄЇ ДЖОБИ.** `core/money.ts` анкериться на `deal_stage_events`, тож хто змінить цю частоту — змінить свіжість грошей у дашборді. Було 3 год → «за сьогодні» відставало до 3 год; 30 хв дає лаг ≤30 хв (як `syncKommo`). Вартість +~30 req/добу — тротл не помічає. **⚙️ Вотермарк рухається ПОРЦІЯМИ ≤24 год** (`processInChunks`, спільне ядро для 3 event-джоб): раніше «все або нічого» після повного проходу — один збій за 51 стор. втрачав прогрес, вікно росло → застрягло на 09.07 (гроші тихо недораховувались). Тепер збій втрачає лише порцію.
- `syncTransfers` — **раз на добу 05:20** (резерв; «передані заявки» тепер із «Реєстру» лідоген-бота). `entity_responsible_changed` на лідах Кваліфікації → `lead_transfer_events`. ⚠️ саме `entity_responsible_changed`, не `lead_responsible_changed` (той 400). ⚙️ Вотермарк порціями (`processInChunks`) — той самий фікс, що event/activity (був застряг з 08.07).
- `syncDealActivity` — **кожні 3 год (:40)** (нотатки → `deals.last_activity_at` = остання РЕАЛЬНА людська активність; Salesbot `created_by 0` НЕ рахується). Живить «застряглі угоди». ⚙️ Вотермарк порціями (`processInChunks`) — той самий фікс (був застряг з 08.07 → «застряглі» бачили несвіжу активність).
- `syncAdBudget` — **щогодини (:15)** (Google-таблиця реклами → `ad_budget_daily`). ⚠️ Таблиця має лише поточний місяць — минулі накопичуються.
- `syncFirstTouch` — кожні 30хв (лист «Перший дотик» → `first_touch_analysis`).
- `syncRingostatCalls` — **щогодини (:35)** (Ringostat API → `sales.calls`). Мапінг **по `employee_fio`** → наш менеджер → тімлід. Результативні = `billsec > 0`. ⚠️ Поле `department` Ringostat **ненадійне** (недобирає ~23%) — **не використовувати**.
- `syncCashIncome` — раз на 3 год (:50) (приход по готівкових 142-угодах через `extractIncomeAmount`, НЕ бюджет — price занижує ~3×).
- `evaluateKpiTasks` — щодня 07:00 (факт із CRM, автозакриття KPI-задач).
- `recomputeStatistics` — щогодини (:25) (розділ «Статистики (відділи)»).
- `snapshotCarryover` — 1-го числа 00:00 (знімок «перенесених» → `monthly_carryover`).
- `reactivateLeads --write --limit=N` — створення лідів реактивації.
- `backupDb` — щодня 03:00 (кожна таблиця → gzip-CSV, ретенція 14). БД на **Neon** (PITR — основний бекап). Деталі — `docs/BACKUP_RECOVERY.md`.
  - **🩹 ГОНКА В `pg-copy-streams` — ПРИЧИНА 60% ПАДІНЬ НА `COPY`** (знайдено 11.08.2026,
    патч `db/copyStreamPatch.ts`). `_cleanup()` обнуляє `this._buffer`, але охоронець
    `_forward()` перевіряє лише `_forwarding`/`_drained`/`connection` — жодного з цих
    полів `_cleanup` не чіпає. Прилетіла подія `readable` після завершення копії →
    `_parse` дістає `null` → `Cannot read properties of null (reading 'push')`.
    **Не таблиця і не розмір рядка:** `stats_series` — 9 892 рядки, 3 МБ, найбільший
    рядок 160 байт; та сама помилка ловить `deals`. **Апгрейд не лікує:** `copy-to.js`
    у 7.0.0 байт-у-байт той самий, окрім шляху `require('obuf')`.
    **Рветься ВСЕРЕДИНІ циклу `_forward`, а не між викликами** (доведено стеком: `_read →
    _forward(87) → _parse(95)`): на якомусь chunk копія завершується, `_cleanup` обнуляє
    буфер, але `_drained` досі `true` — цикл читає ще один chunk і падає. Тому патч на
    `_parse`, і він повертає **`false`**, а не ковтає chunk: `false` лягає в `_drained`, а
    умова циклу перевіряє його ПЕРШИМ, отже `read()` більше не кличеться. Проковтнути chunk
    було б гірше за падіння — після `_detach()` потік належить обробнику `pg`, і зайвий
    `read()` украв би повідомлення протоколу, зіпсувавши **зʼєднання** замість копії.
    ✅ Заміряно після патча: **30 успіхів із 30**; повний бекап — **79/79, `retried: 0`**.
    🔴 **УРОК, ЩО КОШТУВАВ ОДНОГО ВИКАТУ:** перша редакція стерегла ВХІД у `_forward`, і гейт
    `#75b` був **зелений**, поки патч не працював (15 падінь із 30 на проді). Фікстура
    відтворювала мою ГІПОТЕЗУ (гонка між викликами), а не механізм; саботаж її валив, і це
    виглядало як доказ. **Саботаж підтверджує лише те, що фікстура моделює** — якщо модель
    неправильна, зелений колір нічого не вартий.
  - **📉 `tables retried: 0` У МАНІФЕСТІ — ВБУДОВАНИЙ ДЕТЕКТОР, що патч працює.**
    Ретрай спрацьовує лише тоді, коли `COPY` упав; отже поки патч тримає, повторів бути
    не має. Зʼявились ретраї в нічних копіях → патч перестав накладатись (оновили
    бібліотеку), і це видно **без окремої перевірки**. Не «дрібниця в логах»: до патча
    у нічній копії 11.08 було `retried: 5` і `failed: 1`.
  - **🗑 ФАЙЛ НЕВДАЛОЇ ТАБЛИЦІ ВИДАЛЯЄТЬСЯ.** Обірваний `stats_series.csv.gz` на 80 КБ
    лишався в каталозі, файлів було рівно 78 — і копія **виглядала** повною. Маніфест
    казав правду, файли брехали; хто відновлюється за `ls`, а не за маніфестом, підняв би
    обрізану таблицю. Той самий клас «наявність артефакту ≠ придатність», поверхом глибше
    за порожні каталоги.
  - **`COPY_ATTEMPTS = 8`** (було 3) — підстраховка, поки патч не підтверджено нічними
    прогонами. При частоті падінь 60% три спроби давали 21.6% втрати таблиці (≈17 із 78
    за прогін), вісім дають 1.7%.
- Фронт **автооновлюється кожні 5хв** (`refreshNonce`).
- Стан синку в `sync_state`, видно в `/api/health` + індикатор у «Налаштуваннях» + кнопка «🔄 Синхронізувати зараз».

## Реактивація — критерії

Компанія (не фізособа) + є телефон + 3+ оплачених перевезень + остання оплата >3 міс тому + немає відкритої угоди в Реактивації/Продзвіні. Дедуп — по живому Kommo.

## Розділи дашборду

overview · **report** · statistics · **depstats** · teams · managers · loyalty · receivables · leadgen · tasks · messenger · news · training · settings.
Нав-групи в `components/Layout.tsx` (`NAV_GROUPS`). Період-фільтр спільний (`dateRange`/`datePreset` + `QuickPeriods`).
⚠️ Два розділи зі схожою назвою: `statistics` = старі CRM-графіки (інлайн); `depstats` = **«Статистики (відділи)»** (`StatisticsSection.tsx`). Кандидати на злиття.

## Статистики (відділи) — `depstats`

Заміна ручної таблиці «UTS Показники для статистик». 6 відділів. Гібрид: sales-метрики з CRM (auto), решта — imported/manual. Специфікація — `docs/STATISTICS_SPEC.md`, звірка — `docs/STATISTICS_RECONCILIATION.md`.
- **`statistics_values`** (EAV): `department, period_type(month|week), period_start, team_lead, metric_key, value, source(auto|manual|imported)`. **R1**: тиждень→понеділок (лист=неділя, −6 днів). **R2**: «самостійні»+«Шевчук»→`team_lead='Шевчук Назар'`.
- **`backend/src/statistics/catalog.ts`** — ЄДИНЕ джерело правди структури. Фронт і бекенд беруть перелік ЛИШЕ звідси. Планів тут НЕМА (план із `plans`).
- **Межа довіри `STATS_AUTO_FROM='2026-01-01'`**: до неї лист рахував машини вручну → непорівнянно, лишаємо imported.
- **API `/api/statistics`**: `GET /catalog`, `GET /?department&period_type&from&to`, `PUT /manual` (лише admin; auto/derived→400).

## Звіт (`report`)

Менеджер → «Мій звіт», тімлід → «Звіт тімліда», адмін → усе. Все авто з CRM (`GET /dashboard/report` + `/dashboard/funnel-report`).
- **Воронка клієнтів**: 5 етапів × розріз Нові/Постійні/Лідоген, накопичувальна за когортою СТВОРЕННЯ. ⚠️ Дає конверсії >100% — бо угоди заходять посередині воронки (див. вище). Модель потребує перегляду (МASTER_PLAN, КРОК 6).
- **План/факт по воронці**: тімлід ставить план на місяць по етапах (`funnel_plans`).
- ⏳ Ще НЕ перенесено з Excel: Логістика (перевізники), фінсуми по типах клієнтів, очікування наст. міс, цілі на день, чекліст.

### 📅 РЯДКИ ДНІВ У РОЗГОРТЦІ НАРОДЖУЮТЬСЯ З ДАНИХ, А НЕ З КАЛЕНДАРЯ (06.08.2026)

Власник спитав, чому в розгортці по днях **немає суботи 08.08**, і чи не фільтрує
її щось. Не фільтрує: `/kvp-report/manager-detail` збирає дні з рядків фактів, тож
день без жодного факту не існує в принципі — його ніде взяти.

Для **майбутніх** днів єдине джерело — планова дата оплати (`planned_payment_at`).
Заміряно 06.08: **07.08 — 78 угод, 08.08 — 0, 09.08 — 1**. Тобто субота порожня не
через межу періоду й не через робочі дні, а тому, що на неї ніхто не домовлявся
про оплату (розподіл планових дат і так зміщений у пʼятниці — 211 із 424).

🔴 **Наслідок для читання екрана:** відсутній день ≠ «нуль», він = «даних немає».
Саме тому майбутні дні тепер зведені в ОДИН підписаний рядок з екстраполяцією за
темпом, а не показані як низка нулів: низка нулів читалась би як провал, якого не було.

## Задачник KPI

Тімлід ставить план менеджеру (`POST /tasks/plan`): `ads_count`, `leadgen_count`, `dispatch_count`, `avg_check`, `conversion`. **План = ОДНА задача-парасолька** (`task_type='kpi_period'`) + приховані діти-дні (`daily_kpi`, `parent_id`). Парасолька→done, коли всі дні done. **Тижневий план — довільний період з календаря.**
⚠️ Дати в `/tasks` GET віддавати через `to_char(...,'YYYY-MM-DD')` (інакше `<input type=date>` не показує).
- **Тип «🔄 Реактивація клієнтів»**: `GET /dashboard/reactivation-candidates` — **лише компанії**, НЕ боржники: `lapsed` (3+ перевезень, замовкли) і `oneshot_bg` (1 перевезення б/г). Вибрані → **ОДНА задача на менеджера** з чеклістом (`checklist_json`).
- **Департамент** призначається АВТОМАТИЧНО з команди виконавця.
- **Календар**: власний `components/DatePicker.tsx` замість нативних `<input type=date/month>`.
- **Видимість**: менеджер — свої; тімлід — своя команда + власні; адмін — «Мої» / «Усі».

## Застряглі угоди — `/dashboard/stuck-deals`

### 📞 РОЗМОВА ПОЧИНАЄТЬСЯ З 10 СЕКУНД · ⏱ ВІК — ВІД СИНКУ · 🎧 RINGOSTAT (Фаза 3, 10.08.2026)

Три рішення власника, усі в одному правилі `core/stuckRule.ts`:

1. **Дзвінок коротший за 10 c не є активністю.** Недодзвон скидав годинник застрягання
   так само, як розмова, — тобто ХОВАВ угоду тим, що менеджер набрав номер. Заміряно
   на проді: з **31 567** дзвінків у нотатках активних FC-угод **10 238 тривали 0 c**;
   вони ховали **83 угоди**, поріг 10 c додає ще **14** (разом **+97**).
   ⚠️ `null`-тривалість ЗАРАХОВУЄМО: покриття 100%, тож ефекту немає, але «не знаємо»
   не має читатись як «не дзвонив».
2. **Вік рахується від `ASOF_SQL` = `MIN(last_success_at)` по `syncKommo` і
   `syncDealActivity`, а не від `now()`.** Годинник іде завжди, дані — ні: за аварії №2
   (синк стояв 15 год 13 хв) список роздувся б на **17 угод** застою, якого ніхто не
   бачив. У звичайний день Δ = **0** (заміряно тричі, зокрема при анкері віком 162 хв).
   `COALESCE(...,now())` і `LEAST(...,now())` обовʼязкові: без першого джоба, що жодного
   разу не відпрацювала, дала б `NULL` → предикат `NULL` → **порожній список**.
3. **Розмова з Ringostat гасить угоду ЛИШЕ коли у клієнта рівно ОДНА відкрита угода.**
   Ringostat знає клієнта, не угоду. Кілька відкритих — привʼязка неоднозначна, угода
   лишається в списку з позначкою ⚠️. Заміряно: гасить **38**, позначку дістають **16**.

**Наслідок для екрана:** «остання розмова» бере свіжіше з двох джерел (нотатка Kommo /
Ringostat) — саме тут були «99 днів» замість 56: у **647 із 2 304** активних FC-угод
розмова в Ringostat свіжіша за `deals.last_call_at`. Джерело підписане (🎧).

🔴 **`GREATEST` НЕ ЗНИЖУЄ АНКЕР НІКОЛИ — тому поріг вимагав ПЕРЕРАХУНКУ, а не лише
правки правила.** Уже записані `last_activity_at` набиті недодзвонами; інкремент їх не
виправив би ніколи. `recomputeActivity()` (щодня 04:40) бере нотатки **угод І їхніх
контактів** по id і **присвоює** анкер, включно зі зниженням. Він замінив ДВА монотонні
проходи (`healDealActivity` 04:40 + `healContactActivity` 05:40) — і обʼєднати їх було
**умовою коректності, а не оптимізацією**: точний перерахунок лише за нотатками угоди
стер би активність, що прийшла з контакту (там живе телефонія). Вартість та сама, ~47
запитів на добу.
⚠️ Інкремент `applyNotes` лишається **монотонним**: знижувати анкер за ВІКНОМ нотаток
означало б скидати його щоразу, коли в порцію не потрапила остання нотатка.

📊 **ЗАМІРЯНО ПІСЛЯ ВИКАТУ (11.08.2026) — і прогноз розійшовся на 12 угод. Причина
записана, бо вона неочевидна.** Прогноз казав 147 → 244 → 206; вийшло 147 → **232** →
**192**. Порогова частина дала **+85**, а не +97.

Річ у тім, що `recomputeActivity` робить ДВІ речі одночасно, а замір бачив лише одну.
По активних FC-угодах анкер змінився у **369**:
* **262 назад** — спрацював поріг 10 c (те, що й міряли), ще **5** у NULL (уся
  «активність» угоди складалась із самих недодзвонів);
* **85 УПЕРЕД** + **17 із NULL у дату** — точний перерахунок побачив нотатки, яких
  інкремент не бачив ніколи (медіана зсуву 1 день, **макс 340**). Це та сама діра, що
  описана у `healDealActivity`: інкремент іде лише вперед від вотермарка, тож пропущене
  вікно не повертається саме.

Ці **102 «посвіжілі»** угоди виходять зі списку й гасять частину порогових +97. Прогноз
міряв поріг проти бази, ЯКОЇ В ПРОДІ НЕ ІСНУВАЛО (анкери, перераховані з нуля без
порогу); реальна база — накопичені `GREATEST`-анкери, а вони подекуди відставали.
**Урок: замір «що дасть нове правило» треба брати від ТОГО стану даних, який у проді,
а не від ідеального.**

Решта заміряного: `sumRisk` 93 434 → **135 019**, `over90` **0 → 10** (недодзвони ховали
й найдавніші), менеджерів 25 → 27, легасі-список 534 → **638**. Ringostat погасив **40**
угод, позначку «розмова є, угода не одна» дістали **16**, «розмова лише в Ringostat» —
**33**. `last_call_at` змінився у 433 угод (336 назад, 29 у NULL).

🧾 **Замір, зроблений ДО коду (10.08.2026), і його головний урок:** перша редакція
скрипта дала «+0» — і це був не результат, а **підпис непрацюючого фільтра**: предикат
читав `n.durationSec`, тоді як у кеші лежало `n.dur`. Тому в скрипті тепер стоїть
контроль «анкер зсунувся у N угод; 0 → зупинка». Той самий клас, що «успіх за 0 мс».



### 🎯 МОДЕЛЬ ФІНАЛІЗОВАНО (рішення власника 14.08.2026): ТРИ СИГНАЛИ, ОДИН ПОРІГ, СКОУП ДО РАХУНКУ

**Активність = чат + дзвінок (≥10 c) + ЗМІНА ЕТАПУ.** Задачі як сигнал прибрано
повністю; синк задач Kommo (`#76`, `df60149`) — на полицю, закрито `git revert`.

🔴 **«НА ПОЛИЦЮ» ЗРОБЛЕНО РЕВЕРТОМ, А НЕ ОБІЦЯНКОЮ.** `df60149` був HEAD-ом гілки
розробки: лишити його там означало б, що наступний викат мовчки винесе синк задач
разом із чим завгодно іншим. Намір не є механізмом.

| що | було | стало |
|---|---|---|
| скоуп | усі відкриті FC-стадії, включно з «Авто працює» | **до «Виставлення рахунку» (`100274340`) ВКЛЮЧНО**, `SCOPE_STATUSES` — 15 статусів |
| поріг | 7 дн. (гроші) / 21 дн. (рання) | **ЄДИНИЙ**, дефолт `STUCK_MIN_DAYS = 21` |
| сигнали | нотатка людини · розмова Ringostat | **+ рух етапу** (`deal_stage_events`) |
| анкер `asOf` | `syncKommo`, `syncDealActivity` | **+ `syncStageEvents`** |

🔴 **СКОУП — СПИСКОМ СТАТУСІВ, І ІНАКШЕ НЕ МОЖНА.** `funnel_stage` цієї межі не
виражає: `100274340` («Виставлення рахунку», ДО відправки) і `69716300` («Авто
працює») обидва мапляться в `approved`. Спроба описати скоуп стадією затягла б у
список усе, що вже їде. Гейт `#72f` звіряє перелік із `seedKommoMapping.sql` — саме
щоб сусідній `funnel_stage <> 'paid'` не гасив помилку в переліку мовчки.
· «Відкладений запит» (`69716252`/`69500840`) — **у скоупі** (рішення власника).
· У старій воронці `155304` стадії «рахунок ДО відправки» немає — там межа проходить
по «Контроль перед завантаженням». Це не пропуск.

🟢 **ПОРІГ ОБРАНО: 21 (рішення власника 17.08.2026). Живий список — 140 угод.**
14 було стартовим значенням для заміру, а не рішенням: замір Фази 1 дав «без задач
≈383 угоди», але його робили **БЕЗ сигналу «рух етапу»**, який список СКОРОЧУЄ.
Підганяти поріг під 383 означало б пересушити список і сховати реально застряглі.
Тому число знімали проти ЖИВОГО списку (14 / 21 / 30) **до мержу** — і зняли.

⚠️ **Механізм не змінився: поріг лишається параметром.** `?minDays=` живий, обидва
роути беруть дефолт із ядра, підпис на екрані бере число з відповіді сервера. Ні ядро,
ні екран свого числа не тримають (`#72g`) — змінити рішення = змінити ОДИН рядок
`core/stuckRule.STUCK_MIN_DAYS`.

📐 **Заміряно в Neon SQL Editor руками власника** (контейнер асистента до БД не
дістає: `ai_readonly` — NOLOGIN без пароля, порт 5432 із контейнера закритий). SQL
знято з прод-шляху перехопленням `pool.query`, а не переписано — див. `tools/measureStuck.ts`.

🔴 **ФІКСТУРИ ГЕЙТІВ ВІДЛІЧУЮТЬСЯ ВІД ПОРОГА, А НЕ ВІД ЧИСЛА.** `stuckTalk.test.ts`
тримав вік угод як `14 * 24 + 3` — виглядало бездоганно, поки дефолт дорівнював 14, і
розсипалось того ж дня, коли обрали 21 (доведено: `#73b` і `#73c` червоніють). Тепер
там `T = MIN_DAYS * 24`, тож наступна зміна порога **не потребує правки тестів**.

🔴 **ГОДИННИК І «ЧИ ВЕЛИ УГОДУ» — З ОДНОГО ПЕРЕЛІКУ** (`stuckSignals`). Це два
формулювання одного питання «що з угодою робили»; поки вони писались окремо,
дописати сигнал в один і забути в другому було справою рядка — і екран стверджував
би, що угоду не вели, тим самим кодом, яким щойно порахував по ній рух. Той самий
клас, що чипи «новий/постійний». Тримає `#72h`.

⚠️ **РУХ ЕТАПУ БЕЗ ФІЛЬТРА «ХТО» — СВІДОМО, і ось точна ціна.** `deal_stage_events`
актора не зберігає, а Kommo-подію ми розбираємо в `KommoEvent`, де `created_by` не
оголошено взагалі — **поле губиться на РОЗБОРІ, не на вставці**. Отже переїзд,
зроблений Salesbot-ом, зараз гасить тривогу так само, як людський. Фільтр бота —
**окремий прохід** (колонка + правка мапінгу + бекфіл історії), коли буде канал.

⚠️ **`syncStageEvents` В АНКЕРІ — НЕ ФОРМАЛЬНІСТЬ.** Щойно рух етапу гасить тривогу,
його синк живить список нарівні з рештою. Відстав би він один — і угода, яку менеджер
учора зрушив, висіла б як «без руху 14 днів», а підпис «дані станом на» бадьоро казав
би, що все свіже. Доводиться ПОВЕДІНКОЮ (`#74d`): застарілий `syncStageEvents` мусить
заморозити список так само, як застарілий `syncDealActivity`.

Гейти: `#72`(склад) · `#72b`(⊆ кому пишемо дзвінок) · `#72c`(вік від анкера) ·
`#72d`(правило не дублюється) · `#72e`(рух гасить, але не знижує годинник) ·
`#72f`(скоуп) · `#72g`(поріг параметром, включно з підписом екрана) · `#72h`(один
перелік сигналів) · `#73*`(10 c, анкер, Ringostat) · `#74*`(поріг мовчання, `#74d`).

---

Активні угоди повного циклу БЕЗ реальної людської активності за поріг. «Активність» = `deals.last_activity_at` (нотатки з `created_by <> 0`) — **НЕ `updated_at`** (його бампить Salesbot). Пороги: грошові стадії ≥ minDays(7); рання «Взято в роботу» ≥ 3×. Вікно створення 180 днів.

## Звіт КВП — `KvpReportSection`, тільки `admin`

- **Динаміка (\*) ЗАВЖДИ до аналогічного періоду минулого місяця.**
- **Тижні місяця = фіксовані 7-денні блоки від 1-го**: `[1-7],[8-14],[15-21],[22-28],[29-кінець]`.
- **Цільові ліди** = пайплайн `8921932` створені в періоді; **не цільові** = Кваліфікація `8921928` статус `143`.
- **Декомпозиція плану**: треба авто = план ÷ ср.чек; треба лідів = авто ÷ конверсія.
- Тендерний відділ ЗАКРИТО.

## Налаштування

Таблиця `app_settings` (JSON), admin-only API `/api/settings`. Керування користувачами: `/api/settings/users` (provision з CRM, reset пароля, тімлід-роль).

## Брендинг

Лого UTS = `frontend/public/favicon.svg` + `components/Logo.tsx` (червоний `#c8102e`). Заголовок вкладки «UTS Dashboard». Папку `frontend` **НЕ перейменовувати** (на неї завʼязаний деплой).

## Оптимізація витрат

Кожне повідомлення ре-обробляє весь діалог. Тому: **нова сесія під кожен блок робіт**; групувати правки в один меседж; менше скріншотів де досить тексту; тримати цей файл актуальним.
