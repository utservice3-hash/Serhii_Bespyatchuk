/**
 * Некомерційні команди — ті, кого продажні екрани (Звіт, формування планів) не показують як продажну команду:
 * 11 = лідогенерація (Ковтонюк), 12 = фінанси. Дзеркало бекендового `metrics.NON_COMMERCIAL_TEAM_IDS` —
 * фронт не може імпортувати бекенд, тож список один тут. Раніше два екрани тримали власні копії `[11, 12]`.
 */
export const NON_COMMERCIAL_TEAM_IDS: Set<number> = new Set([11, 12]);
