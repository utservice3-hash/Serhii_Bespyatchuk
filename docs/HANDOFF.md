# HANDOFF — точка входу для продовження роботи (UTS Dashboard)

> **Якщо ти нова сесія Claude / новий розробник — почни звідси.** За 5 хв матимеш повний контекст і зможеш продовжити.

## 0. Порядок читання
1. **Цей файл** — де що лежить, поточний стан, черга.
2. **`CLAUDE.md`** (корінь) — усі **бізнес-правила** й деталі (Claude Code читає його автоматично). Головна «памʼять проєкту».
3. **`docs/BACKUP_RECOVERY.md`** — бекапи й відновлення даних.
4. **`docs/INFRASTRUCTURE.md`** — готові рішення (Metabase/моніторинг), дорожня карта, Telegram-пропозиції.

---

## 1. Що це і ціль
Аналітичний дашборд відділу продажів логістики. Джерело істини — **Kommo CRM**, синк кожні 5 хв. Ціль: ясна картина воронки «Перевезення повний цикл», план/факт по менеджерах/командах, задачі, дебіторка, лояльність — усе автоматично з CRM, без ручного Excel.

## 2. Репозиторій і гілки
- **Дев:** `claude/dashboard-development-54e8ng` — тут розробка.
- **Прод:** `claude/friendly-galileo-8pijhl` — це тягне продакшн.
- **Правило:** розробляй на дев → ff-merge у прод → пуш обох. Тримати в синхроні.

## 3. Деплой (продакшн)
Сервер **evraziat** (shared, CloudLinux). Керується через **relay-PHP**:
`POST https://dashboard.uts.ua/relay-d7bb7c59.php`, заголовок `X-Relay-Token: <токен>`, форма `cmd=<shell>`. Токен — у `CLAUDE.md` (⚠️ **світився — треба ротувати**). Cwd: `/home/evraziat/uts.ua/dashboard`. Завжди префікс `export PATH=/usr/local/node26/bin:$PATH`.

**Цикл деплою:**
```
git pull origin claude/friendly-galileo-8pijhl
cd backend && npm install && npm run build && npm run migrate   # migrate лише при зміні schema.sql
cd ../frontend && npm run build && cp -r dist/* ../
pkill -f 'node.*dashboard/backend/dist/index'                   # рестарт лише при зміні backend; exit 15 = норм
curl https://dashboard.uts.ua/api/health                        # {"ok":true}
```
Бекенд авто-респавниться супервізором після pkill.

## 4. Стек і структура
- **Frontend:** React 19 + Vite + recharts. Контейнер `frontend/src/pages/Dashboard.tsx` (весь стейт/ефекти), секції в `pages/dashboard/sections/*`, спільне — `pages/dashboard/{format.ts, widgets.tsx, …}`, API-клієнт `src/api.ts`, нав — `components/Layout.tsx`.
- **Backend:** Node/Express + `pg`, `node-cron`, `grammy` (ТГ). Основна аналітика — `backend/src/routes/dashboard.ts`. Джоби — `backend/src/jobs/*`.

## 5. База даних
- **Neon** (managed Postgres, `…aws.neon.tech`, база `neondb`). `DATABASE_URL` — у `backend/.env`.
- Структура — `backend/src/db/schema.sql`. Мапінг етапів Kommo — `backend/src/db/seedKommoMapping.sql`.
- Міграція: `npm run migrate` (schema) / `npm run migrate:mapping` (+ мапінг).
- Діагностика проду: base64-mjs через relay, `import { pool } from './dist/db/pool.js'`.

## 6. Джоби (`backend/src/jobs`, cron у `index.ts`)
syncKommo (5хв) · syncStageEvents (10хв) · syncTransfers (10хв) · syncReceivables (30хв) · syncNews (08:00) · evaluateKpiTasks (07:00) · snapshotCarryover (1-ше) · реконсиляція (04:00) · **backupDb (03:00)**.

## 7. Бізнес-логіка
**Уся** — у `CLAUDE.md` (гроші, воронка, мінус-угоди, передані заявки, знімки оплат, РПК/РНК, задачі, план/факт). Не дублюю — читай там перед будь-якою правкою метрик.

---

## 8. ПОТОЧНИЙ СТАН (оновлювати!)

### 🌙 Нічна сесія 08.07.2026 (у проді)
- **Реклама «прийняв»** = повний цикл × «Источник клиента» ∈ `app_settings.adSources` (ручний метод КВП; звірено: Михальчевська/тиждень=80, Гофман=14). Вирівняно скрізь (KPI, Огляд, Конверсія, Звіт). Google-utm НЕ додаємо (перекриття).
- **Лідоген «прийняв/передані»** = Google-аркуш «Реєстр» (`leadgen_registry`, `syncLeadgenRegistry` 30хв). Замінило завищений `lead_transfer_events`.
- **Per-показникові ✅/❌** у звіті й задачнику.
- **Звіт КВП → кокпіт**: hero-KPI з темпом/прогнозом, алерти, матриця План/Факт з РЕДАГОВАНИМИ цілями (`kvp_plans`, `/dashboard/kvp-plan`), якість виручки, реклама-ефективність (CPL/CPA/ROMI), лідерборд, декомпозиція. **Очікувані оплати** додано (pendingPayments; мінус-угоди віднімаються авто).
- **Вкладка «Регламенти та документи»** (папки/файли, drag&drop, категорії). Додано готові доки: «Регламенти → Регламент тімліда (щоденний ритуал)», «Навчання → Як працювати з Дашбордом».
- **Ван-ту-ван** (`one_on_ones`, `/api/one-on-ones`, розділ `oneonone`): тімлід — своя команда, admin — усі; менеджери НЕ бачать. Анкета (покращені питання, оцінки 1-10+текст), статистика оцінок, щомісячна задача тімлідам (`oneOnOneReminders`, 7 створено).
- **Мої звіти**: додано поле запиту до АІ (будує віджет через ai-work).
- **Kommo**: браузерний User-Agent + низьконавантажувальний режим (800мс, полінг 30хв, реконсиляція 10д, event-феди 3год без стартових сплесків) + **файловий вимикач `KOMMO_PAUSED`** (touch/rm без деплою). ⚠️ **Наразі синк НА ПАУЗІ** (файл `KOMMO_PAUSED` існує); дані оновлено ручним синком 08.07 21:02.
- **Фікс деплою фронту**: `cp -r dist/* ../` клав бандл у вкладену `assets/assets/` → старий JS. Тепер `rm -rf assets && cp -r frontend/dist/assets assets && cp frontend/dist/index.html index.html`. **ЦЕ причина «Огляд помилка» / папка не створюється / міста не вантажаться / задачі не видаляються — усі бекенди справні, був зламаний бандл.**

**🔴 ПРИЧИНА БАНІВ KOMMO (з листа підтримки):** акаунт банять НЕ ми — інша інтеграція (найпевніше лідоген-бот `app.py`) довбить `GET /leads/{id}?with=contacts` (184×) + `PATCH /leads` (154×) по колу (той самий баг, що дублі в «Реєстрі»). Наш дашборд у лозі = 7 батч-запитів. **Ранок: полагодити цикл у `app.py`, тоді ввімкнути наш синк назавжди (rm KOMMO_PAUSED).**

**⏳ ЧЕРГА (не зроблено — на ранок):**
1. **Реактивація**: перейменувати «Постійні клієнти», додати кнопку «Реактивація» (аналог RepeatPlanGrid): тімлід додає sleeping/lost клієнтів менеджеру; обовʼязкові поля план/факт + 1-й контакт→результат, 2-й контакт→результат; клієнти реактивації видно у звіті менеджера й у тімліда.
2. **Дебіторка**: файл виглядає «криво» — поправити візуал; у деталізації рахунків — календар + коментар до КОЖНОГО рахунку; прострочений дедлайн оплати → задача менеджеру «отримати оплату».
3. **Поліш усього дашборду** до рівня Звіту КВП: візуал, баги.

### ✅ Зроблено й у проді
- **Вкладка «Регламенти та документи»** (`documents`, `DocumentsSection.tsx`, роут `/api/documents`): дерево папок+файли, пошук, drag&drop+прогрес, категорії-теги (Регламент/Шаблон/Інструкція/Інше у `doc_files.category`), автор/дата. Файли на диску в `backend/documents` (поза публічним static). Керує admin (КВП), решта — перегляд/скачування. Таблиці `doc_folders`/`doc_files`.
- **«Прийнято реклами» = повний цикл × «Источник клиента» ∈ `app_settings.adSources`** (ручний метод КВП, звірено з CRM: Михальчевська/тиждень=80-81, Гофман=14). Дефолт adSources = 6 сайтових (uts.ua/yalogist × сайт/дзвінок/callback), admin-редаговані. Замінило хибне `lead_channel='ad'` (реатрибуція за історією → Возович «6 реклам» без жодної; Гофман показувався 0 замість 14 бо все кваліфікує в повний цикл). **Вирівняно скрізь**: `evaluateKpiTasks`, `/overview` (adConversion+тренд), `/conversion`, `/report` скоркарта. Google-utm НЕ додаємо (перевірено: ~92% уже покриті сайтом, непокриті — реактивація/ручні).
- **«Передані заявки (лідоген)» = Google-аркуш «Реєстр» лідоген-бота** (`leadgen_registry`, `syncLeadgenRegistry`, кожні 30хв+старт, TRUNCATE+insert; `LEADGEN_REGISTRY_SHEET_URL`). Кожен рядок = вхід ліда в «Нова заявка від лідогенератора» 69716164. Рахунок = DISTINCT `lead_id` за період, атрибуція за поточним власником у CRM (join deals по kommo_id). Замінило `lead_transfer_events` (рахував зміни відповідального → завищення 5-9×: червень 1311 vs правда ~260). KPI `leadgen_count` теж переведено.
- **Per-показникові ✅/❌/⏳** у KPI-задачах — і в звіті, і в задачнику (не лише кольором); загальна задача не виконана, поки будь-який показник не досягнуто.
- **Чистка фільтрів дат**: прибрано дубль пресетів у `DateRangeFilter` (тепер лише довільний діапазон); швидкі періоди — тільки чіпи `QuickPeriods`.
- **«Відправлено авто»**: рахунок+«Авто працює»=один етап (за проханням КВП) — коректно. ⏳ Задачник `dispatch_count` поки вужчий (лише вхід у «Авто працює»); вирівняти за потреби.

Рефакторинг секцій · надійність синку (guard/timeout/watermark/реконсиляція) · Kyiv-дати скрізь · мінус-угоди (відʼємний price) · **повна мапа етапів + єдина 4-етапна тижнева воронка** (Взято→Авто працює→Рахунок→Оплата) · клікабельні картки Огляду (нові/постійні/створені) · звід усіх боржників · усі постійні клієнти · конверсії реклама(same-deal)/лідоген(передані→успішно) · розкриття команд з планами · **задачі тімлід↔менеджер + «Мої задачі» у звіті** · клік по менеджеру → План/Факт+задачі · **денна продуктивність** (картка+тренд) · роль «Керівник відділу продажу» · приховані адмін-розділи від менеджерів · форма зворотного звʼязку · фікс месенджера · **фільтри переживають оновлення** (localStorage) · **бекапи (03:00) + docs** · плани липня залиті · **Калькулятор ставок (порт lardiweb)** · МГЕР-готівка в дебіторці (приход) · останній-дотик реатрибуція каналів.
- **«Робота з АІ» — справжній чат з Claude** (`backend/src/ai/respond.ts`, модель `claude-opus-4-8`, adaptive thinking): системний промпт із бізнес-правилами + інструмент `query_db` (read-only SQL, `ai/readQuery.ts`). **Вкладення зображень** (📎/Ctrl+V, розпізнає скріни). Ключ `ANTHROPIC_API_KEY` — ЛИШЕ у прод `backend/.env` (не в git). Доступ: admin + `ranutservice@gmail.com`.
- **«Мої звіти»** (розділ `reports`, `ReportsSection.tsx`, роут `/api/reports`): АІ інструментами `create/list/update/delete_widget` будує віджети (table/bar/line/kpi) у дашборд; `ai_widgets` (SQL+config+visibility admin/leads/all), дані scoped по ролі, живий SQL щоразу. Видалення — admin.
- **Kommo rate-limit**: усі запити через ЄДИНУ чергу-тротлер (`kommo/client.ts`, `MIN_REQUEST_GAP_MS=350` ≈ 2.86 req/s глобально, один процес) + **circuit breaker** (5×403 → пауза 15хв, успіх скидає; стан у `/api/health.kommoCircuit`). Стартові event-синки рознесені +2/+4/+6хв. `kommoGet` — санкціонований throttled-GET для разових джобів (reactivateLeads переведено).

### 📋 Черга (узгоджено)
✅ ~~Застряглі угоди~~ — зроблено (Salesbot-proof, `last_activity_at`, згорнута картка, per-manager/team).
✅ ~~Проєкція на місяць~~ — зроблено (Огляд, «за темпом вийде ₴X»).
✅ ~~Опис-підказки (ⓘ) до показників~~ — зроблено (Огляд+Звіт).
✅ ~~Звіт КВП~~ — зроблено (місяць/тиждень, аналог. період, тижні мин.міс., реклбюджет, декомпозиція).

**⏸️ Відкладений запит — ТЗ від КВП (чекає рішень/доступів, реалізація пізніше):**
- **Телефонія** (пропущені/час відповіді/вхідні/ефективність) — ❌ блок: немає джерела (яка система дзвінків? Kommo/Binotel/Ringostat/Phonet — потрібен API).
- **Конверсія-редизайн**: по етапах воронки + головний KPI по ВІДПРАВЛЕНИХ АВТО (не оплатах). Дані є (`deal_stage_events`), треба підтвердити етапи/визначення «відправлене авто».
- **«Динаміка»-редизайн**: єдина методика, виключити перенесені, однакова к-сть роб.днів.
- **«Успішно закриті»-редизайн**: репрезентативно серед місяця (темп/прогноз).
- **РНК-статистика** (звернення/відповіді/озвучені ціни/конверсія між етапами) — дані частково є.
- **Dashboard керівника** (1 екран < 1 хв) — залежить від телефонії+ефективності.
- **Міграція показників з Looker** — потрібен перелік/формули/доступ.
- **Донарахування до плану** — потрібне визначення.
- **Історичні дані попередніх місяців** — глибина backfill? (Kommo events API капить).
- **ФОП-інтеграція** — зовнішня система, після фінального тесту.
- **Метадані KPI**: формула+джерело+дата оновлення (опис вже є в ⓘ).
- **P2**: прогнозування, фінаналітика, окремі модулі.
- ✅ **Зроблено з ТЗ**: §7 «Контроль якості даних» (розділ «Якість даних»).

**🔴 АКТИВНЕ (станом на 08.07.2026):**
- **Kommo API заблокований** (WAF 403 з ~10:19 UTC, повторно після ранкового бану 07:49). Ми ДОКАЗОВО під лімітом (черга ≤3/с, circuit breaker на паузі) — блок на боці Kommo. **Дія власника: відповісти в підтримку Kommo (Микита) що фікс впроваджено → вони знімуть блок.** Після розблокування синк відновиться САМ (breaker пробує→успіх). Дані не губляться (watermark+реконсиляція 04:00). Carriers-backfill (`syncCarriers --loop`) чекає розбану.
- **«Ван ту ван»** (1-on-1) — чекає відповідей власника на 3 питання (приватність/ритм/анкета); проєкт узгоджено, реалізація після.
- **АІ-чат рівень 2** (за бажанням): збережені звіти/розсилки, авто-резюме тижня, АІ-коментарі до просідань. Ключ уже є.

**Лишилось (стара черга):**
1. **Декомпозиція РПК/РНК** — розбити декомпозицію й per-team рядки Звіту КВП на групи РПК/РНК (треба: групування команд + визначення «відправлене авто» = успішні угоди чи етап «Авто працює»).
2. **Експорт** звіту (Excel/PDF).
3. **Мобільна версія** (адаптив).
4. **Telegram-сповіщення** — тригери в `INFRASTRUCTURE.md` §5 (обрати).
5. Звіт КВП: **ручні фінрядки** (CASH FLOW фінансиста тощо) — за потреби.
6. Безпека: **ротація relay-токена**. Опційно: UptimeRobot, Metabase.
7. Звіт (Фаза C, з Excel): Логістика (перевізники), фінсуми по типах клієнтів, очікування наст.міс, цілі на день, чекліст заповнення.

---

## 9. Граблі (важливо)
- Деплой: `npm install` **без** `--omit=dev` (потрібен tsc для build), інакше build падає.
- `pkill` → `exit 15` (SIGTERM) — це **норма**, бекенд респавниться.
- Bash-tool іноді падає по таймауту на важких `git pull && build` — команда на сервері все одно доходить, перевіряй результат окремим запитом.
- Публічний `/api/health` через relay буває лагає під час рестарту — перевіряй `http://127.0.0.1:4000/api/health` локально на сервері.
- **Дати** — завжди `(col AT TIME ZONE 'Europe/Kyiv')::date BETWEEN $from AND $to` (обидва кінці включно).
- `fetchAllContacts/fetchAllCompanies` — **не повертати** (OOM-вбивали синк); тягнути по id вікна.

## 10. Відновлення даних
Див. `docs/BACKUP_RECOVERY.md`. Коротко: Neon PITR (основне) + нічний CSV-дамп у `dashboard/backups/` (14 днів). Багато даних відновлюється й синком з Kommo.

## 11. Доступи (значення НЕ тут — тільки де шукати)
- Neon: console.neon.tech · relay-токен: `CLAUDE.md` (ротувати) · `.env`: `backend/.env` (DATABASE_URL, KOMMO_API_TOKEN, JWT_SECRET) · Kommo API-токен: `.env`.
