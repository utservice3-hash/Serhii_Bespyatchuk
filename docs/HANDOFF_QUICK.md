# HANDOFF_QUICK — точка входу за 30 секунд

> Нова сесія (Claude Code чи зовнішній рецензент) — почни звідси.
> Оновлювати в КІНЦІ кожної робочої сесії (розділ «Де ми зараз»).

---

## Що це за проєкт
Аналітичний дашборд відділу продажів логістики UTS. Джерело — Kommo CRM.
Пройдено повний **reset ядра**: усі метрики переписані на чесні, звірені з CRM, з нічною
самоперевіркою. Зараз будуємо звіти на цьому фундаменті.

## Файли-джерела правди (читати в цьому порядку)
1. **`CLAUDE.md`** (корінь) — бізнес-правила, деплой, граблі.
   ⚠️ З 31.08.2026 корінь тримає лише універсальне; решта правил — у `.claude/rules/*.md` за зонами,
   перелік адрес — розділ «🗺 КАРТА ЗОН». Зонне правило заходить у контекст на ЧИТАННІ файла під його глобом.
2. **`docs/HANDOFF.md`** — детальний стан + інфраструктура.
3. **`docs/MASTER_PLAN.md`** — історія reset'у, ФАКТИ Ф1–Ф12, правила роботи.
4. **`docs/METRICS_GLOSSARY.md`** — одне визначення на метрику (нормативний).
5. **`docs/CONVERSION_RULES.md`** — визначення всіх конверсій + цільові %.
6. **`docs/CRM_SCHEMA.md`** — воронки, стадії, поля Kommo.
7. **`docs/BACKLOG_FULL.md`** — повна черга задач.
8. **`docs/KVP_REPORT_SPEC.md`** — макет і вимоги до звітів.

## Правила роботи (незмінні)
1. **Один крок за сесію**, СТОП після кожного, показати результат.
2. **Заборонені перевірки «A = B ✓»** (тавтологія). Тільки зовнішня звірка: наше / лист / Kommo.
3. **Немає даних у CRM = блокер**, а не привід підмінити метрику.
4. **Не вигадувати бізнес-правил** — немає в доках → питання до власника.
5. Перед деплоєм у показ — таблиця «було→стало», попередити команду про зміну цифр.
6. Стоп-критерій майже завжди: **звірка 4 рівні зелена + еталон РПК-Яцик серпень 2025 = 242 / 680 655**
   (наше 242 / 677 316 — угоди точно, виручка Δ 0.5% через реоупени; це норма).

## Інструменти самоперевірки (вже в проді)
- Нічна звірка 4 рівні (цілісність / синк / дашборд / свіжість) + auto-heal + вартовий покинутих стадій.
- Node-режим хостингу (adm.tools), деплой через relay на `test.uts.ua` (тимчасово; прибрати в кінці — техборг).

---

## 🟢 Де ми зараз (оновлювати щосесії)

**Дата останнього оновлення:** 08.09.2026

> ⚠️ **ЦЕЙ РОЗДІЛ ПРОТУХАВ НА МІСЯЦЬ** (стояв 04.08, код був на 03.09). Нова сесія
> вірила точці входу й пішла б переробляти зроблене. Якщо читаєш це і дата вище
> старша за останній коміт більш ніж на тиждень — **спершу звір `git log`, а вже
> потім вір тексту нижче.**

### ✅ ВИКОЧЕНО 08.09.2026 — виписка: комісія Привату більше не затирає платіж (прод `88a1e41`)

**Привід — Юля, 08.09:** «менеджери кажуть, виписка не всі платежі показує». Приклад: ГУТА-ТРАНС
41 666,67 ₴ з ЮТС 28.08. Платіж був у банку **і в базі** — під його ключем лежала комісія 3 грн.

**Механізм, заміряний напряму в API Привату:** на кожен вихідний платіж банк віддає ДВА записи —
платіж і комісію — з ОДНИМ `REF` і різними `ID`. Ключем був `REF`, тож upsert лишав того, хто
прийшов другим; комісія ще й позначена `is_bank_fee` і схована з екрана — платіж зникав повністю.
Синк «зелений»: кожен upsert успішний. Вхідні не зачеплені: комісій там немає.

📐 **Масштаб до фіксу (30 днів):** 400 пар, 370 платежів затерто — ЮТС 305, Автомув 93. **Після
перезаведення (`tools/rekeyPrivat.js --write`, вся історія від 27.05):** ЮТС 3 359 → 4 357 рядків
(+998), Автомув +335, ФОП +2, USD +1, EUR +2. Приймання: 28.08 ЮТС — **29 вихідних понад 3 грн**,
як в API, замість 11; ГУТА-ТРАНС і Пічкар (Автомув, 03.09) на виписці; рядків за `REF` — 0.

- Ключ — `privatTxKey` (`bankSources/privat.ts`): `ID`, запасний `REF`, далі дата-сума-рахунок.
- `syncBank` додано в `MONITORED_JOBS` — пропущений рахунок більше не мовчить.
- Гейти `#367` · `#367b` 🪞 (обидва доведені саботажем) · `#367c` живий (нуль рядків за REF).
- ⚠️ Зіставлення платежів і дебіторка **не зачеплені**: звʼязків на `bank_transactions.id` у схемі
  нема (перевірено), ключі вхідних змінились, але рядки ті самі за змістом.

**Приймання на проді:** `test:prod` **892 із 892 виконано, 887 зелених, 5 червоних.** `#124`/`#125`/`#181` —
у реєстрі `expectedReds.ts`. **`#274e` і `#59c` — НОВІ й НЕ ПРО ЦЕЙ КОД:** обидва живі гейти
про стан менеджера, і обидва червоніють через те, що Назару (Шевчук, team 5) сьогодні
поставили «завершує», а план поточного місяця в нього лишився (`#274e` ще називає Матюніна
зі станом `null`). `#59c`: у `dismissed` активний менеджер ІЗ командою — правило винятків
цього випадку не знає. Це наслідок дії руками з попереднього викату (див. «Що лишилось
зробити руками ПІСЛЯ викату» нижче), і його треба закрити тому, хто веде стани: або план
Назара знімається, або правило вчиться про «завершує». Ланцюг спинився на `accept`, тож
`publishNews`/`acceptMatrix` **не виконались**; замок звільнено руками з цією причиною.

🔴 **Урок інструмента, знайдений дорогою (і виправлений паралельно іншою сесією в main):** крок
`test` будував базу без `.env`, тож будь-який файл, що тягне пул на імпорті, у базі гинув цілком,
а в дереві проходив — і викат спинявся критерієм ② без жодної причини в дифі. Тепер `.env` іде
в базовий worktree симлінком (рішення власника — не другий екземпляр доступів).

### 🟡 ГОТОВЕ ДО ВИКАТУ — гілка `feature/release-news-autopublish`, ЗЛИТА з `origin/main` 08.09

Пакет «звільнення + новини + дрібне», **влитий у `origin/main` ДВІЧІ за день** —
прод рухався під час роботи: `d640e36` → `e8d1707` (минулі місяці) → `23ca8dd`
(нічийні плани). Викочується КОМІТ ЗЛИТТЯ, не гілка окремо.

**🔴 ЧОМУ ЗЛИТТЯ, І ЧОМУ ДВІЧІ.** Гілка стояла на `d640e36`, а прод за час роботи
поїхав на два чужі проходи. Викат гілки як є **відкотив би їх**. Заразом двічі
вкусив борг 17 (зіткнення номерів): новини були `#357` → зайняте чужими «минулими
місяцями» → перейменував на `#359` → того ж дня чужий прохід зайняв `#359` («нічийні
плани») → **фінально `#361/#361b`**. `#358*` тримає звільнення. Замок чекауту тепер
у нас — поки тримаємо, прод більше не зрушить.

**🔴 ГОЛОВНЕ ЗНАННЯ ЦІЄЇ СЕСІЇ — ГЕЙТИ, ЯКІ НЕ БІГЛИ НІДЕ.** Локально скіпалось **47**
гейтів, серед них ГОЛОВНІ гейти двох проходів (`#274d`, `#358b`). Причина скіпу казала
«немає бінарів PostgreSQL» — бінарі були; кластер не піднімався через логін-шелл і
локаль. Полагодивши старт, одразу знайшли третю поломку — **`schema.sql` не будував
базу з нуля** (`ALTER TABLE news` стояв вище за `CREATE TABLE news`; на проді таблиця є,
тож міграція проходила). Заміряно до/після: **651 → 720** виконаних, 0 падінь.
⚠️ Пастка в лікуванні: `--locale=C` піднімає кластер, але вимикає `lower()` для кирилиці —
база починає відповідати інакше за прод. Локаль обирається пробою `C.UTF-8` → `en_US.UTF-8`.

**Звільнення (рішення власника 07.09: «звільнений вимикає вхід»).** Стан ставиться в
Налаштуваннях новою колонкою «СТАН»; досі механізм не мав ЖОДНОГО UI, тому
`manager_work_state` порожня. 🔴 Вхід — ВИВЕДЕНИЙ факт, а не збережений прапорець:
перша редакція дописувала `UPDATE users SET is_active = false`, і це прожило б
щонайбільше 30 хвилин — `provisionUsers` перезаписує колонку значенням із `managers`
щотіка (саме так зникла деактивація Шевчука 06.08). Тому `routes/auth.ts` читає стан при
КОЖНОМУ логіні. Зворотна половина виходить даром: зняли стан — вхід відкрився сам.

**❓ ВІДКРИТЕ ПИТАННЯ, ЗАКРИТЕ ВЛАСНИКОМ 08.09:** звільнення закриває **вхід**, але не
виганяє того, хто вже всередині (`requireAuth` робить лише `jwt.verify`, токен живе
12 год). Рішення власника — **«лишити як є, тільки не пускати»**. Це не регрес: давня
деактивація поводиться так само. Межа названа вголос у `core/managerState.ts`.

**⚠️~~`accept` БУДЕ ЧЕРВОНИМ на `#107c` і `#359`~~ — ВЖЕ НІ, ПРОТУХЛО ЗА ПІВ ДНЯ.**
Тут стояло: «червоний, бо 12 наявних сиріт у проді ще не бекфілнуто, рішення власника
08.09». **Рішення змінилось того ж дня**: бекфіл виконано (11 привʼязано, 12-й обнулено
за рішенням тімліда), сиріт **0**, і `#107c`/`#359` **зелені** — заміряно проти прода,
17 із 17. Тобто `accept` на цій парі більше не падає, і `publishNews`/`acceptMatrix`
мають виконатись самі. Подробиці — у розділі про нічийні плани нижче.

🔴 **Рядок лишено закресленим навмисно.** Два абзаци одного файла стверджували
протилежне, і перший читався б першим. Це рівно той клас, від якого береже попередження
вгорі цього ж розділу — **твердження пережило свою причину, і ніхто не звіряв**. Ціна
тут була б не теоретична: наступний чат готувався б до червоного приймання й списав би
на нього справжнє падіння. НЕ плутати з `#361` (новини) — це різні гейти.

**⏭ Що лишилось зробити руками ПІСЛЯ викату:** поставити Назару (`managers.id = 33`) стан
«звільнений» через колонку «Стан». Приймання назвати ДО дії: сума команди
«РПК - Яцика Дмитра» до і після — **одним запитом**, і вона не має змінитись
(`countsResult` завжди `true`: гроші лишаються в сумах команди й компанії).

### ✅ ВИКОЧЕНО 08.09.2026 — статистики: Фінанси/HR повернуто, «Ручні» прибрано (прод `1fc76f2`)

**Виправлення виконання не тієї вказівки.** 07.09 слова власника «оці ручні вкладки всі
поскривай» розвʼязали проти НЕ ТОГО розділу: він показував на вкладку **«Ручні»** (✍️) у
розділі «Статистики», а сховали два ВІДДІЛИ в «Статистиках (відділах)».

📐 Ціна, заміряна 08.09: названу ціль не зачепили (вкладка лишалась робочою), зате
сховали **26 метрик**, з яких лише **8** досяжні графіками сусіднього розділу — тобто
**18 не було видно ніде**, хоч новина й обіцяла «дані нікуди не зникли». Сама новина
(id 197) казала «з розділу „Статистики“ прибрано вкладки „Фінанси“ і „HR“»: не з того
розділу, і в названому вони лишались видимими.

🔴 **КОРІНЬ — ДВІ МАЙЖЕ ОДНАКОВІ НАЗВИ РОЗДІЛІВ** («Статистики» і «Статистики
(відділи)»; CLAUDE.md уже позначає їх кандидатами на злиття). Доти вказівку «оця
вкладка» треба розвʼязувати питанням **«у якому вона розділі»** — назви не розрізняють.
Це найцінніше з цього проходу, і воно повторюване.

Зроблено: `HIDDEN_DEPARTMENTS` порожній (константа лишилась — механізм робочий),
панель вкладок відсіює `manualForm`, запис `manual` у `CATS` ЦІЛИЙ (на нього спирається
`ManualForm` через `!`). ⚠️ Межа: це фронт, тож `POST /statistics/manual` живий для
адміна — вхід прибрано з ЕКРАНА, не з API. Заміряно: `stats_series` = 9 892 рядки,
ВСІ `source='sheet'`, ручних **нуль** — формою жодного разу не скористались.

🔻 `#355` **ЗНЯТО** (`RETIRED_GATES`): він справно стеріг виконання не тієї вказівки,
тому й не спіймав помилку. Замість нього `#363` (сервер нікого не ховає, `finance`/`hr`
ПОІМЕННО) і `#363b` (фронт не показує вхід, але запис цілий). Два, а не один: єдиний
зеленів би, поки виконана хоч одна половина — а помилка була саме в тому, що зробили
не ту половину. Обидва доведені саботажем, і кожен лишався зеленим під ЧУЖИМ саботажем.

📰 Новина **203** прямо виправляє попередню, а не мовчки її замінює. ⚠️ Тіло новини
**197 не редаговане** — стара неправда лишається в стрічці як є.

⚠️ **Це вже ТРЕТЄ зіткнення номерів за дві доби** (`#352`, `#359`, `#362`). Щоразу
перенумеровував той, хто ще не змерджений. Борг 17 живий.

### ✅ ВИКОЧЕНО 08.09.2026 — план клієнта більше не буває нічийним (прод `23ca8dd`)

**Дефект.** `POST /client-plan` писав `manager_id = NULL` кожному, хто зберігає план не
будучи менеджером **за скоуп-роллю** (тімлід, адмін, КВП). Такий план рахують «Клієнти»,
не бачить «Формування плану» (`AND manager_id IS NOT NULL`) і **не бачить сам автор** —
його список скоупиться по `rp.manager_id`. Заміряно 07.09: 10 із 22 затверджених
вересневих планів нічиї, 1 079 300 ₴ із 1 162 800 ₴.

🔴 **Це не «дві плитки розійшлись», а мовчазна втрата роботи.** Слід у базі однозначний:
тімлід зберіг «західсолод» **сім разів за 51 хвилину**, тричі поспіль те саме число.

🔴 **І не «баг тімліда»:** `auth.role` — це `ScopeRole`, а не `users.role`. Сироту записав
і користувач із `users.role='manager'` та `role_override='kvp'`. Тому в коді умова
«резолвимо завжди», а НЕ перелік ролей — перелік розійшовся б на першій новій ролі.

✅ **ПРИЙНЯТО ЖИВИМ ТРАФІКОМ, не тестом.** Прод перезапущено о 10:03:54; о 10:06–10:08
не-менеджер зберіг **4 плани, і всі 4 отримали власника** (Чукін Євген, 135). Остання
сирота — 09:16, тобто до рестарту.

Гейти `#359` · `#359b` · `#359c` · `#359d`, усі доведені саботажем. `#107b` **знято**
(запис у `RETIRED_GATES`) і замінено на `#360` — його предикат дрейфнув після `f44b824`
і гейт **стверджував неправду**; `#360` зелений там, де `#107b` був червоний.

✅ **БЕКФІЛ ВИКОНАНО 08.09.2026:** 11 рядків привʼязано до власників, 12-й
(`0990584600`, 880 000 ₴) **обнулено за рішенням тімліда** — він сказав, що цифра не
його, «може тест робили». Після запису **17 із 17 гейтів зелені**: `#107c` і `#359`
позеленіли. Отже `accept` більше не має падати на цій парі, і наступний викат зробить
`publishNews` та `acceptMatrix` САМ.

📐 Прогноз розійшовся з фактом (`308 800 / 32` проти `321 300 / 35`) — за час між
заміром і записом додали ще три плани. Приймання тримало **інваріант** «сиріт = 0», а
не число, і тому не почервоніло на живому знаменнику.

⏳ Стара версія цього абзацу казала, що бекфіл не робиться. Це було правдою пів дня.
Список пар для запису й відкату — `docs/BACKFILL_ORPHAN_PLANS_2026-09.md`; там же два
питання власнику: **880 000 ₴** на клієнті з 21 382 ₴ виручки та розширення доступу
тімлідам, яке бекфіл принесе з собою.
🔴 `repeat_client_plan_history` **не зберігає `manager_id`** — після `UPDATE` відкат
можливий ЛИШЕ за тим списком. Реконструкція за правилом перебирає.
⚠️ Люди можуть полагодити свої рядки самі: `SAVE_SQL` = `COALESCE(наявний, новий)`, тож
повторне збереження привʼязує план. Це сказано в новині.

### ✅ ВИКОЧЕНО 07.09.2026 — очікування з МИНУЛИХ місяців (прод `e8d1707`)

**Привід — Яцик, 07.09:** «пише, що очікування на цей місяць 18 450, а в CRM у мене
98 000». Його гіпотеза підтвердилась дослівно: `expectedMonthByScope` фільтрує планову
дату **за назвою місяця**, тож 37 його угод на **67 274 ₴ із серпневою датою** не
потрапляли ні в «цей», ні в «наступний» — і не показувались НІДЕ.

🔴 **Це календар, а не дефект розрахунку.** У серпні ті самі угоди показувались чесно;
о 00:00 першого вересня вони одночасно стали минулим місяцем. Без окремого числа воно
повторюється **кожного першого**.

📐 Заміряно 07.09 через самі функції екрана: відділ — **419 290 ₴ на 216 угодах** з
минулих місяців проти 660 404 ₴ «цього» і 29 162 ₴ «наступного». Яцик: 67 274 · 18 453 · 0.

🔴 **МЕЖА — ПОЧАТОК МІСЯЦЯ, А НЕ «СЬОГОДНІ», і це рішення власника.** Перша редакція
брала готову умову з плитки КВП (`expectSplit`, межа «сьогодні») — і це **мовчки
зрушило б ПРОГНОЗ**, ухвалений окремо 06.08: усі 6 вересневих угод Яцика датовані
01.09, тож його «цей місяць» став би **0** замість 18 453 ₴. Розвилку винесли власнику
до написання коду.

⚠️ **ТОМУ Й ПІДПИС ІНШИЙ: «з минулих міс», а не «прострочено».** На екрані КВП уже
живе `expected.overdue` з межею «сьогодні» — на Яцику це **85 727 ₴** проти наших
**67 274 ₴**. Два різні числа під одним словом на одному екрані — рівно та помилка,
яку ми ловили на двох «очікуємо» і двох «прогнозах».

Гейти `#357` (+ дзеркало, що стереже саме прогноз) · `#357b` · `#357c` · `#357d` живий.
**Усі чотири доведені саботажем**, контроль після відновлення зелений.

✅ **Викочено й прийнято по своїй частині.** `health.version == e8d1707`,
`buildStale=false`. Пʼять гейтів зелені проти ПРОД-бази.

🔴 **ЛАНЦЮГ СПИНИВСЯ НА `accept`, І ЦЕ НЕ ПРО ЦЮ ЗМІНУ.** `#124`/`#125`/`#181` — **вже в
реєстрі очікуваних червоних**; `#107b`/`#107c` — **поза реєстром** (порожня популяція
нового місяця + «Клієнти» 1 162 800 ₴ проти «Формування» 83 500 ₴). Новину за той викат
опубліковано вручну (id 197), бо до кроку `publishNews` ланцюг не дійшов.

### ✅ ВИКОЧЕНО 07.09.2026 — реактивація для тімліда + пакет «дрібне» (прод `d640e36`)

Два викати за день: `1a2d8e4` (тімлід і архів) і `d640e36` (новини, статистики, ланцюг).

**Що змінилось для людей.** Керівник команди бачить архів СВОЇХ клієнтів і може
архівувати з причиною; зʼявився розділ «Закриті задачі» (клієнт · причина · хто · коли);
причина закриття видима і в самій задачі. Новини про оновлення дашборда публікуються
САМІ після прийнятого викату, непрочитані підсвічуються лічильником у меню. З «Статистик»
прибрано вкладки «Фінанси» і «HR» — усі числа там були ручні (дані лишились).

**🔴 ТРИ РЕЧІ, ЯКІ ЛЕГКО ЗЛАМАТИ НАЗАД:**
1. **Клас причини закриття — пʼять значень, не два.** `legacy` («опрацьовано до реєстру»)
   і `auto` («клієнт повернувся») — РІЗНІ факти. Заміряно: 194 рядки з міткою `migrated:`
   поставив скрипт, але роботу зробили менеджери; спільний підпис «закрито системою»
   приписав би машині їхню працю. Тримають `#348`/`#348b`.
2. **Скоуп архіву й скоуп реєстру — РІЗНІ навмисно.** Архів ріжеться по відповідальному
   за КЛІЄНТА, реєстр — по команді ВИКОНАВЦЯ задачі. Зліпити означало б, що тімлід не
   побачить власної закритої задачі на переданому клієнті.
3. **Крок `publishNews` стоїть за `lockRelease`, і це не випадковість.** Раніше — новина
   вийшла б до приймання; пізніше (`acceptMatrix`) — провал матриці ховав би новину про
   справний реліз. Між `accept` і `lockRelease` не можна взагалі (`#250m`).

**⏳ ВІДКРИТЕ, І ПЕРШЕ, ЩО ВАРТО ВИРІШИТИ:**
- **9 вересневих планів на 1 075 300 ₴ із порожнім `manager_id`** — 93% суми не видно на
  «Формуванні планів» (там ростер `is_active AND team_id IS NOT NULL`). Записані 07.09
  шістьма людьми; кожен новий план на клієнті БЕЗ відповідального провалюється так само.
  Причина: розширення ростера 05.09 дозволило ставити план будь-якій компанії, а роут
  збереження пише `manager_id` без перевірки. Власник відклав окремою задачею.
- **Через це ланцюг спиняється на `accept` КОЖНОГО викату:** `#107b`/`#107c` червоні й не
  оголошені в `expectedReds.ts`. Наслідок — `publishNews` і `acceptMatrix` доводиться
  добігати руками. Внести їх у реєстр може лише той, чиє це рішення (`decidedBy`), тож
  вибір: полагодити планування або оголосити червоне свідомо.
- Решта пакета «дрібне»: C (трекер «не підключено»), B (кнопка ▶ у дзвінках картки),
  F (стан «завершує» + Назар). G і H — питання до власника, коду немає.

📐 **Заміряно 07.09 і варте памʼяті:** борг 18 («ланцюг спиняється після власного успіху»)
закрито — але НЕ тим лікуванням, що було в ньому записано. Порада «перенести `lockRelease`
за `acceptMatrix`» зламала б `#250m`; справжня причина була в коді (`lockOurs` не
повертався в `false`). Урок: запис боргу називає симптом правильно, а причину — здогадом.

### ✅ ВИКОЧЕНО 05.09.2026 (пізніше) — ТЗ «Клієнти та реактивація» (прод `622a817`)

Чотири викати поспіль, усі прийняті (`ca07f8e` → `d0ca853` → `38ecb8c` → `622a817`).

**Що змінилось для людей.** Вкладки «План місяця» і «Реактивація» **злиті в один список**
зі станом-фільтром (рішення власника): 667 рядків = 179 активних + 188 сплячих + 300
втрачених, дублів нуль. Клієнт, що заплатив, виходить зі списку сам; задача на нього
закривається сама і **з видимим слідом**, чому саме. План можна ставити всім компаніям.
У картці зʼявились дзвінки по роках (рік згортається) і **журнал дій керівника**.

**Головне під капотом.** `POST /client-plan` не зберігав НІЧОГО з 29.07 — `$6` виводився
двічі з різними типами, тобто падіння на РОЗБОРІ, за будь-яких значень. Полагоджено,
відмови стали видимими, а `submit`/`approve`/`return` тепер пишуть слід ТИМ САМИМ
запитом — доти на питання «хто і коли подав» відповіді не існувало в принципі.

⏳ **НЕ ЗАКРИТО, і це перше, що треба зробити наступною сесією:**
1. **37 старих пачок реактивації досі живуть чеклістами** — інструмент
   `dist/tools/backfillPackChildren.js` написаний і викочений, але запускається ЛИШЕ з
   прод-чекауту (у стенді роль read-only, і це навмисно). Пробний прогін тепер САМ
   вставляє рядки й відкочує їх, тож «зелено» означає «база прийняла». Числа: 37 · 347 · 194 · 0.
2. **Роздвоєне читання задач** (`core/reactivation.ts`) прибирати ЛИШЕ після п.1 —
   інакше неперенесені пачки зникнуть із карток клієнтів.
3. **Жива перевірка ланцюга** «менеджер подав → тімлід затвердив» — не проходила
   ЖОДНОГО разу за життя екрана. Менеджер мусить бути **з командою**, тімлід — **тієї
   самої** команди (троє активних менеджерів команди не мають, з ними «Затвердити» мовчить).

📐 **Три речі, які знайшла реальність, а не міркування:** `CHECK` у базі спинив запис,
який пройшов усі мої перевірки (пробний прогін рахував, але не питав базу); чужий `#328`
виявився флаком на мілісекундній роздільності `Date.now()`; мій `#30c` спирався на
інваріанту, яку я сам же й прибрав.

### ✅ ВИКОЧЕНО 05.09.2026 — роль «Бухгалтерія» і обсяг даних (прод `e2f8e01`)

**Скарга:** завели роль бухгалтера, дали Дебіторку й Виписку — люди нічого не бачать.

**Причина була не в дебіторці.** Форма створення ролі слала обсяг `own` ЗАШИТИМ
(`SettingsSection.tsx:352`), а перемикач обсягу жив лише в редагуванні. Далі `middleware`
вивів із `own` робочу роль `manager`, `receivablesScope` звузив по менеджеру — яким
бухгалтер не є, — і екран віддав **нуль рядків БЕЗ помилки**.

📐 Один прохід проти живої бази: власник 72 рядки, бухгалтерія **0**. Після перемикання
обсягу власником — 72 проти 72, **множини ключів клієнтів збігаються**; менеджер і тімлід
лишились строгими підмножинами; після проходу синку — те саме.

**Код:** обсяг більше не має мовчазного значення (`auth/roleScopeInput.ts`: `null` = «не
передали», роут відмовляє 400 із причиною); форма створення питає обсяг явно.
`#341`/`#341b` стережуть двері ЗАПИСУ, `#342`/`#342b` — ЧИТАЛЬНИЙ бік (заміна `?? "own"`
на `?? "company"` у `rbac.ts` тепер червоніє). Роль оголошена в `ROLE_DECLARATIONS`
**тим самим рухом**, що й перемикання: розійтись їм не можна в жоден бік (`#15d`).

**Рішення власника (голосом 05.09, записані в `.claude/rules/access-roles.md`):** перелік
екранів не звужуємо, обсяг — вся компанія, **тільки перегляд**, потреба одна.
🔴 По дорозі знайдено розбіжність із самим рішенням: роль тримала `manage_bank_accounts`
і `manage_bank_hidden`, тобто могла **видалити банківський рахунок**. Права зняті,
перевірено ОБОМА половинами: 0 дверей редагування відкрито, 0 переглядів загублено.

**Приймання:** `deploy:check` 11/11 · `test:prod` **841/841, падінь 3 — усі в реєстрі** ·
`test:matrix` **«#11: жодна клітинка не змінилась»** · у живому бандлі є нова підказка
про обсяг (байтова перевірка, не скріншот).

🔴 **ЛИШИЛОСЬ ІЗ ТЗ — окремий прохід, власник сказав «зробимо»:**
- **§5.2** — обсяг має читатись з ОДНОГО місця. Заміряно: **74** обробники звужують дані,
  **0** із них питають `data_scope`; 143 згадки `managerId`/`teamId` у 9 файлах роутів.
- **§5.4** — ОДИН підтримуваний шлях призначати роль. Зараз пʼять писарів `users.role`
  повз роут (панель, `create-user`, бот, `seed.ts`, міграції); синк переживає лише
  `role_override`. Бухгалтерії поставили правильним — тобто **пощастило**.

⚠️ **Клас помилки, вартий памʼяті:** доступ склався правильно ВИПАДКОВО. Скарга була «не
бачимо даних», тож дивились ту вісь, на яку скаржились, — а друга (права редагування)
лишалась відкритою, і цього не бачив ніхто.

⚠️ **Ланцюг деплою спіткнувся ПІСЛЯ успішного приймання:** `lockRelease` стоїть перед
`acceptMatrix`, тож наступний `lockTouch` б'є в порожній замок і ланцюг падає кодом 7.
Викат при цьому повний і зелений; `test:matrix` довелось прогнати руками.

### ✅ ВИКОЧЕНО 04.09.2026 — плашка «вийшла нова версія» (прод `5f48518`)

**Що зроблено.** Вкладка, яка крутить стару збірку, тепер каже про це людині сама:
плашка над банером тривог + кнопка «Оновити». Правило живе в **бекенді**
(`clientStale()` у `version.ts`, віддається через уже наявний публічний
`GET /api/health?loaded=<sha>`), фронт лише малює. Причина такого поділу заміряна:
**у `frontend/src` немає жодного тесту**, тож правило, покладене туди, було б
неперевірюваним за побудовою. Sha вшивається в бандл через `define` у vite-конфізі.

**Навіщо це знадобилось (ціна — пів дня трьох людей).** Кнопку «Подати план»
полагодили 02.09 і викотили; бандл на проді зібрано 04.09 об 11:47. О 14:51
користувачка написала «кнопка сіра, ніхто не може» — і була права щодо того, що
бачила: її вкладка крутила стару збірку. Симптому не існувало в принципі — сайт
відкривається, health 200, версія правильна, просто **не та, що в браузері**. Прод
умів помітити стару збірку на СЕРВЕРІ (`buildStale`) і не вмів — у ВКЛАДЦІ.

**Гейти:** `#330` / `#330b` / `#330c` (правило: різні → true · однакові → false ·
невідоме → **null**, а не false) + `#331` / `#331b` по АРТЕФАКТУ (бандл несе 40-hex
sha; без цього `define` можна тихо загубити, і плашка помре назавжди при зелених
`#330*`). Усі пʼять доведені саботажем.

**Приймання:** `deploy:check` 11/11 · два повні `test:prod` і матриця, **837 із 837
обовʼязкових, несподіваних скіпів 0**. Червоних чотири, жодне не від цієї зміни:
`#181`/`#124`/`#125` — у реєстрі `EXPECTED_REDS`; `#15` — див. нижче; `#36` упав на
першому прогоні (`/report` 2112 мс проти порога 2000) і **зник на другому** —
дисперсія Neon, не регрес. У матриці ще `#328` (відомий флейк харнесу). **Дрейфу
матриці немає.**

**⚠️ Не доведено й чесно назване:** сама поява плашки в браузері — для цього
потрібна справді застаріла вкладка. Доведе себе **на наступному викаті**: будь-яка
вкладка, відкрита до нього, тоді покаже плашку. Пункт «перевірити під роллю
менеджера» теж відкритий.

### 🔴 ДВІ ЗНАХІДКИ ТОГО Ж ДНЯ — ОБИДВІ НЕ ПРО ЦЕЙ КОД

**1 · У прод-базі є неоголошена роль `____________`** (дванадцять підкреслень).
Саме вона червонить `#15`, і створена вона **через екран Налаштувань**, а не кодом —
тобто лежить там уже якийсь час, а гейт мовчки червонів на кожному прийманні. Треба
або прибрати, або оголосити в `ROLE_DECLARATIONS`; заразом подивитись, чи не
призначена вона комусь.

**2 · Спільне імʼя учасника ПОВНІСТЮ знімає замок чекауту.** Дві сесії підписались
однаково (`Mac-сесія`) — інструмент прочитав чужу заявку як свою й пропустив далі.
Крок `standTo` перевів стенд на чужий sha і **зіпсував приймання сусідньої сесії**;
`tsc -b` убило хостом через дві одночасні збірки. Прод не постраждав (`prod-untouched`).
🔴 **Підпис має бути унікальним за СЕСІЄЮ, а не за машиною** (далі йшов під
`version-banner`). Варто, щоб `--take` на вже взятий замок із тим самим імʼям вимагав
явного підтвердження, а не мовчав.

### 🟡 ВІДКРИТЕ, ЩО ЗНАЙДЕНО Й НЕ ЗРОБЛЕНО

- **Звіт КВП порівнює неповний місяць із повним.** `resolveKvpScope`
  ([dashboard.ts:7536](../backend/src/routes/dashboard.ts)) для режиму «Місяць» бере
  попередній період як ВЕСЬ попередній місяць. 04.09 картка «Отримані кошти» показувала
  **▼ 2.3М ₴** — це 4 дні вересня проти 31 дня серпня (2 574 251 vs 311 971). На
  однакових вікнах 1–4 вересень **ушестеро кращий** за серпень (311 971 / 111 угод проти
  50 092 / 22). Лікується like-for-like вікном, як уже зроблено у Звіті 2.0, + підписом
  бази на картці. **Власник назвав це аварією — це аварія екрана, не продажів.**
- **Плани по клієнтах не зберігаються з 03.08** (`POST /client-plan` → 500,
  `inconsistent types deduced for parameter $6`). Фікс `03a3f44` існує в гілці
  `feature/client-plans-save-fix`, **у `main` його НЕМАЄ**. Це крок 1 ТЗ
  «Клієнти та реактивація».
  ⚠️ **Дата в ТЗ і в тілі коміта неправильна:** там стоїть 29.07, а `ClientPlansSection`
  створено **03.08** (`4373567`) — перевірено на повному клоні. Висновок ТЗ від цього не
  падає, але дату треба виправити, інакше наступний перевіряльник витратить на неї день.

---

### Попередній стан (04.08.2026) — пакет «екрани клієнтів» (прод `e0d27ad`)
Шість правок власника, усі в проді, обидва набори зелені.
1. **Ієрархія команда → менеджер → клієнти** (адмін/ОД/КВП — згорнуто; тімлід — його команда розгорнута; менеджер — плаский список). Підсумок рівня = сума ВИДИМИХ рядків.
2. **Відповідальний біля кожного клієнта** — одне джерело з формою передачі: `COALESCE(pinned, основний за оплатами)`, 📌 = спрацювала перша гілка.
3. **Перемикач сортування** на обох рівнях: дефолт «найгірші зверху» (% плану), другий режим «найбільші зверху» (факт ①). Рівні без плану — після тих, у кого план є.
4. **Живий пошук клієнта** (`/dashboard/client-search`): КВП/ОД/адмін — усі; **тімлід — лише своя команда, кламп на сервері** (`mm.team_id`).
5. **Картка клієнта** (`/dashboard/client-card`): 12 міс ① по канонічному ключу + останні 15 угод. Два якорі підписані.
6. **Обʼєднання клієнтів тімліду — лише коли ОБИДВА боки його команди** (`auth/mergeScope.ts`, кламп у `/client-merge`, `/preview`, `/revoke`; журнал тімліду звужено до його записів). Передача відповідального лишилась за `merge_clients`.
   **Прибрано:** три блоки старого покоління + картки менеджерів унизу + кнопки «➕ в реактивацію». `GET /loyalty`, `GET /regular-clients`, `GET|POST|PUT|DELETE /reactivation` — у `DEAD_ROUTE_CANDIDATES` (перегляд після 01.10.2026).

**Приймання (заміряно на проді):** `npm test` 80/0 · `test:prod` **150/150, падінь 0** · `test:matrix` **1094 пари на 196 ендпоінтах, дрейф 0**. Health: `e0d27ad`, sync `stale:false`.

**🔴 ОДИН ВІДКАТ ПО ДОРОЗІ — ЗАПАМʼЯТАТИ КЛАС ПОМИЛКИ.** Перший викат (`0908c9d`) дав червону матрицю: `POST /client-merge` і `/revoke` віддавали **400 замість 403**, бо перевірка «alias і canonical обовʼязкові» стояла ПЕРЕД клампом — запит помирав на валідації, не дійшовши до межі. Доступу це не відкривало (з реальними ключами кламп працює), але ламало гарантію, на якій стоїть увесь зліпок #11: «403 у deny-рядку означає, що спрацював ГЕЙТ». Відкат за процедурою → фікс `e0d27ad` (гейт першим оператором) → повторний викат → обидва набори зелені. **Правило на майбутнє: у роуті з клампом гейт іде ДО валідації тіла, інакше матриця перевіряє не те, що думає.** Тримає окремий тест `#30j` (порожнє тіло від тімліда → 403), бо матриця ганяється раз на реліз, а це — щоразу.

**Відкрите на власника (ЗАКРИТО 04.08.2026):** дія «🗑 прибрати з постійних» повернута в КАРТКУ КЛІЄНТА — рішення власника.

---

### 🟡 ГОТОВЕ, АЛЕ ЩЕ НЕ ВИКОЧЕНЕ — пакет «реактивація + прибрати з постійних + розвідка реклами» (гілка `claude/git-log-review-872eh2`)

**А · Екран реактивації.** Ієрархія команда → менеджер → клієнти (той самий механізм, що на екрані планів: адміну згорнуто, тімліду його команда розгорнута, менеджер — плаский список). Дві дати в рядку: «остання оплата · N дн.» з підписом **джерело стану** і «останній дзвінок · N дн.» (`ringostat_calls` по канонічному ключу) з підписом **довідка, на стан не впливає**. Правило написане над списком, пороги приїхали з ядра. **Фактичні пороги: сплячий `SLEEPING_DAYS=60`, втрачений `LOST_DAYS=180`** — збігаються з очікуваними.

**Б · «Прибрати з постійних» — у картці клієнта.** По дорозі знайдено ДВІ поломки (обидві — ДІЄЮ, не читанням):
1. upsert затирав НЕпередані поля → знімав би закріпленого менеджера (`#32`);
2. **`hidden` ніколи не працював** — `LEFT JOIN … AND NOT lo.hidden` разом із `WHERE COALESCE(lo.hidden,false)=false` це порожня операція (`#25e`).

**В · Розвідка рекламних лідів — `docs/AD_LEADS_RECON.md`** (read-only, нічого не побудовано).

**🔴 ЩО ЛИШИЛОСЬ ПЕРЕД ВИКАТОМ — і чому цього не зробила та сесія.** У неї **не було доступу ні до бази, ні до прод-API** (немає `DATABASE_URL`, токена, `RINGOSTAT_AUTH_KEY`). Тому:
- `npm test` — **83 pass / 0 fail** локально, включно з новими `#25d`/`#25e`/`#32` проти піднятого одноразового кластера PostgreSQL; усі три доведені **саботажем** (ламаєш перевіряєме — червоніє саме на цьому рядку);
- **`npm run test:prod` НЕ ГАНЯВСЯ** — потрібні `API_BASE` + `DATABASE_URL`. Нові `#30k`/`#30l` написані й у маніфесті, але **жодного разу не виконувались**. За правилом проєкту («скіп, який ніколи не виконувався, — не страховка») прогнати їх ПЕРШИМИ;
- **порахувати `SELECT COUNT(*) FROM loyalty_overrides WHERE hidden` у проді.** Ці клієнти зараз видно попри позначку; після фіксу вони зникнуть з планів і реактивації — це зміна цифр, попередити команду.

---

---

**Попередній стан (17.07.2026):**

**Ядро — ЗАКРИТО.** Гроші, воронка, сегменти, ліди, дебіторка, конверсії — усі в `core/`,
чесні, звірені. Конверсія ввімкнена в UI (14300%/2909% → чесні ~12%, нерекламні → «—»).

**Поточний блок — ЗВІТИ (Р4).** Один компонент `ManagerReport` (менеджер + КВП, фільтр рівня).
- ✅ Р1 — чесна когортна воронка (`funnelCohortHonest`, ≤100%, «зайшли посередині» окремо)
- ✅ Р2 — «Очікування надходжень» (5-стадійна зона `виставлення рахунку…очікуємо оплату`), **самостійний
  блок із CRM, ПОВНІСТЮ окремо від дебіторки** (жодного зв'язку в коді/тексті). Бакети суто за МІСЯЦЕМ
  планової дати (`expectedPaymentsByPlanned`, взаємовиключні, покривають усю зону): `total` =
  overdue+thisMonth+nextMonth+later+noDate **сходиться до копійки**. thisMonth = ВЕСЬ календарний місяць
  (минулі дні місяця більше НЕ падають у overdue — це занижувало). UI: «Загальні» великим, під ним 3
  картки (цей/наступний/**протерміновані «з CRM · не борг»**), «пізніше» — рядок звірки. Верифіковано на
  проді: 428 / 1 467 145 = 290+5+132+1+0.
- ✅ Р4a — бекенд `/dashboard/manager-report` (усі секції, роль-скоуп 3 рівні + 403, тижні
  `weeklyBreakdown`, Δ до compareWith + `maturityMismatch`, цілі конверсій)
- ✅ **Прогноз hero = ФОРМУЛА A (обрана БЕКТЕСТОМ, не смаком): факт + ПОВНА грошова зона +
  добір на нові угоди.** Бектест 5 завершених міс (реконструкція «на 17-те» через
  `deal_stage_events`): A = зміщення **−3.9%, MAE 4.6%** (найгірший міс −14%); C (зона×65%
  closeCoef) = −17%; D-підлога (success×k+paidOnly) = **−46%** — місяці ЕКСТРЕМАЛЬНО
  задньозавантажені (на 17-те закрито ~20-25% фіналу). Ключовий коеф: **~65% зони@17
  реально закривається до кінця міс** (стабільно 62-67%), решту фіналу добирають угоди з
  РАННІХ стадій — тому повна зона (100%) точніша за зону×65%. Складові диз'юнктні (доведено:
  статуси + дата створення). **Добір** = `money.newBusinessDobir`: трейл-3м середнє від угод,
  створених ПІСЛЯ поточного дня місяця й закритих того ж міс (без зазирання вперед, скоуп
  по зрізу). floor(D)/byPace — контекстні підписи. Лип/відділ: 1 369 247 + 1 464 524 + 559 865 =
  **3 393 636 (~126%)**, коридор ±5% (MAE). ⚠️ Прогноз бере ПОВНУ зону (~1.47М), картка
  «Очікування → цей місяць» — planned-дату (~1.02М): РІЗНІ питання («де сяде у фінал» vs
  «коли за планом надійде») — підписано в обох місцях. ⚠️ `planned_payment_at` НЕ
  історизується → бектест B-формули (лише майбутній пайплайн) неможливий — чесно виключена.
  **🔁 Самоперевірка формули (стоп-критерій): `core/projectionCheck.ts` у `reconcileNightly`
  (Ч.4)** — першої ночі нового місяця логує «прогноз@17 vs реальний фінал» щойно завершеного
  місяця в табл. `projection_backtest` (PK ym, ідемпотентно, та сама методологія, що бектест);
  **|зміщення| > 10%** (`PROJECTION_ALERT_PCT`) → Telegram «формула поплила — ребектест».
  Верифіковано на проді: червень залогований (прогноз@17 2 812 351 vs фінал 2 770 142 =
  **+1.5%**, без алерту; RUN 2 → isNew=false).
- ✅ Р4b — фронт-каркас «Звіт 2.0» (hero виконання плану + очікування; решта — заглушки-плейсхолдери)
- ✅ **Р4c.1 — світлофор команд** (`TeamsTrafficLight`, лише рівень «Відділ»): команди рядками,
  найгірші зверху (сорт бекенду), бар+колір по % плану (<70 черв / 70-95 жовт / ≥95 зел), залишок у
  грн, клік → звіт по команді (level=team). Верифіковано: 6 команд, 37%→57% найгірші зверху.
- ✅ **Світлофор рівне-залежний + плоский рейтинг** (`TrafficLight`, генеричний): рівень «Відділ» —
  перемикач «Команди / Усі менеджери» (клік команда→level=team→менеджери команди; клік менеджер→
  level=manager); рівень «Команда» — менеджери цієї команди. % плану менеджера — НАТИВНИЙ `plans`
  (per manager_id; Σ = план команди = відділ; 15 менеджерів без плану на липень → «—», не вигадуємо).
  Інваріант доведено: Σ менеджерів = відділ по ФАКТ (1 369 247), ОЧІК (1 015 667), ПЛАН (2 700 000).
- ✅ **Р4c.1b — дрілдаун очікувань**: метрика «Цей місяць» скоупована по команді/менеджеру
  (`expectedThisMonthByScope`, те саме джерело що `expected.thisMonth`). Картка «Цей місяць» —
  розгортана матрьошка (chevron, без вкладеного скролу): відділ → команди (найбільші зверху) →
  менеджери. Рядок світлофора має підсумкове «очік. цей місяць» (одне число). Інваріант доведено:
  **Σ менеджерів = Σ команд = відділ = 1 015 667 / 288** без залишку (і по кожній команді).
- ✅ **Fix #3: світлофор показує 0%-виконавців + розподіл плану звільнених** — рядки (команди+
  менеджери) = сутності-з-планом ∪ з-фактом (не лише `receivedBy*`). Менеджер/команда з планом і 0
  факту → 0%, зверху. **План деактивованого РОЗПОДІЛЯЄТЬСЯ рівномірно на активних членів команди**
  (+план/N, точний залишок; рішення власника) — деактивований рядка не має, але план не зникає →
  **ціль лишається 2 700 000**. Σ teams=Σ managers=dept=2 700 000 (точно). Факт звільнених лишається
  в команді (`receivedByTeam` вкл. неактивних), НЕ на колегах; розрив `teamFact − Σактивних` →
  **явний орфан-рядок** (managerId=0, не клікабельний), зараз порожній (факт звільнених=0). Крайній
  випадок (0 активних) → план у той самий орфан-рядок. Верифіковано: Пехньо план 28 333 (20k+8 333) 0%.
- ✅ **E2E smoke-QA** (`qa/smoke-qa.mjs`) — постійний Playwright-аудит перед показом: механіка
  (роути/рівні/періоди/dropdown/світлофор), інваріанти (Σ=Σ=відділ, бакети=total, конв≤100%),
  крос-екран Огляд↔Звіт 2.0. Live (QA_TOKEN) / offline (QA_FIXTURES). Зловив: Огляд-прогноз і
  Огляд-очікування на СТАРИХ формулах (Крок 2), і цей fix #3.
- ✅ **Like-for-like Δ** (hero + світлофор): для ПОТОЧНОГО неповного місяця вікно порівняння =
  `previousRange` (1–17 vs 1–17), для завершених — повний vs повний; база Δ — **датований потік
  `success`** (`compare.successFlow`, `teams.flowCur/flowPrev`), НЕ received-снапшот (свіжий paidOnly
  vs дозрілий місяць перекручував). Received лишається базою % плану. Підпис бази стоїть на обох
  місцях. Було→стало (лип-відділ): hero-Δ −50.7% → **+44.5%**.
- ⬅️ **НАСТУПНЕ: Р4c.2** — воронка (`funnelCohortHonest`) → далі конверсії vs цілі → тижні/перенесені
- ✅ **Fix: manager-picker Звіту 2.0 був порожній** — `managerOptions` вантажився ефектом, гейтованим
  `section==='report'` (старий звіт), тож на `manager-report` лишався `[]`. Новий `ManagerPicker`
  (пошук + групування по командах + «— без плану») живиться масивом `managers` з відповіді звіту
  (кеш merge-by-id). Team-picker OK (teams глобальний). Прод `/manager-report` → 29 менеджерів.
- Р4f (пізніше) — провалювання в метрику → графік історії

**🔴→✅ БАГ КОМИ-РОЗДІЛЬНИКА виправлено (17.07).** Парсер сум дебіторки стирав кому як
«сміття» (`.replace(/[^\d.-]/g,"")`), тож комірка «26073,48» (26 073,48 грн) → «2607348» →
роздувалась рівно **×100**. Укрпошта показувала **2 684 449** замість **~103 174 грн** і
роздувала ВСЮ дебіторку на **29,9%** (11,4 млн → **~8,84 млн**). Виділено `parseAmount`
(пробіл→розряди тисяч, кома→десятковий) у `syncReceivables.ts`, застосовано в `parseRow` +
`parseLimitRow`. Верифіковано на проді: Укрпошта=103 174, sheet=8 640 094, тотал=8 839 794.
✅ `syncAdBudget.ts:8` перевірено на даних — там ІНШИЙ формат (крапка-десятковий + пробіл-тисячі:
«4 839», «3 029.97»), КОМИ немає взагалі → поточний парсер коректний, стрип-коми не спрацьовує.
Фікс `parseAmount` туди НЕ застосовувати (кома→крапка зламала б «1,234»=тисячі). Баг лише в дебіторці.

**✅ Дебіторка = уся безнал-таблиця + готівка МГЕР з CRM (оновлено 17.07, РЕВЕРС 1:1).**
За рішенням власника тотал знову включає готівку МГЕР. `receivablesTotal` = SUM(усі
`receivables`, обидва source) = **11 409 569** (безнал 11 221 369 + готівка МГЕР 188 200; безнал
дрейфує з кожним 15-хв ресинком — інваріант: тотал = sheet + cash = сумі рядків). Прибрано
`source='sheet'` у `debtWhere` → готівка входить і в тотал, і в розрізи (клієнт/менеджер/команда)
окремим рядком «МГЕР (готівка)»; тотал == сумі видимих рядків (перевірено: sum(byManager)==total).
`receivablesCash` (188 200) лишився під-рядком «з них готівка» на картці Огляду. «Без менеджера»
(ПВК АРСЕНАЛ 1.575 млн) — у тоталі (Є в таблиці). Верифіковано на проді через прямий імпорт
`dist/core/metrics` + сирий SQL: total==SUM(all), total−cash==sheet, cash==188 200 — усі PASS.

**✅ Вартовий покинутих стадій (КРОК 6.6).** Раптове падіння обсягу подій ключової стадії
(handoff-и, грошові етапи) >80% від медіани 3 міс → Telegram-алерт (`checkAbandonedStages`,
`abandonedStagesWatch` щодоби). Ловить зламаний CRM-процес, як реактивація-142 (покинули 04.2026).

## Правила плану для звітів (власник)
- **Динамічна розбивка місяць→тижні:** перевиконав тиждень → залишок на решту тижнів зменшується
  (`weeklyBreakdown`, фіксовані 7-денні блоки від 1-го, тижневий факт = датований success).
- **Перенесені з минулого місяця** — окремим рядком (у загальному профіті, але видно «чистий старт»).
- **Цільові конверсії:** РНК 15% · лідген 7-8% (ціль 7.5%) · менеджер (лід від лідогена→успіх) 10%
  (`CONVERSION_TARGETS`, CONVERSION_RULES §8).
- **Очікування = цей + наступний місяць** (прострочене → дебіторка, не сюди).

## Відкладено (потребує зовнішніх інтеграцій)
- **Реальна дебіторка** — API банків + 1С (замість ручної гугл-таблиці). Великий окремий блок.
- **Білий екран / продуктивність фронту** — діагностувати з вимірюванням (error boundary + розбити моноліт).

## Техборг (закрити в кінці блоку)
- Прибрати relay з `test.uts.ua` + ротувати relay-токен (світився).
- Видалити стару конверсію-логіку (Фаза 3, після Р4c коли UI стабільний).
- `conversion_full_cycle` (В4) — коли опишеться.

## Хто за що
- **Власник (Сергій)** — бізнес-рішення, доступи, попередження команди.
- **Claude Code** — код, дані, деплой. Працює по цих доках.
- **Зовнішній рецензент (архітектор)** — ловить сліпі зони, перекладає бачення в ТЗ, рецензує
  результат. Тримати ОКРЕМО від виконавця (дві незалежні голови ловлять те, що одна не бачить).
