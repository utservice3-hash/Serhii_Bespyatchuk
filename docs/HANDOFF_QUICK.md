# HANDOFF_QUICK — точка входу за 30 секунд

> Нова сесія (Claude Code чи зовнішній рецензент) — почни звідси.
> Оновлювати в КІНЦІ кожної робочої сесії (розділ «Де ми зараз»).

---

## Що це за проєкт
Аналітичний дашборд відділу продажів логістики UTS. Джерело — Kommo CRM.
Пройдено повний **reset ядра**: усі метрики переписані на чесні, звірені з CRM, з нічною
самоперевіркою. Зараз будуємо звіти на цьому фундаменті.

## Файли-джерела правди (читати в цьому порядку)
1. **`CLAUDE.md`** (корінь) — бізнес-правила, деплой, граблі.
2. **`docs/HANDOFF.md`** — детальний стан + інфраструктура.
3. **`docs/MASTER_PLAN.md`** — історія reset'у, ФАКТИ Ф1–Ф12, правила роботи.
4. **`docs/METRICS_GLOSSARY.md`** — одне визначення на метрику (нормативний).
5. **`docs/CONVERSION_RULES.md`** — визначення всіх конверсій + цільові %.
6. **`docs/CRM_SCHEMA.md`** — воронки, стадії, поля Kommo.
7. **`docs/BACKLOG_FULL.md`** — повна черга задач.
8. **`docs/KVP_REPORT_SPEC.md`** — макет і вимоги до звітів.

## Правила роботи (незмінні)
1. **Один крок за сесію**, СТОП після кожного, показати результат.
2. **Заборонені перевірки «A = B ✓»** (тавтологія). Тільки зовнішня звірка: наше / лист / Kommo.
3. **Немає даних у CRM = блокер**, а не привід підмінити метрику.
4. **Не вигадувати бізнес-правил** — немає в доках → питання до власника.
5. Перед деплоєм у показ — таблиця «було→стало», попередити команду про зміну цифр.
6. Стоп-критерій майже завжди: **звірка 4 рівні зелена + еталон РПК-Яцик серпень 2025 = 242 / 680 655**
   (наше 242 / 677 316 — угоди точно, виручка Δ 0.5% через реоупени; це норма).

## Інструменти самоперевірки (вже в проді)
- Нічна звірка 4 рівні (цілісність / синк / дашборд / свіжість) + auto-heal + вартовий покинутих стадій.
- Node-режим хостингу (adm.tools), деплой через relay на `test.uts.ua` (тимчасово; прибрати в кінці — техборг).

---

## 🟢 Де ми зараз (оновлювати щосесії)

**Дата останнього оновлення:** 17.07.2026

**Ядро — ЗАКРИТО.** Гроші, воронка, сегменти, ліди, дебіторка, конверсії — усі в `core/`,
чесні, звірені. Конверсія ввімкнена в UI (14300%/2909% → чесні ~12%, нерекламні → «—»).

**Поточний блок — ЗВІТИ (Р4).** Один компонент `ManagerReport` (менеджер + КВП, фільтр рівня).
- ✅ Р1 — чесна когортна воронка (`funnelCohortHonest`, ≤100%, «зайшли посередині» окремо)
- ✅ Р2 — сума очікування (5-стадійна зона `виставлення рахунку…очікуємо оплату` = 441 угода / 1.52 млн;
  показ = цей + наступний місяць; overdue → дебіторка, не сюди)
- ✅ Р4a — бекенд `/dashboard/manager-report` (усі секції, роль-скоуп 3 рівні + 403, прогноз
  `revenueProjection`, тижні `weeklyBreakdown`, Δ до compareWith + `maturityMismatch`, цілі конверсій)
- ✅ Р4b — фронт-каркас «Звіт 2.0» (hero виконання плану + очікування; решта — заглушки-плейсхолдери)
- ⬅️ **НАСТУПНЕ: Р4c** — наповнити заглушки: світлофор команд → воронка → конверсії vs цілі → тижні/перенесені
- Р4f (пізніше) — провалювання в метрику → графік історії

**✅ Дебіторка 1:1 з гугл-таблицею (закрито 17.07).** `receivablesTotal` = 11 314 369 == сира
таблиця до копійки. Готівка з CRM (МГЕР 188 200) винесена окремою позначкою (`receivablesCash`,
`receivables.source='cash'`), НЕ в основному тоталі. «Без менеджера» (ПВК АРСЕНАЛ 1.575 млн) —
ЛИШИЛОСЬ у тоталі, бо воно Є в таблиці (немапнутий менеджер), прибрати = занизити проти джерела.
Різниця, що була, = рівно готівка. Парс sheet звірено з raw до копійки (не застаріння).

**✅ Вартовий покинутих стадій (КРОК 6.6).** Раптове падіння обсягу подій ключової стадії
(handoff-и, грошові етапи) >80% від медіани 3 міс → Telegram-алерт (`checkAbandonedStages`,
`abandonedStagesWatch` щодоби). Ловить зламаний CRM-процес, як реактивація-142 (покинули 04.2026).

## Правила плану для звітів (власник)
- **Динамічна розбивка місяць→тижні:** перевиконав тиждень → залишок на решту тижнів зменшується
  (`weeklyBreakdown`, фіксовані 7-денні блоки від 1-го, тижневий факт = датований success).
- **Перенесені з минулого місяця** — окремим рядком (у загальному профіті, але видно «чистий старт»).
- **Цільові конверсії:** РНК 15% · лідген 7-8% (ціль 7.5%) · менеджер (лід від лідогена→успіх) 10%
  (`CONVERSION_TARGETS`, CONVERSION_RULES §8).
- **Очікування = цей + наступний місяць** (прострочене → дебіторка, не сюди).

## Відкладено (потребує зовнішніх інтеграцій)
- **Реальна дебіторка** — API банків + 1С (замість ручної гугл-таблиці). Великий окремий блок.
- **Білий екран / продуктивність фронту** — діагностувати з вимірюванням (error boundary + розбити моноліт).

## Техборг (закрити в кінці блоку)
- Прибрати relay з `test.uts.ua` + ротувати relay-токен (світився).
- Видалити стару конверсію-логіку (Фаза 3, після Р4c коли UI стабільний).
- `conversion_full_cycle` (В4) — коли опишеться.

## Хто за що
- **Власник (Сергій)** — бізнес-рішення, доступи, попередження команди.
- **Claude Code** — код, дані, деплой. Працює по цих доках.
- **Зовнішній рецензент (архітектор)** — ловить сліпі зони, перекладає бачення в ТЗ, рецензує
  результат. Тримати ОКРЕМО від виконавця (дві незалежні голови ловлять те, що одна не бачить).
