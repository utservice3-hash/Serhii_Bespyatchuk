# Передача UTS Dashboard — вхідний документ

> 🚧 **КІСТЯК. Документ пишеться просто зараз, чотирма паралельними проходами.**
> Розділи з поміткою «⏳ пишеться» ще не заповнені — це видно навмисно, щоб порожнє
> місце не читалось як «тут нічого немає». Готовність кожного розділу — у таблиці нижче.

**Кому:** Роман Денисюк (`roman680`).
**Від кого:** Сергій Беспятчук (власник продукту й усіх доступів на момент передачі).
**Гілка:** `handover/roman`. **У `main` НЕ змерджено навмисно** — це зробить Роман сам,
прочитавши; успішний мерж і буде першим доказом, що передача працює.

---

## 0. Як цим користуватись

Цей файл — **єдиний вхід**. Він не дублює наявну документацію, а веде до неї: дублікат
правди — це два джерела брехні. Якщо тут і в спеціалізованому документі щось
розходиться, **правий спеціалізований**, а цей файл треба виправити.

| документ | про що | стан |
|---|---|---|
| **цей файл** | доступи, як це крутиться, що в польоті, холодний старт | ⏳ пишеться |
| `docs/TRANSFER_ZONES.md` | зони відповідальності, `.claude/rules/*`, хуки | ⏳ пишеться (інший прохід) |
| `docs/TRANSFER_TESTS.md` | нумеровані тести, гейти, реєстри винятків | ⏳ пишеться (інший прохід) |
| `docs/TRANSFER_METRICS.md` | священні метрики та їхні еталони | ⏳ пишеться (інший прохід) |

Наявна документація, яку **не переписуємо**, а перевіряємо й виправляємо:
`docs/HANDOFF.md` · `docs/HANDOFF_QUICK.md` · `docs/STATE.md` · `docs/INFRASTRUCTURE.md`
· `docs/MASTER_PLAN.md` · `docs/BACKLOG_FULL.md` · `docs/KNOWLEDGE.md`
· `docs/BACKUP_RECOVERY.md` · `CLAUDE.md`.

---

## 🔴 Перше, що треба знати до будь-якої дії

### Збереження плану на клієнта на проді не працює. Ні для кого. Уже пʼять тижнів.

Фікс **написаний, протестований і запушений** — коміт `03a3f44` у гілці
`claude/task-gates-collision-rebase-a8ecik`. Він **не в `main` і не на проді**.

**Що саме зламано.** `POST /client-plan` віддає **500 на кожному збереженні** з
29.07.2026 — дня, коли екран переїхав із мертвої сітки `RepeatPlanGrid` на
`ClientPlansSection`. Причина — `inconsistent types deduced for parameter $6`: у запиті
`$6` стоїть і як `updated_by`, і всередині `CASE WHEN $5='approved' THEN $6 END`, тож
Postgres виводить для нього суперечливі типи.

**Це помилка РОЗБОРУ, а не даних.** Вона виникає до виконання запиту, отже падає
**завжди** — за будь-яких значень, будь-якої ролі, будь-якого клієнта. Наслідок у базі:
за серпень і вересень **нуль рядків**, чернеток не існує в принципі, кнопка «Подати»
сіра назавжди.

**Перевірено на живій базі, а не виведено з коду** (read-only, `PREPARE` у транзакції з
`ROLLBACK` — дані не чіпались):

```
РОЗБІР ВПАВ: inconsistent types deduced for parameter $6
код: 42P08
```

Те саме перевірено з трьох боків: прод `75519dd` фікс **не містить**; `origin/main`
фікс **не містить**; у `main` немає каста `$6::int` (`git grep -c` → `0`), тобто дефект
живий і в основній гілці.

🔴 **ДВА ВАРІАНТИ, І ОБИДВА НАЗВАНІ ПРЯМО — мовчання тут гірше за будь-який із них:**

1. **Викотити `03a3f44` до передачі.** Дефект зникає, Роман приймає робочу систему.
   Ціна: ще один викат руками Сергія за процедурою з розділу B.
2. **Прийняти систему з відомим живим дефектом.** Тоді це перше, що Роман викочує сам —
   і заразом його перший повний прохід деплою, на реальній, уже діагностованій задачі.

Обидва варіанти робочі. Не назвати цього взагалі — не варіант: екран виглядає
справним, кнопка нікого ні про що не попереджає, і наступна людина шукала б причину
знову з нуля.

---

---

## A. Доступи

⏳ **пишеться.** Таблиця: сервіс · для чого · де живе доступ · хто власник зараз · що
зробити для передачі. Рядок на **кожне** імʼя змінної оточення, знайдене в
`backend/src`, без пропусків.

> 🔒 **У цьому репозиторії немає і не буде ЗНАЧЕНЬ секретів** — ні тут, ні в
> `.env.example`, ні в історії git. Тільки: **імʼя** змінної · **де** живе · **хто**
> власник · **як** ротувати. Значення передаються поза git, і після кожної передачі
> ротація — обовʼязковий крок, а не побажання (див. розділ H).

---

## B. Як це крутиться

⏳ **пишеться.** Топологія, деплой точними командами, замок чекауту й три умови його
взяття, легкий режим проти повного, відкат коду **і наслідків**, бекапи, джоби.

---

## D. Що в польоті

### D1. Гілки на `origin` — 33, і майже всі можна прибрати

⚠️ **Спершу про пастку в цифрах, інакше висновок буде протилежним.** `git rev-list
--count` показує на цих гілках 208, 230, 653, 278 «незлитих комітів». **Це не робота,
яка загубилась.** Історію колись переписували, тож старі гілки розійшлись із `main`
базою, а не змістом. Правильний вимір — **що гілка ДОДАЄ** відносно бази злиття:

```
git diff origin/main...origin/<гілка> --stat | tail -1
```

Заміряно по всіх 17 «незлитих»: у **14 із них — нуль файлів різниці з `main`**. Тобто
їхній вміст уже в `main` цілком, попри лічильники в сотні комітів.

**✅ Вміст цілком у `main` — можна видаляти без втрат** (14):
`affectionate-hypatia-TRWp8` · `contact-import-discrepancy-awx06w` · `continuation-rsg4z5`
· `dashboard-development-54e8ng` · `hotfix-sync-dedup` · `keen-allen-qbSBo`
· `leadgen-call-transcripts-ako7jp` · `magical-gates-qt9749`
· `manager-time-tracker-app-5cohsd` · `replace-claude-md-bjibai`
· `session-handoff-setup-ujebcq` · `tatiana-kovtynuk-analysis-yk937k`
· `uts-bot-logic-review-gir8c8` · `vps-relay-setup-mRJAW`

**✅ Уже предки `main`** (8): `consolidated-block-measurements-ykt1gq` ·
`friendly-galileo-8pijhl` · `matrix-coverage-gate` · `matrix-in-chain` · `tracker-c2` ·
`tracker-page-guards` · `tracker-user-sync-gates` · `uts-relay-slug-echo-ky4qlw`,
а також `feature/tracker-sso` · `feature/tracker-user-sync` · `fix-79b` · `pass2` ·
`pass3`.

**🔴 Має вміст, якого в `main` немає — рівно чотири:**

| гілка | що несе | що з нею робити |
|---|---|---|
| `claude/task-gates-collision-rebase-a8ecik` | **`03a3f44` — фікс `$6::int`** (4 файли, +130) | рішення з блоку «перше, що треба знати» |
| `claude/git-log-review-872eh2` | 11 файлів, +357: заморожена робота по деплой-ланцюгу (див. D3) | заморожено власником, не скасовано |
| `claude/tracker-user-sync-merge` | 7 файлів, +255 навколо трекера | ⚠️ **перевірити перед видаленням**: файли `trackerRoster.ts` і `trackerSso.test.ts` у `main` уже Є, причому `trackerRoster.ts` збігається **байт-у-байт** (0 рядків різниці). Решта різниці — у файлах, які `main` відтоді змінював. Тобто ймовірно `main` попереду, але це **не доведено**, і саме тому тут стоїть «перевірити», а не «видаляти» |
| `claude/uts-relay-check-optimize-jgbzg2` | один файл `qa/measure/tasks-sizing.mjs` (+328) — інструмент замірів, не продукт | вирішити, чи потрібен інструмент |

### D2. Прод проти `origin` — розходження немає

Заміряно: `health.version` = `75519dd`, `origin/main` = `75519dd`, розрив `0 0`.
⚠️ Це стан **на момент написання**. Перевіряти щоразу однією командою — вона в
розділі B; розходження «прод попереду гілки» в нас уже траплялось двічі й обидва рази
означало, що «повернути як було» повернуло б **не те**, що працює.

### D3. Заморожена робота — не скасована

У `claude/git-log-review-872eh2` (коміт `db09db0`) лежить закінчений і прийнятий, але
**свідомо заморожений власником** прохід із трьох частин:

- **`#211f`** — читання більше не пише: `weekPlansForMonth` втратив саму можливість
  морозити знімки, єдиним писарем лишився `backfillWeekPlans`, і він переїхав у крон
  (щодня 04:50, глибина 3 місяці). Причина заміряна на проді: відкриття КВП за минулий
  місяць писало в `weekly_plan_snapshots`, під роллю `test_readonly` це давало
  `permission denied`, а під повними правами — тихий запис, якого ніхто не просив.
- **`#328`** — обхід кешу модулів був `?bust=${Date.now()}` із роздільністю в
  мілісекунду; на швидкому хості два виклики збігались, і гейт падав «через раз».
- **`#211c`** — не чіпалось, чужа зона (`core/overviewCache.test.ts`).

**Стан:** запушено, у `main` не змерджено. Це не борг і не помилка — це пауза.

### D4. Ухвалено, але не виконано

Джерело — таблиця в `CLAUDE.md` (розділ «⏳ УХВАЛЕНО, АЛЕ НЕ ВИКОНАНО») і
`docs/BACKLOG_FULL.md`. Тут не переказую їх, а називаю **порядок за ціною помилки**:

1. **Стейджинг** (окрема гілка Neon + інший хост). Поки його немає, пункт 7 «Definition
   of Done» — повний прогін проти живого API **до** викату — невиконуваний у принципі,
   і це визнано вголос, а не обійдено.
2. **Історії прогонів джоб немає** (`job_runs` тримає лише останній стан). Питання
   «скільки разів джоба впала за добу» **не має відповіді в принципі** — видно лише вік
   останнього успіху. За одну добу це ставало стіною тричі.
3. **Невдала спроба збереження не лишає сліду** — база не відрізняє «не пробували» від
   «пробували й не вийшло». Саме через це дефект із блоку вище прожив пʼять тижнів
   непоміченим.
4. **Помилка сервера на екрані формування планів невидима** — кнопка лишається
   клікабельною, 400 летить у порожнечу, людина бачить «нічого не сталось».
5. **Свята в календарі робочих днів** — власник назвав це боргом, не зміною.
6. **Relay → вужчий канал** — див. розділ A, рішення описане там.
7. **Розчистити зіткнення номерів гейтів** (`#6`, `#29`, `#111b`, `#30*`). Гейт на
   унікальність номера **вже існує і зелений** (`#223`/`#223b`) — заводити його не
   треба, лишилось саме розчищення.

---

## G. Холодний старт

⏳ **пишеться.** Перевірений чекліст від нуля: кожен крок — команда і **очікуваний
вивід**. Крок, який неможливо перевірити без Сергія, буде так і названий.

---

## H. Що Роман отримує поза git

⏳ **пишеться.** Чекліст **для Сергія**: що передати захищеним каналом і яка ротація
йде після кожного пункту.

---

## ❓ Невідомо — спитати Сергія

⏳ **пишеться.** Сюди йде все, чого не вдалося встановити з коду, `docs/`, `.claude/`
чи git-історії. **Правдоподібний текст тут гірший за порожнє місце:** Роман не має
способу його перевірити.
