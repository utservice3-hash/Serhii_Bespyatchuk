# ТЗ — Задачник: групи, файли, спільна задача

_Для Романа Денисюка. Складено 14.09.2026. Стан заміряно по коду й проду 14.09._

## 1. Що є зараз — заміряно

| можливість | стан | де |
|---|---|---|
| Групи / папки задач | **немає**. Є лише поле «команда» (`tasks.department`, вільний текст із відділів і команд), яке працює як кольорова мітка, а не як папка | `schema.sql:352`, `TasksSection.tsx:566` |
| Файли до задачі | **немає**. У `tasks` немає ні колонки, ні таблиці вкладень. Але механізм завантаження вже є в «Документах»: `doc_files` (uuid на диску, mime, розмір, автор), тека `backend/documents`, у нічному бекапі | `schema.sql:1149`, `routes/documents.ts` |
| Спільна задача | **наполовину**. `created_by` і `assignee_id` є, історія змін немає. Хто бачить: менеджер — лише свої призначені й свої особисті; тімлід — команду + свої; адмін — усі. Отже **менеджер, що призначив задачу іншому, її більше не бачить** | `routes/tasks.ts:59-90` |
| Доповнення до задачі | **одне текстове поле** `tasks.comments`, яке перезаписується. Хто й коли дописав — не зберігається | `routes/tasks.ts:399` |
| Виконавець | лише **менеджер з CRM** (`assignee_id REFERENCES managers`). Акаунти без менеджера (бухгалтерія, HR, рекрутер) виконавцями бути не можуть | `schema.sql:349` |
| Пряме посилання на задачу | немає (окрема задача Юлі) | |

🔴 Замок `#56`/`#56b`/`#56c`: Звіт читає з `tasks` поля плану (`metrics_json`, `payment_amount`, `kpi_period`). Нічого з цього ТЗ їх не чіпає, але роут `routes/tasks.ts` у списку «файлів контракту», тож зміна контракту — окрема задача з окремим прийманням. Це вона.

## 2. Що робимо

### 2.1 Групи задач
- Нова таблиця `task_groups` (id, name, owner_id → users, parent_id для вкладеності на майбутнє, created_at). Колонка `tasks.group_id` NULL-able.
- Група **особиста**: належить акаунту-власнику, створювати може будь-хто, чужі групи не показуються. Задача в чужій групі для іншого глядача виглядає як «без групи». **Група не змінює доступ**, лише розкладку.
- Екран: ліва панель «Групи» над списком, «Без групи» за замовчуванням, перетягування не робимо, вибір групи у формі й у рядку.
- Поле «команда» лишається як є: це інший вимір (чия команда), а не папка.

### 2.2 Файли до задачі
- Нова таблиця `task_files` за зразком `doc_files`: task_id, name, stored_name, mime, size_bytes, created_by, created_at. Тека `backend/task-files` поруч із `documents`, поза публічним `/api/files`.
- `POST /tasks/:id/files` (multipart), `GET /tasks/:id/files`, `GET /tasks/:id/files/:fid` (віддача з перевіркою доступу до задачі), `DELETE` лише автору файла або адміну.
- Ліміт **5 МБ** на файл (рішення Романа 14.09), до 10 файлів на задачу. Зображення й PDF відкриваються в браузері, решта на завантаження.
- 🔴 Заміряти до початку: скільки місця на диску хоста і чи `documents` справді потрапляє в бекап (у коментарі так написано, перевірити скриптом бекапу).

### 2.3 Спільна задача
- **Правило видимості:** автор бачить задачу, яку призначив, завжди, незалежно від ролі. Зміна в `routes/tasks.ts:59`: до умов менеджера й тімліда додати `OR t.created_by = <я>` для призначених задач (зараз це є лише для особистих без виконавця).
- **Стрічка доповнень:** таблиця `task_comments` (task_id, author_id, body, created_at). Поле `tasks.comments` лишається як «короткий коментар» у рядку, стрічка з'являється в розкритті задачі. Писати можуть автор, виконавець, тімлід виконавця й адмін.
- **Історія статусу:** `task_status_log` (task_id, from, to, changed_by, at). Автор бачить, хто й коли рухав статус.
- **Сповіщення:** **відкладено** (рішення Романа 14.09: спершу зʼясувати, чи є робочий Telegram-бот). У цьому проході лише бейдж «є нове» у задачнику для автора й виконавця. Канал додається окремим кроком.
- Виконавцем може бути **будь-який акаунт**: `tasks.assignee_user_id → users` поруч зі старим `assignee_id`, старе поле не чіпаємо (Звіт читає його). Виводиться одне імʼя.

## 3. Чого не робимо
1. Не чіпаємо KPI-парасольки, `metrics_json`, `plan`, `daily_kpi`, реактиваційні задачі, 1×1.
2. Не робимо вкладені групи в інтерфейсі (поле `parent_id` є, екран плоский).
3. Не робимо перетягування між групами.
4. Не видаляємо файли фізично при видаленні задачі одразу: помічаємо, чистимо нічною джобою через 30 днів.
5. Не переносимо старе поле `comments` у стрічку автоматично.

## 4. Що може зламатись — поіменно
- `/report-plan` і картка менеджера читають `tasks` (замок `#56`). Додаємо колонки, не міняємо наявні → гейт має лишитись зеленим, це перевіряється прогоном.
- `GET /tasks` для менеджера почне повертати більше рядків (призначені ним). Перевірити, що особисті чужі задачі й далі приватні (`#` на протікання є в тестах роуту).
- Матриця доступу: три нові роути файлів і два для коментарів → рядки в `accessMatrix.ts`, інакше `#17` червоніє.

## 5. Гейти
| # | твердження | червоніє, якщо |
|---|---|---|
| A | автор бачить призначену ним задачу в ролі менеджера | прибрати `created_by` з умови |
| A🪞 | чужа особиста задача менеджеру не видна | зробити умову ширшою |
| B | файл задачі недосяжний тому, хто не бачить задачу | віддавати файл без перевірки скоупу |
| C | коментар пише лише учасник задачі, дзеркало: учасник може | зняти перевірку або заборонити всім |
| D | зміна статусу лишає рядок у логі з автором | прибрати запис у лозі |
| E | `#56`/`#56c` без змін | будь-яка правка полів плану |

## 6. Приймання
1. Ти створюєш задачу мені з файлом і групою «Дашборд»; я міняю статус і пишу доповнення; ти бачиш обидва в себе без перезавантаження ролі.
2. Менеджер створює задачу тімліду: бачить її у «Свої», тімлід бачить у «Командні».
3. Бухгалтер призначений виконавцем, задача видна йому.
4. `npm test` приріст падінь 0, `test:prod` після викату, матриця зелена.
5. Скріншот розкритої задачі зі стрічкою, файлом і групою.

## 7. Обсяг і порядок
| крок | зміст | обсяг |
|---|---|---|
| 1 | Видимість автора + лог статусу + стрічка доповнень (без сповіщень) | 1 сесія |
| 2 | Групи: таблиця, панель, вибір у формі | 1 сесія |
| 3 | Файли: таблиця, роути, зона завантаження у розкритті | 1–2 сесії |
| 4 | Виконавець-акаунт (`assignee_user_id`) | 1 сесія |
| 5 | Сповіщення в Telegram | відкладено, окремий крок після рішення про бота |

Разом 4–6 сесій, два-три викати. Крок 1 має цінність сам по собі й закриває твій приклад «ти призначаєш, я виконую, ти бачиш статус».

## 8. Рішення Романа 14.09.2026
1. **Групи особисті.** Кожен акаунт створює свої групи без обмежень, чужих груп не бачить.
2. **Сповіщення відкладено** до зʼясування стану Telegram-бота. Поки бейдж у задачнику.
3. **Ліміт файла 5 МБ.**
