# Інфраструктура та готові рішення — UTS Dashboard

Що будувати самим, а що взяти готовим. Плюс дорожня карта узгоджених доробок.

---

## 1. Принцип: custom-логіка — своя, «загальне» — готове

| Потреба | Рішення | Чому |
|---|---|---|
| **Бізнес-логіка** (воронка, гроші, мінус-угоди, передані заявки, задачі, план/факт) | **Наш застосунок** | Жоден готовий BI не знає ваших правил (статуси Kommo, знімки оплат, РПК-воронка). Це ядро цінності. |
| **Ad-hoc аналітика, довільні графіки** | **Metabase** поверх того ж Postgres | Будь-хто кліками будує звіти без коду. Заміна Google Data Studio, але на живих даних. |
| **Бекапи** | Neon PITR + наш нічний CSV-дамп | Див. `BACKUP_RECOVERY.md`. Не пишемо своє сховище. |
| **Моніторинг аптайму** | **UptimeRobot** (безкоштовно) на `/api/health` | Пінгує щохвилини, шле алерт у Telegram/пошту, якщо впало. |
| **Сповіщення** | наш бекенд + **grammy** (вже є) у Telegram | Логіку знаємо ми; доставку робить Telegram. |

---

## 2. Metabase — ad-hoc аналітика (рекомендую)

**Що дає:** керівник/аналітик відкриває веб, кликами будує таблиці/графіки/дашборди по будь-яких полях, зберігає, шарить. Без звернень до розробника.

**Як підняти (≈1 год):**
1. Розгорнути Metabase (Docker: `metabase/metabase`, або хмара metabase.com).
2. Підключити **той самий Neon Postgres** — але **окремим read-only користувачем** (щоб аналітики не могли нічого змінити):
   ```sql
   CREATE ROLE metabase_ro LOGIN PASSWORD '…';
   GRANT CONNECT ON DATABASE neondb TO metabase_ro;
   GRANT USAGE ON SCHEMA public TO metabase_ro;
   GRANT SELECT ON ALL TABLES IN SCHEMA public TO metabase_ro;
   ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO metabase_ro;
   ```
3. Готово — таблиці видно, будуються звіти.

**Межа:** Metabase бачить **сирі таблиці**, не наші «розумні» метрики (мінус-угоди, знімки оплат). Для складних правил — лишається наш дашборд. Metabase = «покопатись у даних», наш застосунок = «правильні бізнес-показники».

---

## 3. Моніторинг

- **UptimeRobot** → монітор HTTP на `https://dashboard.uts.ua/api/health`, keyword `"ok":true`. Алерт у Telegram, якщо ні.
- `/api/health` уже віддає стан синку (`stale`, `consecutiveFailures`, `lastError`) — можна алертити і на «синк застиг».
- Логи бекенду: `/home/evraziat/.system/supervisor/2199.log`.

---

## 4. Дорожня карта узгоджених доробок

Статус на момент документа:

- [x] **Бекапи** — нічний CSV-дамп + документ відновлення ✅
- [ ] **Час у етапі / застряглі угоди** — підсвічувати в огляді менеджера угоди, що довго стоять на етапі *(дані вже є: `deal_stage_events`)*
- [ ] **Проєкція на місяць** на сторінці Огляд — «за поточним темпом вийде ₴X»
- [ ] **Експорт** звіту (Excel/PDF)
- [ ] **Мобільна версія** (адаптив таблиць/карток)
- [ ] **Telegram-сповіщення** — розширити (див. нижче)
- [ ] **Ротація relay-токена** (безпека)
- [ ] **Metabase** — за бажанням
- [ ] **UptimeRobot** на `/api/health`

---

## 5. Пропозиції по Telegram-сповіщеннях (на обговорення)

Зараз ТГ уже є (сповіщення про передані заявки). Пропоную додати **адресні, дієві** тригери:

**Щоденні (ранкові дайджести):**
- **Тімліду о 08:30:** підсумок вчора по команді (оплати ₴, прорахунки, авто) + хто 0 активності 2+ дні.
- **Менеджеру о 09:00:** його план на сьогодні + відкриті задачі з дедлайном сьогодні.

**Подієві (миттєві):**
- 🔴 **Синк впав** (`consecutiveFailures ≥ 3`) → адміну.
- 💰 **Велика оплата** отримана (> порогу) → тімліду (мотивація).
- ⏰ **Боржник прострочив** понад ліміт → відповідальному менеджеру + тімліду.
- 🐢 **Угода застрягла** на етапі > N днів → менеджеру.
- 📉 **План відстає** від темпу (у середині місяця виконання < очікуваного %) → тімліду.

**Тижневі:**
- **Пн 09:00:** підсумок минулого тижня по кожному менеджеру (для наради).
- **Чт 09:00:** темп поточного тижня.

Механіка: ті самі cron-джоби в бекенді + grammy. Пороги — у `app_settings` (редагуються в Налаштуваннях). Скажи, які тригери вмикаємо — зроблю.

---

## 6. Поточний стек (для нового розробника/асистента)

- **Frontend:** React 19 + Vite + recharts. `frontend/src/pages/Dashboard.tsx` — контейнер, секції в `pages/dashboard/sections/*`.
- **Backend:** Node/Express + `pg`, `node-cron` (джоби), `grammy` (ТГ). `backend/src/routes/dashboard.ts` — основна аналітика.
- **БД:** Neon Postgres. Структура — `backend/src/db/schema.sql`. Мапінг етапів — `seedKommoMapping.sql`.
- ⚠️ **PORT = 4000, не 3000.** У документі роками стояло 3000; перевірено по живому
  процесу 07.08.2026 — у логу `Backend listening on 0.0.0.0:4000`, а на 3000 не
  слухає ніхто. Через це локальний `curl 127.0.0.1:3000/api/health` під час деплою
  віддає порожнечу, і секунд тридцять здається, що прод покладено (сайт при цьому
  живий — Apache проксує на 4000).
- **Деплой:** relay-PHP на сервері evraziat (див. `CLAUDE.md`). Прод тягне гілку `claude/friendly-galileo-8pijhl`. **Повна процедура — нижче, §7.**
- **Джоби:** синк Kommo (5хв), події етапів (10хв), передані заявки (10хв), дебіторка (30хв), новини (08:00), KPI-задачі (07:00), реконсиляція (04:00), **бекап (03:00)**.
- **Проєктна памʼять:** `CLAUDE.md` — тримати актуальним (усі бізнес-правила там).

## 6b. Зʼєднання з БД: ПРЯМЕ, не через пулер Neon (рішення власника 05.08.2026)

`DATABASE_URL` вказує на **прямий** ендпоінт Neon, не на `-pooler`. Це свідомо, і
ось чому — щоб через півроку ніхто не «оптимізував» перехід:

- наш **read-only запобіжник** тестового харнесу стоїть на `PGOPTIONS=-c
  role=test_readonly`, який їде у **стартовому пакеті зʼєднання**. Пулер Neon
  працює в **transaction mode**, де сесійні опції не гарантовані — тобто
  запобіжник, що вже одного разу врятував прод від справжнього `DELETE`, став би
  «спрацьовує через раз, а виглядає завжди зеленим»;
- економити нема чого: `max_connections = 901`, пул застосунку **15**, реально в
  роботі одиниці зʼєднань (заміряно 05.08.2026).

🔴 Перехід на pooled-ендпоінт можливий ЛИШЕ разом із заміною механізму read-only
(наприклад, окремим користувачем БД замість ролі в опціях) — і це окреме рішення,
а не деталь конфігурації.

**Пул застосунку `max: 15`** (`db/pool.ts`). Був 6; `/overview` робить 35 запитів
на один запит екрана, тож четверо одночасних користувачів ставили 140 запитів у
чергу на 6 зʼєднань. Заміряно до/після: `/overview` ×4 8 546→4 550 мс, ×8
8 627→4 331 мс.

## 6c. Правила ЗАМІРІВ швидкодії

🔴 **МІНІМУМ ДВА ПРОГОНИ.** Розійшлись — шукати ЗОВНІШНЄ навантаження (тік синку,
паралельний набір тестів), а **не вибирати красивіший**. Реальний випадок
05.08.2026: перший «після»-замір дав `/overview ×4 = 10 500 мс` (гірше за «до»),
другий — 4 550 мс; різниця виявилась тіком `syncKommo`, що потрапив у вікно.
Подати перший як результат означало б відкотити правильну зміну; подати другий
без згадки про перший — прикрасити.

⚠️ Заміри «під навантаженням» усередині `test:prod` не є чистими: набір сам
вантажить сервер. Для порогів беруться ЧИСТІ заміри, у гейті — з запасом.

## 6d. 🔪 ДРУГИЙ NODE-ПРОЦЕС УБИВАЄТЬСЯ ЗА 35-55 с — ЛІМІТ АКАУНТА (LVE)

**Факт, заміряний 05.08.2026, не наша помилка й не привід досліджувати заново.**
Піднятий поруч із продом другий `node dist/index.js` (порт 4611) отримує **SIGKILL**
(`bash: … Killed`) через 35-55 секунд. Разом із ним гине й підпроцес-монітор, що за
ним стежив, — тобто вбивають ГРУПУ, а не один процес по памʼяті.

**Чому це не памʼять:** `free -m` показує ~200 ГБ вільних, RSS процесу ~160 МБ,
сума RSS усього акаунта ~670 МБ. Кап `--max-old-space-size=256` не допоміг.
Cgroup акаунта — `/lvp1100/lve1100` (CloudLinux LVE), його лічильники нам не читаються.
Відтворено **чотири** рази: із прод-каталогу, з копії `/tmp/br`, з обрізаним хіпом,
із фоновим і синхронним запуском.

**Наслідки, які з цього випливають:**
1. **Ізольований прогін `#11` (матриця доступу) неможливий** — їй потрібно ~40-55 с
   безперервних проб, тобто рівно те вікно, у якому процес помирає.
2. **Штатний режим матриці — проти ЖИВОГО API** (`API_BASE=https://dashboard.uts.ua`).
   Це не обхід: проба так і задумана — зліпок знято з живого прода, а незапис
   доведено знімком 70 таблиць до і після (див. коментар над `#11`).
3. **Це та сама причина, з якої знято п.7 DoD** (повний `test:prod` ДО викату).
   Тепер вона має назву: не «хостинг чомусь вбиває», а ліміт LVE.
4. Борг «стейджинг» лишається — окрема гілка Neon + ІНШИЙ хост. Поки його немає,
   пункт 7 невиконуваний **у принципі**, і вдавати виконання заборонено.

## 6e. 🧩 ЩО САМЕ ПІДНІМАЄ ПАНЕЛЬ — І ЧОМУ ВОНА **НЕ** Є ПАСТКОЮ

**Перевірено 05.08.2026 по живому процесу**, бо була підозра, що панель
перезапускає node зі СТАРОЇ збірки й тихо відкочує деплої. **Підозра не
підтвердилась.** Повний ланцюг запуску:

```
lve_suwrapper 1100
 └ bash -c 'source ~/.bashrc; cd /home/evraziat/uts.ua/dashboard/backend/ && npm start 2>&1 | tee -a …log'
    └ npm start
       └ node dist/index.js      (cwd = .../dashboard/backend)
```

`systemd --user` — 0 юнітів, `pm2` — не встановлено, у crontab запуску немає,
жодного конфіга, що пришпилював би артефакт. Панель просто робить `npm start` у
**живому checkout**, тобто **завжди піднімає той `dist`, що лежить на диску**.
Отже, автоматичний респавн НЕ може відкотити деплой — він підніме найновішу збірку.

### 🔴 СПРАВЖНЯ ПРИЧИНА ІНЦИДЕНТУ: ЗІБРАВ І НЕ ПЕРЕЗАПУСТИВ

Банер показав «рантайм 7e1c0b9, копія d91c342» — **49 хвилин** прод крутив
попередню збірку. Докази часом, а не здогадом:
- процес стартував **21:21:40**, `dist/version.json` перезаписано **21:25:07**,
  тобто збірка сталася через 3.5 хв ПІСЛЯ старту;
- `ppid` — звичайний `npm start`, той самий, що й після мого рестарту; процес
  прожив 3137 с без перерв, тобто його ніхто не перезапускав.

Коміт чіпав **лише фонову джобу**, тож рестарт здався зайвим — і саме тому його
не зробили. `version.json` читається **один раз на старті**, і health далі чесно
повідомляв те, що процес завантажив.

**ПРАВИЛО: будь-який `npm run build` на проді ЗАВЕРШУЄТЬСЯ рестартом.**
Немає «дрібних» збірок: після збірки рантайм і копія розходяться завжди, просто
іноді код збігається за поведінкою.

### 🛡 СТРУКТУРНИЙ ЗАПОБІЖНИК (а не пильність)

`/api/health` тепер віддає `buildStale` і `buildOnDisk`: порівнює SHA, який
завантажив процес, із тим, що лежить у `dist` **на момент запиту** (без кешу).
Стан «зібрано, але не перезапущено» більше не невидимий. Тримає гейт `#42`
(саботаж підміною файла під живим процесом) + дзеркало `#42b` (у деві без
`version.json` прапорець мовчить, інакше на нього перестали б дивитись).

⚠️ Межа: health бачить розбіжність лише ПІСЛЯ збірки. Якщо збірки не було
взагалі — порівнювати нема з чим, і це правильно.

## 6f. 🔒 ЗАМОК ПРОД-ЧЕКАУТУ (з 26.08.2026)

```
npm run lock -- --status [--log]
npm run lock -- --take    --who=<чат> --reason="що саме робиш"
npm run lock -- --release --who=<чат>
npm run lock -- --steal   --who=<чат> --reason="чому забираєш"
```

🔴 **ЧОГО ВІН НЕ РОБИТЬ, І ЦЕ СКАЗАНО ПЕРШИМ.** Замок **не унеможливлює** зіткнення —
будь-хто може виконати `git merge` руками, не спитавши нікого. Він **робить зіткнення
видимим і дорогим**: «я не знав, що там хтось є» перестає бути правдою, а перехоплення
лишає імʼя. Обіцяти більше означало б продати мʼютекс, якого тут немає.

🔴 **БЕРЕТЬСЯ ЗА ДОТИК ДО ЧЕКАУТУ, А НЕ ЗА НАМІР ДЕПЛОЮ.** Намір — стан голови; дотик —
те, що ламає чуже дерево. 26.08.2026 мій `merge --ff-only` не пройшов, а окремі виклики
збірки й копії — пройшли, і в докрут поїхав бандл чужого коміту. Тобто «я ж не деплою,
я лише подивлюсь» і є той випадок, заради якого замок існує.

**Два файли, і поділ не косметичний:**

| файл | що це | доля |
|---|---|---|
| `.deploy-lock` | **претензія**; її існування і є замок | зникає при звільненні |
| `.deploy-lock.log` | **журнал**, append-only | НІКОЛИ не видаляється |

Тому «хто тримав» — не поле, яке можна переписати, а історія, яку можна лише дописати.
Обидва в `.gitignore`: це локальний стан, не код.

### ⏳ TTL 20 хв НЕ ЗВІЛЬНЯЄ САМ

Протермінування рахується **на читанні** і **нічого не звільняє**. Автозвільнення за
таймером — це та сама крадіжка, тільки **без імені й без причини**: замок зникає, а в
журналі порожньо. Прострочений замок лишається ЗАЙНЯТИМ; забрати його можна лише явно:

```
npm run lock -- --take --who=<ти> --after-ttl
```

Причину такої події складає **інструмент**, а не перехоплювач: вона називає попереднього
тримача, вік замка й те, що він робив. `--steal` існує окремо — для аварії, коли TTL ще
не вийшов, — і **вимагає причини руками**.

### 🔓 «Чекаут вільний» ≠ «замка немає»

Вільний чекаут — це **немає змінених ВІДСТЕЖУВАНИХ файлів** (`git status --porcelain`
без `??`). Докрут живе в корені репозиторію, тож нетрековані файли там є **завжди** —
критерій «робоче дерево чисте» тут не працює в принципі.

### ❓ Чому не визначати зайнятість автоматично

Питання законне: чому не дивитись на `git status`, свіжий `dist` чи `app_boot`. Бо
**тиша має щонайменше три причини, і вони вимагають протилежних дій**:

| що видно | могло означати | правильна дія |
|---|---|---|
| чекаут чистий, нічого не рухається | ніхто не працює | заходь |
| те саме | пауза **посеред** чужого викату | не чіпай |
| те саме | чужий викат обірвався й лишив мішаний стан | розберись, потім чіпай |

**Відсутність сигналу — це не стан.** Автовизначення обрало б перший рядок і мовчки
помилялось би у двох випадках із трьох; замок вимагає, щоб про свій намір сказали
словами. Саме на цьому 26.08.2026 я вже спробував побудувати поллер «тиша = вільно» —
і та «тиша» була б паузою перед чужим аварійним викатом.

### 🧩 Скрипт бере замок сам, ручний дотик — ні

`deploy:run` має кроки `lockTake` (перед `ff`) і `lockRelease` (після `pushBranch`).
При **аварійному** виході замок лишається взятим **навмисно** — недороблений викат і є
той стан, заради якого замок існує; вихід каже про це вголос і дає команду звільнення,
щоб замок не читався як забуте сміття.

### 🔬 Атомарність — заміряна, з названою умовою

| операція | системний виклик | замір (24 паралельні процеси) |
|---|---|---|
| взяття вільного | `open(O_CREAT\|O_EXCL)` — прапорець `wx` | 1 переможець, 23 × `EEXIST` |
| перехоплення | `rename(2)` поверх наявного | 1 переможець, 23 × `ENOENT` |

⚠️ **Умова стоїть поруч із твердженням:** `rename` атомарний **у межах однієї ФС**, тому
тимчасовий файл кладеться поруч із замком, а не в `/tmp`. Гейта на «чи це справді одна
ФС» **немає навмисно**: перевірити це чесно з JS не можна, а гейт, що вдає перевірку,
гірший за названу межу.

Гейти: `#227` (відмова називає тримача, час і справу) · `#227b` (TTL не звільняє) ·
`#227c` (перехоплення пише подію з машинною причиною) · `#227d` (`--steal` без причини
заборонено, слід переживає звільнення) · `#227e` (втрату видно колишньому тримачеві
**обома** шляхами) · `#227f` (звільнення не-тримачем називає втрату) · `#227g` (`wx`,
а не «перевірив і записав»; пошкоджений замок = зайнятий) · `#226k` (аварійний вихід
каже про взятий замок).

## 7. Процедура деплою в прод (порядок кроків)

🚀 **З 25.08.2026 цей ритуал ЗАПИСАНО СКРИПТОМ** — `npm run deploy:check` (у контейнері)
і `npm run deploy:run` (на проді). Реєстр кроків із причинами — `src/tools/deployPlan.ts`;
перелік нижче лишається чинним і потрібен, коли скрипт недоступний.

🔴 **`deploy:check` — ЛИШЕ В КОНТЕЙНЕРІ.** На проді він відмовляється стартувати (код 3):
фаза робить `rm -rf dist && npm run build`, тож у прод-чекауті перезбере `dist` ПРОДА з
тієї гілки, що там лежить. 26.08.2026 так уже зробили з чужої незапушеної гілки — прод
лишився на старому sha, а на диску опинився бандл іншого коду.

🔗 **Фази стикуються без ручного кроку:** `deploy:run --target=<sha>` бере ціль ЯВНО.
Звіряти артефакт із HEAD прод-чекауту не можна — до `ff` там стоїть старий sha, і збігу
не було б ніколи. Sha беруть із поля `branchSha` артефакта, що лишає `deploy:check`.

⚠️ **Скрипт НЕ деплоїть сам** і НЕ прибирає жодного кроку: він робить непотрібною
памʼять про них. Режим вимагається явно (`--mode=full|light`), мовчазного дефолту немає.
`deploy:run` не стартує без свіжого артефакту від `deploy:check` — і свіжість звіряється
за ДВОМА sha (гілки і прода), бо перевірка тригодинної давності могла звірятись із продом,
що відтоді зрушив вісім разів.

📐 **Навіщо, числом:** за одну добу 25.08.2026 один виконавець зробив чотири викати — і
шість кроків виконав по-різному; `npx tsc -b` фронту не виконувався ЖОДНОГО разу з чотирьох.

Гілка розробки → прод-гілка `claude/friendly-galileo-8pijhl`. Деплоїти лише за явним токеном власника `ДЕПЛОЙ-ПРОД: ТАК`.

0a. **🛠 ПЕРЕВІРКА СЕРЕДОВИЩА — ПЕРЕД ПЕРШИМ `rm`, А НЕ ПІСЛЯ (додано 26.08.2026).**
   ```
   command -v npm node git || { echo "СТОП: середовище не вміє зібрати"; exit 1; }
   ```
   🔴 **Привід — реальний, того ж дня.** Ланцюг `… && rm -rf dist && npm run build`
   на проді виконав `rm` і аж тоді впав на **`npm: command not found`**: relay — не
   логін-шелл, і `npm` у його `PATH` немає (через relay треба `bash -lc`). Прод
   лишився **без `dist` на диску**: сайт працював із памʼяті процесу, health віддавав
   200, `test:prod` був би зелений — а рестарт у ту мить не підняв би нічого.
   ⚠️ **Урок вужчий за «зчеплення», і саме тому записаний окремо:** `&&`-ланцюг
   рятує від продовження ПІСЛЯ збою, але **не від того, що вже виконалось ДО нього**.
   Руйнівний крок не сміє стояти перед кроком, який здатен не знайти інтерпретатора.
   🤖 `npm run deploy:check` / `deploy:run` роблять це кроками `toolsCheck`/`toolsRun`
   і відмовляються йти далі; порядок стереже `#226i`.

0. **🔴 `npm test` НА ЧИСТІЙ ЗБІРЦІ ГІЛКИ — ДО ВСЬОГО ІНШОГО (додано 25.08.2026).**
   `cd backend && rm -rf dist && npm run build && npm test`
   Це не «ми зазвичай його ганяємо»: у DoD (`CLAUDE.md`) він названий обовʼязковим, а
   в ЦІЙ процедурі його не було жодним рядком — тобто той, хто деплоїть строго по
   інструкції, не запускав його ніколи.
   **Чому це критично саме тепер:** гейти механізму тижневої цілі (`#220`-`#220c`)
   піднімають одноразовий кластер PostgreSQL, а на прод-сервері бінарів PostgreSQL
   НЕМАЄ — там вони чесно скіпаються. Отже `test:prod` після викату їх НЕ виконує, і
   єдине місце, де саботаж `manual ?? dynamic` буде спійманий, — ось цей крок. Гейт,
   який стереже те, що можуть не запустити, не стереже нічого.
   ⚠️ Очікувані падіння в цьому оточенні (без `.env`) — ті, що потребують живого API
   чи повного набору змінних; звіряти треба ПРИРІСТ падінь, а не абсолютне число.
   🔴 **БАЗА ДІФУ — `health.version`, А НЕ ПРОД-ГІЛКА** (поправка 25.08.2026).
   Гілка й прод розходяться регулярно: інший чат викочує, не запушивши, і тоді
   «приріст проти гілки» міряний проти коду, якого на проді немає. Базу брати
   так: `curl -s https://dashboard.uts.ua/api/health` → `version.shortSha`, і
   рахувати baseline у `git worktree` САМЕ на цьому sha.
   ⚠️ І не звіряй роботу за sha наосліп: **ребейз лишає ту саму роботу з ІНШИМ sha**
   (25.08.2026 `51b1176` приїхав у прод як `a178454` — диф ідентичний). «Чи є цей
   КОМІТ у гілці» ≠ «чи ця РОБОТА на проді»; коли треба друге — звіряй диф або
   повідомлення, а не ідентичність хеша.
1. **FF-merge:** `git fetch origin <dev-branch>` → `git merge --ff-only origin/<dev-branch>` на прод-checkout (`/home/evraziat/uts.ua/dashboard`).
2. **Build BE:** `cd backend && npm run build` (tsc + копія `*.sql` у `dist/db`).
2b. **🔴 МІГРАЦІЯ — ВРУЧНУ, ПЕРЕД РЕСТАРТОМ. `npm start` ЇЇ НЕ ЗАПУСКАЄ.**
   `cd backend && set -a && . ./.env && set +a && npm run migrate`
   Пропустиш при зміні `schema.sql` — новий код звернеться до колонки, якої ще
   немає, і роут упаде з **500**. Заміряно 07.08.2026: `tasks.period_kind` додали
   в схему, `effectiveWeekTargets` почав по ньому фільтрувати — без міграції
   `/report-plan` (тобто ВЕСЬ екран Звіту) віддавав би помилку. У документі до
   того було 8 кроків і жодного слова про міграцію: деплой строго по інструкції
   поклав би прод.
   ⚠️ **`Migration applied.` — НЕ доказ.** Рядок друкується й тоді, коли частина
   роботи відкотилась (дедлок відкочує ВСЮ транзакцію, а повідомлення вже
   надруковане). Тому після міграції — **окремий запит по результату**: колонка
   існує І в ній немає несподіваних `NULL`. Приклад перевірки, що робили 07.08:
   `information_schema.columns` на наявність + `GROUP BY period_kind` на бекфіл
   (вийшло `week=43 · month=7`, жодного NULL). BE-only деплой без змін
   `schema.sql` — крок можна пропустити, але переконатись, що схема не чіпалась.
3. **Build FE:** `cd frontend && npm run build` (vite → `frontend/dist/`, новий хеш `index-*.js`).
4. **🔴 ОБОВʼЯЗКОВО — копія FE у веб-докрут.** Apache віддає **не** `frontend/dist`, а корінь `/home/evraziat/uts.ua/dashboard/` (там власні `index.html` + `assets/`). Vite пише лише в `frontend/dist`, тож без копії користувач бачить **старий** бандл, хоч API вже нове. Порядок (щоб index.html ніколи не показав на відсутній asset):
   - 🔴 **СПЕРШУ ПЕРЕВІРИТИ, ЩО Є ЩО КОПІЮВАТИ** (додано 25.08.2026):
     ```
     ls frontend/dist/assets/ || { echo "СТОП"; exit 1; }
     ```
     Без цього при впалій збірці `cp` мовчки не скопіює нічого, `&&`-ланцюг
     обірветься — і `index.html` лишиться СТАРИМ при новому бекенді, тобто
     розходження бандла й сервера. Спіймано на викаті `5662225`: `cp` упав із
     `cannot stat 'frontend/dist/assets/.'`, і врятувало лише те, що фронтовий діф
     був порожній і хеші не змінились. Це везіння, а не механізм.
   - потім нові asset-и: `cp -r frontend/dist/assets/. assets/`
   - статику: `cp frontend/dist/favicon.svg frontend/dist/icons.svg .`
   - **останнім** `index.html`: `cp frontend/dist/index.html index.html`
   - прибрати старий бандл — **ЛИШЕ якщо його імені НЕМАЄ в новому `index.html`**:
     ```
     for f in assets/index-*.js assets/index-*.css; do
       grep -q "$(basename "$f")" index.html || rm -f "$f"
     done
     ```
     🔴 **НЕ «якщо ім'я змінилось».** Vite хешує за ВМІСТОМ: правка лише в `.tsx`
     лишає css із ТИМ САМИМ іменем, і перевірка `OLD != NEW` не спрацьовує — а
     `rm assets/$OLDC` зносить рівно той файл, на який показує новий `index.html`.
     Так 05.08.2026 прод ~4 хвилини віддавав сторінку **без стилів** при зелених
     гейтах, health 200 і правильній версії. Спіймав скріншот приймання.
   - `index.html` віддається `no-cache` (див. `.htaccess`), тож новий хеш підхоплюється одразу.
4b. **🥶 ПРОГРІВ ПЕРЕД ПРИЙМАННЯМ.** Перший запит після рестарту холодний —
   заміряно **12 133 мс** на `/overview` проти 2 432-3 355 мс через хвилину.
   Перед `test:prod` зробити по 2 запити на `/overview` і `/report`, інакше
   `#36` почервоніє без жодного регресу. Це вимір, а не поріг.

5. **🔴 Рестарт node — `kill -TERM` ЧЕРЕЗ RELAY (переписано 25.08.2026).**

   🔴 **ПОПЕРЕДНЯ РЕДАКЦІЯ КАЗАЛА «pid не видно взагалі» — ЦЕ ВІРНО ЛИШЕ ДЛЯ SSH.**
   Суперечність назвали ДВА чати незалежно, і ціна помилки не косметична: читач
   ішов писати в підтримку замість рестарту за 8 секунд.

   | канал | `ps -C node` | `ps -eo args` | `kill -TERM` |
   |---|---|---|---|
   | SSH | порожній | процесу не видно (інший namespace) | ❌ нема кого вбивати |
   | **relay** | **порожній теж** | ✅ **видно** `node dist/index.js` | ✅ **працює** |

   Тобто `ps -C node` порожній в ОБОХ каналах — саме він і створив враження, що pid
   не існує. Через relay процес видно повним переліком аргументів.

   **Робочий спосіб — ОДНІЄЮ командою** (pid не має протухнути між викликами):
   ```
   PID=$(ps -eo pid,args | awk '$2=="node" && $3=="dist/index.js" {print $1}' | head -1)
   [ -n "$PID" ] && kill -TERM "$PID" || echo "процес не знайдено — НЕ вбиваю нічого"
   ```
   🔴 **НЕ `pgrep -f node`**: `-f` матчить ВЛАСНИЙ командний рядок виклику, тож
   знайде себе і «доведе», що процес живий. На цьому вже спіймались — вибірка по
   `ps`+`awk` з ТОЧНИМ порівнянням аргументів такої дірки не має.

   ✅ Заміряно САМОСТІЙНО на двох викатах — `f5f5b81` (24.08) і `9ec05dc` (25.08);
   про третій поспіль повідомив власник із заміру іншого чату. Респавн конвеєром
   `npm start | tee`, health віддає новий sha за ~8-15 с.
   ⚠️ Розрізняти своє й переказане тут важливо: попередня редакція цього ж розділу
   стверджувала більше, ніж було перевірено, — і саме це коштувало наступним читачам
   зайвого звернення в підтримку.

   ### 🔁 Що відбувається після `exit 1` (замір 25.08.2026)

   Питання стояло відкритим: `loadRolesOrDie()` → `exit 1` має сенс лише якщо процес
   хтось підніме. Тепер відповідь спирається на замір, а не на здогад.

   **Ланцюг нагляду на проді** (прочитано `ps`, read-only):
   ```
   lve_suwrapper (ppid 1)
     └─ bash -c 'source ~/.bashrc; cd …/backend && npm start 2>&1 | tee -a <log>'
         └─ npm start └─ node dist/index.js
   ```

   **① КОД ВИХОДУ НЕ ДОЇЖДЖАЄ НІКУДИ — заміряно в контейнері на тій самій формі:**

   | що зробив node | код `bash -c '… npm start 2>&1 \| tee -a log'` |
   |---|---|
   | `exit 1` | **0** |
   | вбито `SIGTERM` | **0** |
   | `exit 1`, але `set -o pipefail` | **1** ← доказ причини |
   | без пайпа взагалі | 1 (тобто `npm` код пропускає справно) |

   `PIPESTATUS=1 0` — код node **не втрачений**, він просто не стає кодом пайпа.
   🔴 **Наслідок:** аварійний вихід і штатну зупинку **неможливо розрізнити** за
   статусом. Тобто будь-який нагляд, що дивиться на код виходу, поводиться з `exit 1`
   ТОЧНО ТАК САМО, як із нашим `kill -TERM`, — а після `kill` процес підіймається
   (заміряно двічі, ~8-15 с).

   **② РЕСПАВН ПІСЛЯ КРАХУ СПОСТЕРЕЖЕНО НА ПРОДІ** (`app_boot`, 23.08.2026):
   **17 стартів поспіль** із рівною каденцією **~60 с** (36…172 с), без наростання
   паузи й **без відмови піднімати**; епізод розсмоктався сам. Каденція 60 с і
   латентність рестарту 8-15 с не суперечать одна одній: процес жив ~45 с, помирав,
   і за ~15 с його піднімали.

   ⚠️ **МЕЖА ЦЬОГО ЗАМІРУ, названа вголос.** `app_boot` веде записи лише з 21.08.2026
   (44 старти), і **всі 17 крахів — це ОДИН епізод**. Отже доведено: «17 разів поспіль
   не здався». НЕ доведено: що порога відмови немає взагалі, і що причиною того епізоду
   був саме `exit 1`, а не вбивство дубля процесу хостом (той самий інтервал 1-2 хв).

   ✅ **Практичний висновок для fail-closed роль-кешу:** `exit 1` НЕ означає «лежачий
   сайт до звернення в підтримку». Означає: сайт лежить, процес підіймають знову,
   цикл ≈ 31 с ретраїв + до ~60 с очікування, і система вичухується сама, щойно Neon
   відповість. Це прийнятна ціна за закриті гейти — але про неї треба знати.

   ⚠️ **Кнопки Supervisor** («Відновити»/«Перезапустити»/«Зупинити») на викаті
   `878600b` на процес **не подіяли**, хоча напередодні пробний рестарт тією ж кнопкою
   пройшов. Механізм ненадійний, його зелений відгук нічого не доводить — **не тиснути**.
   **Техпідтримка adm.tools** лишається запасним шляхом, якщо relay недоступний;
   тоді планувати як ЗОВНІШНЮ ЗАЛЕЖНІСТЬ: між збіркою й рестартом прод крутить старий
   код при новому `dist` (`/api/health.buildStale: true`).
6. **Health:** `curl https://dashboard.uts.ua/api/health` = 200 ×N, `stale:false` (за потреби смикнути `POST /api/dashboard/sync` з admin-JWT — освіжає вотермарк без ризику подвійного синку).
   ✅ **`version` у health — ЄДИНЕ достовірне підтвердження, що рестарт стався.**
   Не «кнопка позеленіла» і не «pid змінився» (pid ми не бачимо). Поки sha старий —
   рестарту не було, скільки б разів панель не відзвітувала про успіх.
6b. **🔴 `buildStale == false` — ТВЕРДЖЕННЯ, А НЕ СПОСТЕРЕЖЕННЯ (додано 26.08.2026).**
   ```
   curl -s https://dashboard.uts.ua/api/health   # buildStale має бути false
   cat backend/dist/version.json                 # sha має збігатися з health.version
   ```
   Детектор існував увесь час — бракувало **кроку з ненульовим виходом**. Стан
   «на диску не те, що в памʼяті» (або на диску немає нічого) не болить одразу:
   health 200, приймання зелене, — і бʼє при НАСТУПНІЙ дії, коли причину вже забуто.
   Різниця між «я подивився на вивід обірваного ланцюга» і «крок відмовився» — це
   різниця між уважним виконавцем і ним же о третій ночі; уважність не масштабується.
   ⚠️ **Лише ПІСЛЯ рестарту.** Між збіркою й `kill` `buildStale: true` — це НОРМА,
   а не поломка; перевірка до рестарту червоніла б завжди. Тримає `#226j`.

7. **Перевірка бандла наживо:** `curl https://dashboard.uts.ua/index.html?cb=<ts>` має показати НОВИЙ `index-*.js`.
8. **🔴 `git push origin HEAD:claude/friendly-galileo-8pijhl` — ОБОВʼЯЗКОВО, і це не формальність.**
   Заміряно 07.08.2026: прод-checkout був **на 36 комітів попереду** origin —
   тобто гілка на origin **не відображала того, що крутить прод**. Хто підняв би
   сервер із origin (нова машина, відновлення, інший асистент), мовчки відкотив би
   36 комітів, серед них фікс 500 на розгортці по днях. Крок дешевий, наслідок
   пропуску — тихий відкат без жодного сигналу.

⚠️ Крок 4 — системний, не разовий: пропустиш → «ендпоінт віддає нове, а сайт показує старе» (реальний інцидент). BE-only деплой (FE не змінювався) → хеш той самий, крок 4 можна пропустити, але звірити хеш усе одно.
