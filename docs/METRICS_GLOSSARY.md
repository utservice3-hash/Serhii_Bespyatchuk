# METRICS GLOSSARY — одне визначення на метрику

> Зафіксовано 13.07.2026 на основі `AUDIT.md`.
> **Статус виконання:** ПРОМТ 0.5 — ✅ виконано (див. `AD_LEADS_INVESTIGATION.md`;
> результати вже інтегровані в §3/§3a цього документа). §3b (adSources) + Правило №1
> (машини) + §2-заборона INNER JOIN + техборг — впроваджено 13.07 (див. git log).
> **Це нормативний документ.** Якщо код розходиться з ним — баг у коді, а не в словнику.
> Змінювати визначення можна тільки явним рішенням власника, з датою і причиною.

---

## Правило №0

**Kommo — істина.** Розбіжність дашборду з CRM = баг дашборду.
Розбіжність із ручним листом = **може бути помилкою листа**. Не підганяти під лист.

Доведено в аудиті: наша БД зійшлась із Kommo до копійки (320 угод, 906 912 грн),
лист має ручну похибку (321 угода) і не нетить мінусові угоди.

---

## ПРАВИЛО №1a — ДВІ ГРОШОВІ МЕТРИКИ, ① ОСНОВНА (рішення власника 02.08.2026)

Дослівно:

> **① РЕЗУЛЬТАТ МЕНЕДЖЕРА = виключно етап «успішно реалізовано» (142).**
> Причина: частково оплачені угоди автоматично переносяться в наступний місяць.
> Зараховувати їх людині зараз = рахувати незавершене. Метрика **ОЦІНКИ РОБОТИ**.
>
> **② ГРОШІ КОМПАНІЇ ЗАРАЗ = «оплата отримана» ∪ «успішно реалізовано».**
> Причина: для розуміння ситуації важливо, скільки коштів фактично в компанії.
> Метрика **СТАНУ**.
>
> Уточнення того ж дня: **орієнтуємось ЗАВЖДИ на «успішно реалізовано»**. ② — не друга
> рівноправна метрика, а **допоміжний орієнтир**.

**Розбіжність між ними — НЕ баг**, доки екран чесно підписаний, якою метрикою рахує.

- **①** — `core/money.ts`: `successMoney` та розрізи `successBy*`. Анкер `closed_at`,
  множина «ЗАРАЗ 142», signed price. Замір 07.2026: **2 765 155 ₴ / 1007 угод**.
- **②** — `receivedMoney` та розрізи `receivedBy*` (success ⊎ paidOnly, дедуп).
  Замір 07.2026: **2 865 961 ₴ / 1036 угод**. Різниця = 29 угод на 100 806 ₴, які
  оплачені, але ще не закриті.

**② живе рівно у двох місцях:** картка «Отримані кошти» на Огляді (підписана як
орієнтир) і AI `get_metric`, де три метрики названі окремо. Більше ніде.

⚠️ Може переглядатись, якщо з'являться розбіжності. Дата й причина — вище.

---

## ПРАВИЛО №1 — ДВА ЯКОРІ. Головне правило всього дашборду.

Це **корінна причина** «куди не глянеш — неточності». У проєкті змішувались два різні
якорі дати, і ніде не було підписано, який саме використовується.

| Якір | Дата-якір | Для яких метрик |
|---|---|---|
| **ПРОДАЖІ** | перехід угоди в етап **«Успішно реалізовано»** | Отримані кошти · Поставлені машини · Середній чек · Конверсія · Плани менеджерів · Уся звітність тімліда/КВП |
| **ФІНАНСИ** | дата **розвантаження (вигрузки) = дата акту** | Дохід по акту · Cash flow · Витрати · Комісійні · Дебіторка · Чистий прибуток |

**Наслідки — обов'язкові до виконання:**

1. **Дохід і машини мають ОДИН якір** (перехід у «Успішно реалізовано»). Тому вони не можуть
   розійтись, а `середній чек = дохід ÷ машини` консистентний за визначенням.
   Це скасовує попередню ідею рахувати машини за датою поставки.

2. **Два якорі НІКОЛИ не збігаються в межах місяця — і це НЕ баг.**
   Угода, що перейшла в успіх 30 червня, а розвантажилась 3 липня:
   для ПРОДАЖІВ — червень, для ФІНАНСІВ — липень. Так і має бути.

3. **Змішувати якорі в одній таблиці — ЗАБОРОНЕНО.**
   У кожній таблиці/картці дашборду має бути **явно підписано**, за яким якорем цифри
   (напр. підзаголовок: «за датою переходу в успіх» / «за датою акту»).
   Це не косметика — це те, через що місяць не сходились цифри.

4. Дати **завантаження / розвантаження** — операційні поля. Вони НЕ визначають успішність
   продажу. Вони живуть тільки у фінансовому якорі.

5. Снапшот-проксі («створені в періоді, ЗАРАЗ на АВТО», 339) і подієвий варіант
   (`deal_stage_events`, 163) — **обидва видаляються**. Обидва були неправильні:
   снапшот мутує заднім числом, події історично недобрані.

---

## 1. ПОСТАВЛЕНІ МАШИНИ  ✅ вирішено

**Визначення:** кількість авто в угодах, що **перейшли в етап «Успішно реалізовано»** в періоді.
Якір — ПРОДАЖІ (див. Правило №1).

Той самий якір, що й у «Отримані кошти». Одна реалізація в `core/metrics.ts`.
Історія перебудовується за весь період. Очікувана дельта з листом (305 vs наше) — пояснити,
не підганяти.

**Окрема метрика (ФІНАНСОВИЙ якір):** `Машини по акту` — авто за датою розвантаження.
Використовується тільки у фінансовому блоці. **Ніколи не в звіті продажів.**

**Уточнено 13.07 (ПРОМТ 0.9) — факти з Kommo:**
- Окремого поля «кількість авто» в угоді Kommo **НЕМАЄ** (перевірено всі кастом-поля
  320 угод Яцика за червень). Проксі «к-ть заповнених слотів Приход 1–5» дає 1:1
  (318 угод = 1 слот, 1 = 2, 1 = 0; SUM=320) — у 2026 одна угода = одна машина,
  тож SUM(cars) = COUNT(успішних угод) ПО ДАНИХ, не за означенням.
- Поле «Марка і Номер автомобіля» (2096383) заповнене на 100%, але БРУДНЕ
  («1» ×185, «й», порожні) — унікальні машини (120) з нього не порахувати.
- Листове «поставлені = 305» НЕ відтворюється жодним чистим правилом:
  price>0=315 · приход>0=319 · безнал=255 · готівка=65 · унік.машини=120.
  Найближче до листового «готівкою» 705 537 — сума БЕЗНАЛ 694 681 (Δ1.6%).
  Висновок: 305 — ручне/точково-часове число (Правило №0: пояснюємо, не підганяємо).
- Щоб машини стали справжніми машинами: або чистити «Марка і Номер», або завести
  в Kommo числове поле «К-ть авто». До того машини = успішні угоди (1:1 ера).

---

## 2. КОНВЕРСІЯ  ⚠️ ЦЕ ТРИ РІЗНІ МЕТРИКИ

**Головне відкриття.** «Конверсія» — не одна метрика. Це **три метрики зі спільним чисельником і різними знаменниками**, залежно від каналу. **Одна команда може мати всі три одночасно.**

Чисельник завжди один: **Поставлені машини** (§1, якір ПРОДАЖІ), відфільтровані по каналу.

| Метрика | Чисельник | Знаменник (ТАБЛИЦЯ/ПРЕДИКАТ, не описово) | Хто має |
|---|---|---|---|
| `conversion_ads` | дійшли до `MONEY_ZONE` у FC | **`adDealSql`** (§3 `ads_accepted`, вхід `ADZONE_TAKEN`) | команди на рекламному трафіку (напр. РНК) |
| `conversion_leadgen` | дійшли до `MONEY_ZONE` у FC | **`leadgen_touch` за `transfer_date`** (DISTINCT `lead_kommo_id`) | команди на лідогенерації |
| `conversion_full_cycle` | дійшли до `MONEY_ZONE` у FC | **створені угоди у FC-пайплайнах** `8921932`/`155304` | команди повного циклу |

**🔴 ЧИСЕЛЬНИК В УСІХ ТРЬОХ — ОДИН І ТОЙ САМИЙ:** із вхідної когорти — скільки дійшли до
**`MONEY_ZONE`** = `EXPECT_ZONE ∪ {69716460, 60412544, 142}` у воронці Повного циклу, дедуп по зерну.
Реалізація одна на всіх — `conversionByCohort` у `core/metrics.ts`; змінюється лише `entry` (знаменник).

**🔴 ЧОМУ ФІНІШНА ЛІНІЯ СПІЛЬНА:** доки вона була різна, канали були **несумісні між собою** —
лідоген показував 3.8% проти 7.6% на тих самих лідах лише тому, що його чисельник обривався раніше.
Спільна фінішна лінія — єдина умова, за якої канали можна класти поруч в одну таблицю.

**⚠️ Знаменник лідогену — це `leadgen_touch`, а НЕ `lead_transfer_events` і НЕ `leadgen_registry`
напряму.** `leadgen_touch` зводить обидва джерела (`source = 'registry' | 'transfer_events'`) в один
дедуплікований перелік лідів. Рахунок по сирих таблицях дає розбіжність у десятки разів.

**Заборонено:**
- Показувати «Конверсію» без вказівки, яка саме. У UI — завжди підпис каналу.
- Зводити три конверсії в одну усередненням. Це різні воронки.
- `INNER JOIN psm` у знаменнику. Немапнені ліди **не мають зникати мовчки** — це і давало 67% замість 42%.

**Наслідок:** кожна відправлена машина мусить мати **канал**. Правило атрибуції каналу (і реатрибуції — див. CLAUDE.md) — єдине, в `core/`.

**Розгортка плану по воронці** використовує конверсію **того каналу**, до якого належить сегмент. Усереднювати між каналами — заборонено (це вже було в правилах движка планів).

---

## 2b. КОГОРТНА vs ПЕРІОДНА КОНВЕРСІЯ  ✅ вирішено

Лід, прийнятий у червні, часто поставляється в липні/серпні. Тому існують **дві** конверсії,
і плутати їх не можна.

| Метрика | Як рахується | Для чого | Коли готова |
|---|---|---|---|
| `conversion_*_cohort` | **Когорта:** ліди, ПРИЙНЯТІ в періоді → скільки з них **зрештою** поставилось (без обмеження по даті поставки) | **Розгортка плану по воронці.** Тільки вона математично коректна. | «дозріває» 2–3 міс. |
| `conversion_*_period` | **Період:** поставлені в місяці ÷ прийняті в тому ж місяці (різні когорти) | Оперативний звіт, динаміка | одразу |

**Правила:**
- **Розгортка плану ділить ТІЛЬКИ на когортну конверсію.** Періодна для планування —
  систематично неправильна: вона стрибає щоразу, коли потік лідів росте або падає.
- Для розгортки береться когортна конверсія **зрілих когорт** (лаг ≥ 90 днів), а не
  поточного місяця (він ще не дозрів і покаже штучно низьку конверсію).
- У UI обидві мають **різні назви**. Показувати «Конверсію» без уточнення — заборонено.
- Незріла когорта позначається явно: «червень 2026 (дозріває, 68% лідів ще в роботі)».

---

## 3. ЛІДИ З РЕКЛАМИ / З МІТКАМИ  ✅ вирішено (розслідування 0.5)

**Загадку 1251 розкрито:** правило «3 поля» без фільтра Кваліфікації по ВСІХ воронках дає
**1352** (Кваліфікація 329 + Повний цикл 857 + Продзвін 98 + Реактивація 68) ≈ лист 1251.
Лист рахував мітки по всіх воронках. Питання закрите.

**Ключове відкриття, яке псувало ваш маркетинг:** правило «3 поля» ловить **НЕ ТІЛЬКИ платну
рекламу**. Серед 857 тегованих повного циклу: `cpc 318`, **`organic 87`**, `none 53`,
`referral 1`. Тобто третина «рекламних» лідів прийшла безкоштовно.
**Наслідок: історичний cost-per-lead був занижений** — бюджет ділився на ліди, частина
яких органічні. Назва «Ліди з реклами» для цієї метрики була просто неправильна.

**ТРИ ОКРЕМІ МЕТРИКИ (не варіанти однієї):**

| Метрика | Правило | ~/міс | Для чого |
|---|---|---:|---|
| `web_tagged_leads`<br>**«Ліди з веб-мітками»** | 3 поля, **всі воронки**, без фільтра стадії | ~1352 | Заміна листової `ad_leads` у Статистиках. Історія бекфіллиться (мітки є на старих лідах). **НЕ називати «реклама».** |
| `paid_leads`<br>**«Платні ліди (cpc)»** | підмножина `web_tagged_leads`, платний трафік: **`traf_type='cpc' OR utm_medium='cpc'`** | ~318* | **Єдина метрика, на яку можна ділити рекламний бюджет.** CPL = бюджет ÷ paid_leads. |

**🔴 ПРАВИЛО ПЛАТНОГО ТРАФІКУ (paid) — ДВА ПОКОЛІННЯ МІТОК:** у CRM співіснують два
**взаємовиключні** набори трекінг-полів (доведено КРОК 6.1 — обидва присутні в КОЖНОМУ
з 12 місяців, не темпоральна міграція): **utm-покоління** (`utm_source`/`utm_medium 481995`/
`utm_campaign 481997`) і **TRAF-покоління** (`ADV_CAMP 2098331`/`TRAF_SRC 2098327`/
`TRAF_TYPE 2098329`). Платний сигнал живе в РІЗНИХ полях кожного покоління:
**`paid = (traf_type='cpc' OR utm_medium='cpc')`**. По угоді заповнене лише ОДНЕ покоління
(перетин ~1.5%). **Рахувати лише по `traf_type='cpc'` — ЗАБОРОНЕНО:** воно покриває ~55%
мічених угод, решта ~45% (utm-покоління, теж `cpc` у `utm_medium`) випала б із платних —
дзеркало помилки «organic серед реклами». Далі — як і раніше: **тільки перший дотик,
реактивовані виключаються** (навіть з `cpc`, див. §3 нижче).
| `ads_accepted`<br>**«Прийнято реклами (CRM)»** | `adDealSql` (з правильним списком `adSources`) | ~912 | Скільки рекламних угод узяли менеджери. **Знаменник для `conversion_ads`.** |

\* цифра з розслідування — по повному циклу; перерахувати по всіх воронках.

**`ads_accepted` і `web_tagged_leads` — різні ОСІ, не конкуренти.** Доведено:
перетин 753, ad-only 159 (кол-трекінг упіймав джерело, web-міток нема — дзвінок ≠ клік),
tag-only 104 (мітки є, але угода атрибутована реактивації, яку `adDealSql` свідомо ріже).

**Історія CPL перераховується** за `paid_leads`. Старі цифри — позначити як завищені.

---

## 3a. ПРАВИЛО АТРИБУЦІЇ КАНАЛУ  ✅ вирішено

**Воронка «Кваліфікація» = сирий вхід реклами.** Туди падає ВСЕ, що приходить з реклами:
нерозібрані заявки, пропущені дзвінки, залишені форми. Коли лід розібрали і взяли в роботу —
**та сама угода (той самий `deal_id`) міняє воронку** на «Повний цикл».

**Наслідок:** підсумок по воронках (1352) — **чесний, подвійного рахунку немає**.
Угода в кожен момент часу лежить рівно в одній воронці.

### Пріоритет атрибуції (застосовувати саме в цьому порядку)

| Пріоритет | Умова | Канал |
|---|---|---|
| **1 (б'є все)** | Угоду **реактивував лідогенератор** | `leadgen` |
| 2 | Угода підпадає під `adDealSql` (кол-трекінг `client_source` **або** web-мітки) | `ads` |
| 3 | Інакше | `full_cycle` |

**«Реактивація б'є мітки».** Клієнт прийшов з реклами в березні → менеджер закрив →
лідогенератор реактивував у червні. На угоді ЛИШИЛИСЬ старі utm/geo-мітки — але вони
від першого дотику і більше нічого не означають. Канал = **лідоген**, не реклама.

Це доводить, що `adDealSql` **правильно** ріже `%реактив%`. Ті 104 «tag-only» угоди
з розслідування — не втрачені рекламні ліди, а лідоген.

### 🔴 Наслідок для CPL — подвійне списання бюджету

`paid_leads` **рахує ТІЛЬКИ ПЕРШИЙ ДОТИК.** Реактивовані угоди виключаються — **навіть з міткою `cpc`**.

Інакше: клієнт, куплений рекламою в березні і реактивований у червні, потрапить у
`paid_leads` червня. Ми **порахуємо одного клієнта двічі** і **вдруге спишемо на нього
рекламний бюджет, якого не витрачали**. CPL занижується вдруге — вже після органіки.

---

## 3b. `adSources`  ✅ ВИПРАВЛЕНО (13.07.2026)

**Було (міна):** `adSources` у БД порожній, дефолтний список жив у коді (`getSettings()` мерджив) →
хтось зберігав налаштування → `ads_accepted` **мовчки перераховувалось заднім числом за всю історію**.

**Стало (чинний стан):**
- Список живе **в БД** — сид у `schema.sql` (`app_settings.data->'adSources'`).
- `getSettings()` **НЕ** мерджить дефолт із коду. Порожній список = **гучна конфіг-помилка**
  (`console.error`, рекламні метрики = 0), а не тихий фолбек.
- Зміна списку пишеться в журнал `ad_sources_log` (хто/коли/було→стало).

**Залишилось відкритим (не блокує):** історичні метрики рахуються **поточним** списком, а не тим,
що діяв на дату періоду. Тобто правка списку досі зачіпає історію — але тепер **видимо** (журнал),
а не мовчки.

Три цифри за червень, жодна не збігається:

| Джерело | Червень | Що це може бути |
|---|---:|---|
| Лист (`ad_leads`, imported) | **1251** | ? — джерело невідоме |
| CRM `adDealSql` («Прийнято реклами») | **914** | ? |
| Правило власника «3 поля» (Кваліфікація + utm/ADV/TRAF) | **329** | тільки ті, що дійшли до Кваліфікації |
| Уся Кваліфікація створена | 1934 | всі ліди, не тільки реклама |

**✅ ЗНАМЕННИК УЗГОДЖЕНО (рішення власника) — це НЕ блокер.** `conversion_ads` рахується і реалізована
в `core/metrics.ts` (`conversionAdsByMonth` / `ByManager` / `ByTeam` / `ByDirection`):

- **Знаменник = `adDealSql`** (рядок «CRM `adDealSql`» вище, ~914/міс) — «Прийнято реклами», з платним
  фільтром і вирізаною органікою (`traf_type <> 'organic'`, див. §3 `ads_accepted`).
- **Чисельник = ті самі ліди, що дійшли до `MONEY_ZONE`** (`EXPECT_ZONE ∪ {69716460, 60412544, 142}`)
  у воронці Повного циклу, дедуп по зерну.
- **Жива цифра — 11–13%** за місяць.

**🔴 ЧОМУ ОБИДВА КАНАЛИ (реклама і лідоген) МІРЯЮТЬСЯ ДО `MONEY_ZONE`:** доки фінішні лінії були різні,
цифри двох каналів були **несумісні між собою** — лідоген показував 3.8% проти 7.6% на тих самих лідах
лише тому, що його чисельник обривався раніше. Спільна фінішна лінія = єдина умова, за якої канали
можна класти поруч в одну таблицю й порівнювати.

**Що залишилось відкритим (не блокує конверсію):** розбіжність 1251 / 914 / 329 — це, за робочою
гіпотезою, **дві різні метрики**, які злиплись в одну:
- «Ліди з реклами (всі)» — скільки трафіку купили. Потрібно маркетингу для оцінки бюджету.
- «Кваліфіковані ліди з реклами» — скільки з них виявились живими. Потрібно продажам.
- Відношення = «% кваліфікації рекламного трафіку» — цінна метрика сама по собі.

Це питання **розкладки рекламного трафіку**, а не конверсії. `conversion_ads` уже має свій знаменник
(`adDealSql`) і від цієї розкладки не залежить.

---

## 4. ДОХІД / ОТРИМАНІ КОШТИ  ✅ вирішено (КРОК 2, 14.07.2026 — анкер по даті входу)

- **🎯 ЄДИНЕ ДЖЕРЕЛО — `backend/src/core/money.ts`. Рахувати гроші деінде (свій SQL по виручці) — ЗАБОРОНЕНО.**
- **«Отримані кошти»** = угоди, що ввійшли в **етап 9 (Оплата отримана `69716460`/`60412544`) АБО 10 (Успішна `142`)** у періоді. Анкер по **ДАТІ ВХОДУ В ЕТАП** (`deal_stage_events`), **дедуп по угоді (порахувати РАЗ)** — етапи 9 і 10 це ОДНІ Й ТІ САМІ гроші, без дедупу виручка подвоюється. Розклад без подвійного рахунку: `received = success(лише 142) ⊎ paidOnly(етап 9, ще не 142)`.
- **Жодних недатованих знімків у сумах виручки.** Знімок поточного етапу не має дати → «за червень» мутувало щодня, минулі місяці пливли. Прибрано в КРОЦІ 2.
- **«Очікування оплат»** = **етап 8 «Очікуємо оплату»** (`69716312`/`25044997`), анкер по даті входу. Етап 4 «Виставлення рахунку» (`100274340`) — операційний, у гроші **НЕ входить**.
- **Знімок «станом на зараз»** (угоди, що ЗАРАЗ на етапах 8+9) — окрема ПРОГНОЗ-картка, підпис «станом на зараз», НІКОЛИ не у виручці періоду.
- Одна назва: **«Отримані кошти»**. Синоніми «Виручка» / «Факт» / «Дохід успішно закритих» — прибрати з UI.
- **Мінусові угоди нетяться** (віднімаються). Свідоме правило власника. Лист рахує брутто → дельта при звірці **очікувана, не баг**. (Легасі-звірка: 852 376 нетто + 2×27 268 = 906 912 = Kommo ✅.)

---

## 5. СЕРЕДНІЙ ЧЕК  ✅ вирішено (КРОК 2, 14.07.2026 — ДВА чеки)

- **ДВА СЕРЕДНІ ЧЕКИ. Складати їх ЗАБОРОНЕНО:**
  - **Чек фактичний** = маржа(received: етапи 9∪10, дедуп по угоді) ÷ угоди цієї множини.
  - **Чек очікуваний** = маржа(етап 8 «Очікуємо оплату») ÷ угоди цієї множини.
- Окремо **`avg_check_success_only`** = маржа(**лише** етап 10, `142`) ÷ успішні угоди — **саме її звіряємо з листом**. Діапазон **2 600–2 900**.
- ⛔ Стара формула «(успішно + оплата-знімок + очікувані-знімок) ÷ машини» давала **5 313** — ЗМІШУВАЛА якорі і клала недатовані знімки в чисельник (порушення Правила №1.3). **ВИДАЛЕНО.** Знімки в чисельнику чека заборонені.
- Зовнішня звірка (**еталон**): **РПК-Яцика, серпень 2025 = 242 угоди / 680 655 ₴** (лист) → наше `avg_check_success_only` 243 / 681 356 = **2 805**, Δ 0.1% ✓. (Дельта до брутто-листа — правило мінусів §4.)
  ⚠️ **ЕТАЛОН ПРИЗУПИНЕНО 03.09.2026 — не відтворюється.** Причина й розклад — `docs/HANDOFF_QUICK.md`, блок «ЕТАЛОН РПК-ЯЦИКА ПРИЗУПИНЕНО». Числа тут — історичний запис, не чинний критерій.
- `avg_check_income` (легасі колонка листа) — тільки для історичної звірки, у UI не показується.
- **⚠️ РЕОУПЕНИ ТРАПЛЯЮТЬСЯ РЕГУЛЯРНО** (виправлено КРОК 4, опція Б; 14.07.2026). Угода може входити в 142 кілька разів (виграли → повернули → знову виграли; доведено — 10 угод/місяць в однієї команди). Рахується **РАЗ, за ФІНАЛЬНИМ входом** у 142 (= поточний `closed_at`). Якщо угоду повернули з успіху — у попередньому місяці вона НЕ була виграна. Раніше вважалось «1-2 виключення на всю історію» — **це неправда**. Дедуп має бути **МІЖ місяцями** (не лише всередині періоду), інакше пере-виграна угода рахується двічі. Реалізація: `success`=ЗАРАЗ 142 за `closed_at`; `paidOnly`=ЗАРАЗ етап 9 за останнім входом; у кожної угоди один анкер → один місяць.

---

## 6. ПРОРАХУНКИ ЛІДОГЕНУ  ⚠️ рішення

- **Істина з червня 2026** — реєстр бота.
- Історія до червня — імпорт із листа, позначена `source='imported'`.
- У графіках динаміки — **візуальна межа** на червні. Не робити вигляд, що ряд однорідний.
- ⚠️ **Це НЕ знаменник `conversion_leadgen`.** «Прорахунки» = скільки передали (реєстр бота);
  знаменник конверсії = `leadgen_touch` за `transfer_date` (§2). Різні метрики — не плутати.

---

## 7. МЕТРИКИ, ЩО ЗБІГАЮТЬСЯ (не чіпати)  ✅

«Успішно реалізовано» — всі 4 варіанти дали 852 376 / 320 угод. Одна реалізація вже є.

---

## 9. СЕГМЕНТИ УГОДИ (нові / постійні / лідоген) ✅ вирішено (КРОК 5, 14.07.2026)

**🎯 ЄДИНЕ ДЖЕРЕЛО — `backend/src/core/metrics.ts` (`segments`).**

- **Ярлик належить УГОДІ, за станом на момент угоди** — НЕ глобальна мітка клієнта. Той самий клієнт може дати «нову», потім «постійну» угоду.
- **Пріоритет-партиція (саме в цьому порядку):**
  1. **Лідоген** — угода атрибутована лідогену (`lead_channel = 'leadgen'`, що вже включає «реактивація б'є мітки», §3a).
  2. **Постійний** — НЕ лідоген **І** клієнт уже мав **≥1 попереднє успішне перевезення** (paid-угода `client_key` зі створенням РАНІШЕ за цю → тобто це 2+).
  3. **Новий** — решта.
- Приклад власника: реклама→закрили→лідоген реактивував = **лідоген**; той самий клієнт утретє до того ж менеджера = **постійний**.
- **🔴 ІНВАРІАНТ: Лідоген + Постійні + Нові = ЗАГАЛ, точно до угоди** (CASE вичерпний → сума = всі угоди повного циклу, створені в періоді). Перевіряти явно.
- ⚠️ **«Активний / замовклий / рідкісний-але-наш» — це НЕ сегмент, це шар ЛОЯЛЬНОСТІ** (`/loyalty`: regular/occasional/sleeping/lost, пороги `settings`). **НЕ змішувати.** «Постійний» тут = просто 2+ перевезення (частотну класифікацію робить лояльність окремо). Історична помилка: `/funnel-report` рахував «постійний» як lifetime `cnt≥2` (глобальна мітка), а не «попереднє до цієї угоди» — це різні цифри для клієнта з кількома оплатами в одному періоді.

---

## 10. ДЕБІТОРКА ✅ вирішено (КРОК 5, 15.07.2026)

**🎯 ЄДИНЕ ДЖЕРЕЛО — `core/metrics.ts` (`receivablesTotal`/`ByManager`/`ByClient`).** Дебіторка — **ЗНІМОК «винні ЗАРАЗ»**, БЕЗ періоду (борг — не період). У `core` дати заборонені на рівні типу (`SnapshotScope`, `from?: never`).

- Джерело — таблиця `receivables` (писар `syncReceivables`, TRUNCATE+insert): і **безнал** (Google-файл «выгрузка» → `receivable_invoices` → агрегат по клієнту), і **готівка** (`insertCashReceivables`, `CASH_RECEIVABLE_CLIENTS`; amount = **приход** `extractIncomeAmount`, не бюджет). `SUM(receivables) = SUM(receivable_invoices)` (звірка з джерелом).
- **🔴 ТОТАЛ = ПОВНА сума, включно з неатрибутованим боргом** (рішення власника 15.07). Стара логіка фільтрувала `manager_id IS NOT NULL` (INNER JOIN) і **ховала 1.575 млн** (гол. ПВК АРСЕНАЛ 1.56 млн — без відповідального в Kommo). Це **знайдений прихований борг, не помилка**. `core` бере повну суму (LEFT JOIN), а неатрибутований борг виходить **окремим рядком «Без менеджера»** (`managerId=null`), а не губиться.
- **⚠️ При міграції (КРОК 9) тотал підстрибне з 7.57 → 9.14 млн** — попередити команду (як з грошима/воронкою). Це не «core=стара» — стара була неправа; core звірено проти ДЖЕРЕЛА.
- Скоуп по менеджеру/команді природно виключає null-рядки → «Без менеджера» видно лише в загальному/адмін-розрізі.

---

## 8. ⚠️ ПОВНОТА ТАБЛИЦІ `deals` — інкрементальне дзеркало, НЕ повний зріз CRM  ✅ (виявлено 14.07.2026, коштувало дня)

- **`deals` — інкрементальне дзеркало по `updated_at`, а НЕ повний зріз CRM.** `syncKommo` тягне угоди фільтром `filter[updated_at][from]` (перший прохід узагалі — лише `now − 180 днів`). Угода, виграна й «заснула» (не оновлювалась) до старту синку, у `deals` **не потрапляє НІКОЛИ** — її `updated_at` старіший за вікно, і ніщо її не «торкає».
- **Наслідок:** історія за 2025 показувала лише **20–26% реальної виручки**. За червень–грудень 2025 у `deals` бракувало **25–83%** виграних угод повного циклу (~3.8 тис.). Різкий обрив на межі **грудень-2025 → січень-2026** (від січня — 0% втрат). Доведено по серпню 2025: з 681 угоди, що зайшла в 142, у `deals` було лише 118 (17%).
- **`deal_stage_events` ПОВНІШІ за `deals`** — бекфіляться по часовому вікну подій (незалежно від `updated_at`), тому дають перелік відсутніх угод.
- **Виправлено:** `jobs/backfillMissingDeals.ts` дотягнув 35 561 відсутню угоду (DISTINCT `kommo_id` з `deal_stage_events`, яких немає в `deals`) + `reclassifyAdChannel`/`backfillClientKey`.
- **🔴 РАЗОВИЙ БЕКФІЛ — НЕ ГАРАНТІЯ.** Інваріант **цілісності `deal_stage_events` ↔ `deals`** (норма = 0 угод з грошовими подіями, яких немає в `deals`) МАЄ бути в **КРОЦІ 5 (звірка)**. Саме він зловив би цю дірку роками раніше — і нічого не сигналило.

---

## Технічний борг з аудиту

- `backfillClientKey.ts` + `backfillClientKeys.ts` — дублікат. Визначити канонічний, другий видалити.
- Розділ `statistics` (інлайн у `Dashboard.tsx`) дублює `depstats` — злити.
- Ringostat `department` ненадійний (недобирає ~23%) → дзвінки рахувати **тільки** через `employee_fio` → команда. Старий фільтр `department='Менеджери з продажу'` — видалити.

---

# ПРОМТ 0.5 — Розслідування «Ліди з реклами»  ✅ ВИКОНАНО

> ⚠️ **АРХІВ. Це історія завдання, а НЕ чинна інструкція.** Виконано, результати інтегровані
> в §3/§3a/§3b (див. `AD_LEADS_INVESTIGATION.md`). Нічого з цього блоку не блокує метрики.
> Текст нижче збережено лише як запис того, що і навіщо перевіряли.

```
Задача: з'ясувати, що саме рахують три різні цифри «лідів з реклами», і звести їх
до узгоджених визначень. КОД НЕ ПИШЕМО (крім разових SELECT для аналізу).

Контекст (з AUDIT.md, червень 2026, відділ):
  Лист (ad_leads, imported):        1251
  CRM adDealSql:                     914
  Правило власника «3 поля»:         329   (Кваліфікація + непорожнє ≥1 з:
                                            utm_campaign 481997 / ADV_CAMP 2098331 / TRAF_SRC 2098327)
  Уся Кваліфікація створена:        1934

Гіпотеза (перевірити, НЕ приймати на віру): 1251 і 329 — різні метрики.
329 = тільки ті, що ДІЙШЛИ до Кваліфікації. 1251 = всі ліди з реклами, включно
з тими, що не дійшли.

Кроки:
1. Візьми правило «3 поля» і зніми з нього фільтр по стадії «Кваліфікація».
   Скільки виходить за червень? Якщо ≈1251 — гіпотеза підтверджена, і питання закрите.
2. Якщо ні — знайди, чим ще відрізняються множини. Конкретно:
   - Візьми 20 угод, які є в листі, але яких немає в правилі «3 поля».
     Подивись на них у Kommo. Що в них спільного? Яка стадія, які поля, який джерело?
   - Візьми 20 угод з adDealSql, яких немає в «3 полях», і навпаки. Те саме.
   Покажи мені конкретні угоди з ID, а не агреговані числа.
3. Розберись, ЧИМ adDealSql (914) відрізняється від правила «3 поля» (329).
   Він ширший — за рахунок чого саме? Інші utm? Інші стадії? Інші пайплайни?
4. З'ясуй, чи лист узагалі відтворюваний із CRM. Якщо в листі є ліди, яких
   у CRM немає ЗОВСІМ — так і скажи. Це означає, що лист заповнювався з іншого
   джерела (напр. рекламного кабінету), і тоді це третя, окрема метрика.

Результат — docs/AD_LEADS_INVESTIGATION.md:
  - Чи підтвердилась гіпотеза (крок 1) — так/ні, з числом.
  - Таблиця: множина / скільки / що в неї входить / що ні / приклади ID угод.
  - РЕКОМЕНДАЦІЯ: скільки насправді метрик потрібно, як кожну назвати і як рахувати.
  - Чи можна відновити історію листа з CRM. Якщо ні — з якої дати дані достовірні.

ЗАБОРОНЕНО:
- Обирати «правильну» цифру на свій розсуд.
- Писати «ймовірно» / «схоже». Прогони SELECT і подивись на конкретні угоди.
- Змінювати код.
```
